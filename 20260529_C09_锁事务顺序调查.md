# C-09 锁事务顺序调查

## 结论

C-09 当前不存在，已验证。

原报告描述为：`V4ScheduleLockService` 中存在 `TransactionTemplate + synchronized`，且 `synchronized` 锁可能在事务提交前释放，造成竞态条件。

当前代码未检出 `synchronized`、`ReentrantLock`、`ConcurrentHashMap` 或其他 JVM 级本地锁。`lock()` 与 `unlock()` 直接通过 `transactionTemplate.execute(...)` 包裹内部逻辑，不存在“本地锁先释放、数据库事务后提交”的释放顺序问题。

## 现状证据

- `V4ScheduleLockService.lock()` 调用 `transactionTemplate.execute(status -> lockInternal(request))`。
- `V4ScheduleLockService.unlock()` 调用 `transactionTemplate.execute(status -> unlockInternal(request))`。
- `V4ScheduleLockService` 中没有 `synchronized` 关键字。
- `lockInternal()` 先查已有锁，再插入锁记录；并发重复锁定由数据库唯一约束兜底，插入冲突会捕获 `DuplicateKeyException` 并转为业务异常。
- `schedule_locked_item` 通过 `active_key` 生成列和唯一索引限制同一活动锁重复：
  - `uk_locked_plan_item (plan_item_id, active_key)`
  - `uk_locked_schedule (schedule_id, active_key)`

## 判断

原始 C-09 不成立。当前并发一致性不是依赖 JVM 本地锁，而是依赖事务内写入和数据库唯一约束。

暂不改生产代码。

## 本轮验证

- 新增 `C09LockTransactionOrderInvestigationTest`，锁定：
  - `V4ScheduleLockService` 没有 `synchronized` / `ReentrantLock` / `ConcurrentHashMap`。
  - `lock()` 和 `unlock()` 均通过 `TransactionTemplate` 执行。
  - 锁定插入路径捕获 `DuplicateKeyException`。
  - 初始化脚本和 `SemesterSchemaInitializer` 仍定义 `schedule_locked_item` 活动锁唯一约束。

