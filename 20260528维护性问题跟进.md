# 20260528 维护性问题跟进

## 1. 背景

- 来源报告：`CODE_REVIEW_REPORT.md`
- 复核总账：`20260527bug验证报告.md`
- 严重问题执行单：`20260527严重问题修复计划.md`
- 当前分支：`chore/review-maintenance-followups`

严重问题主线已合并到 `main`。本文只跟踪 `M-13` 之后的低优先维护项，不再纳入严重问题修复计划。

## 2. 已处理

| 条目 | 提交 | 处理结果 |
|---|---|---|
| `M-17` | `0b98907 refactor(mapper): 收口任务和冲突统计契约` | `selectFilteredTaskIds` 更名为 `selectFilteredTasks`，Mapper 接口、XML 和调用方命名一致。 |
| `M-18` | `0b98907 refactor(mapper): 收口任务和冲突统计契约` | `selectDailyConflictCounts` 改为明确 DTO `ScheduleDailyConflictCounts`，移除 `resultType="HashMap"`。 |
| `M-33` | `c604af0 docs(config): 收敛本地环境示例占位` | `.env.example` 去掉 `root/root` 示例，改为本地低权限账号和强密钥占位说明。 |
| `M-47` | `4dd3ec7 fix(validation): 补齐分页参数边界约束` | 15 个分页列表 Controller 增加类级 `@Validated`，并为默认分页参数补齐 `@Min(1)` / `@Max(200)`；新增 `ControllerPaginationValidationTest`。 |
| `M-48` | `f150760 fix(validation): 补齐请求字符串长度约束` | 基础资料和教学任务请求表单按数据库 `VARCHAR` 长度补齐 `@Size`；新增 `ControllerRequestStringValidationTest` 锁定约束覆盖。 |
| `M-46` | `2841a4e fix(api): 对不存在资源返回 HTTP 404` | `TimeSlotController` 与同类 `AutoScheduleBatchController` 的详情不存在分支改抛 `BusinessException(404, ...)`，复用全局异常处理返回真实 HTTP 404；新增 `ControllerNotFoundStatusTest`。 |

验证：

```powershell
cd D:\paike\backend
$env:JWT_SECRET='test_secret_32_bytes_minimum_value_123'
mvn '-Dtest=!SchedulerBackendApplicationTests' test
```

结果：`Tests run: 68, Failures: 0, Errors: 0, Skipped: 0`。

## 3. 暂缓或需确认

| 条目 | 原结论 | 当前处理意见 |
|---|---|---|
| `M-49` | `ScheduleRuleWeightController` 的 `weight` 字段未检出 `@NotNull`。 | 暂缓。当前前端 API 将 `weight` 声明为可选，后端 `updateWeight` 支持部分更新；强制 `@NotNull` 会改变接口契约。 |
| `M-51` | `AdminUserInitializer` 将随机初始密码写到 stdout。 | 暂缓。随机密码若完全不输出，初始 admin 可能无法登录；改写日志也会增加持久化风险。需先确定初始化凭据交付方式。 |

## 4. 候选下一批

| 条目 | 原结论 | 建议 |
|---|---|---|
| `M-19` | 多个 Service 存在重复 `trimToNull` 辅助方法。 | 可低风险抽取公共字符串工具，但需避免为了少量重复引入过度抽象。 |

## 5. 不建议短期展开

| 条目 | 原结论 | 原因 |
|---|---|---|
| `M-13` | Controller 直接注入 Mapper。 | 范围较大，属于架构分层重构；缺少直接故障证据。 |
| `M-14` | 多处 `Map<String, Object>`。 | 可读性问题成立，但需要逐接口替换 DTO，适合随功能模块渐进处理。 |
| `M-15` | `TimetableController` 承载导出逻辑。 | 可维护性问题，拆服务前应先确认导出行为和测试边界。 |
| `M-16` | Entity 中有多个 `@TableField(exist = false)` 视图字段。 | 改动会影响前后端返回结构，适合结合 DTO 分层整体设计。 |
