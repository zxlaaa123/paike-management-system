# 20260602 N23 N31 ECharts 类型收口

## 结论

`N-23`、`N-31` 已修复。当前前端源码未检出显式 `any`。

## 调查

- `ScheduleCharts.vue` 原有 5 处 `any`：
  - heatmap tooltip formatter 参数。
  - 教师、教室、密度、雷达图点击参数。
- V5 页面另有 2 处表格事件内联 `any`：
  - `CandidatePositionView.vue` 当前行变化。
  - `RepairTaskDetailView.vue` 建议多选变化。

## 修改

- `EChartPanel.vue` 的 `chartClick` emit 从 `unknown` 改为 `ECElementEvent`。
- `ScheduleCharts.vue` 的图表点击 handler 改用 `ECElementEvent` 派生类型。
- heatmap tooltip formatter 改用 `unknown` + `readDensityValue()` 类型守卫。
- V5 两处表格事件改为具名 typed handler。

## 验证

- 脚本扫描：
  - `anyHits=0`
- `cd D:\paike\frontend; npm run build`
  - `vue-tsc -b && vite build` 通过。
  - Vite 构建完成时间：7.70 秒。
