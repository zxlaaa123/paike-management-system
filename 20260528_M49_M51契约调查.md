# 20260528 M-49 / M-51 契约调查

## 结论

| 条目 | 结论 | 建议 |
|---|---|---|
| `M-49` | 不建议直接给 `WeightUpdateRequest.weight` 加 `@NotNull`。 | 保留当前部分更新契约；如产品要求“单条保存必须包含权重”，应同步修改前端类型、后端校验和接口说明。 |
| `M-51` | 不建议直接移除随机初始密码 stdout 输出。 | 保留当前首次初始化交付方式；生产环境应通过 `ADMIN_DEFAULT_PASSWORD` 显式注入初始密码。 |

## M-49 证据

- 后端 `ScheduleRuleWeightController.update()` 将 `weight`、`enabled`、`description` 三个字段传给 `ScheduleRuleWeightService.updateWeight()`。
- `ScheduleRuleWeightService.updateWeight()` 只在字段非空时更新对应列，属于部分更新语义。
- 前端 `updateRuleWeight()` 类型声明中 `weight?: number`、`enabled?: number`、`description?: string` 均为可选。
- 当前页面保存单行时传入 `weight` 和 `enabled`，但接口层并未声明必须全量提交。

直接增加 `@NotNull` 会把部分更新接口收窄成“必须提交 weight”，属于接口契约变更，不应作为普通校验 bug 修复。

## M-51 证据

- `AdminUserInitializer` 支持 `app.admin.default-password`，可由环境变量 `ADMIN_DEFAULT_PASSWORD` 注入。
- 未配置时生成随机密码，并只输出到 stdout，代码注释明确避免写入日志文件。
- `backend/.env.example` 已说明 `ADMIN_DEFAULT_PASSWORD` 可选，不设置则随机生成并打印到 stdout。
- `README.md` 已说明本地启动可设置 `ADMIN_DEFAULT_PASSWORD`，未设置时查看首次启动输出。

直接移除 stdout 会导致未配置初始密码时无法取得首次 admin 登录凭据；改写到日志或文件反而可能扩大明文凭据持久化范围。

## 后续触发条件

- `M-49`：产品确认接口不再支持部分更新，或前端改为始终提交完整规则权重。
- `M-51`：运维确认初始凭据交付渠道，例如一次性密钥文件、部署 Secret、强制配置缺失即启动失败。
