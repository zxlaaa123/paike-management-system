# M-38 规则缓存失效调查

## 结论

不成立，当前代码已有写入失效机制。

原条目指出 `ScheduleRuleService` 使用 `ConcurrentHashMap` 缓存且未见过期机制。复查后确认：

- `getIntValue()` / `getBoolValue()` 会缓存规则值。
- `updateRules()` 成功写库后调用 `ruleCache.clear()`。
- `resetToDefault()` 成功写库后调用 `ruleCache.clear()`。

因此当前确实没有 TTL 过期机制，但规则由管理接口更新或恢复默认值后，缓存会主动失效，不会一直返回旧值。

## 判断

当前实现属于“写入清缓存”，不是“无失效缓存”。对本项目的规则配置场景，这比固定 TTL 更直接，也避免排课过程中规则值被时间过期打断。

本轮不改生产代码。

## 本轮验证

新增 `ScheduleRuleServiceTest` 锁定当前行为：

- 同一规则重复读取会命中缓存。
- `updateRules()` 后再次读取会访问数据库并拿到新值。
- `resetToDefault()` 后布尔规则不再返回旧缓存。

后续如果出现绕过 `ScheduleRuleService` 直接写 `ScheduleRuleConfigMapper` 的路径，再单独评估是否需要事件通知或统一写入口。
