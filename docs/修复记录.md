# 代码设计问题修复记录

> 修复日期：2026-05-14
> 依据文件：`代码设计问题审查报告核实记录.md`

---

## 已修复问题

### 🔴 严重问题

| # | 问题 | 修复方式 | 状态 |
|---|------|---------|------|
| 1 | ScheduleController.list() 全量查询+内存分页 | 新增 selectFilteredScheduleIds SQL 查询 + 数据库分页 | ✅ |
| 2 | TeachingTaskController.list() 全量查询+内存分页 | 新增 selectFilteredTaskIds SQL 查询 + 数据库分页 | ✅ |
| 3 | UnscheduledTaskService.list() 全量查询+内存分页 | 无关联过滤条件时走数据库分页，有时保留内存过滤 | ✅ |
| 4 | CORS 配置不安全 | 明确指定 `localhost:5173` 和 `127.0.0.1:5173` | ✅ |
| 5 | ScheduleConflictService N+1 查询 | 批量查询 teachingTask 放入 Map | ✅ |
| 6 | ScheduleController.fillRelation() N+1 | 改为调用批量 fillRelations 方法 | ✅ |
| 8 | AutoScheduleService 内部 DTO | 抽取到 service.dto 包独立类 | ✅ |

### 🟠 中等问题

| # | 问题 | 修复方式 | 状态 |
|---|------|---------|------|
| 10 | TimetableVo 位置不当 | 移到 controller/vo/ 子包 | ✅ |
| 12 | TimetableController 重复逻辑 | 抽取 querySchedulesByTaskField 通用方法 | ✅ |
| 13 | 硬编码字符串未枚举化 | 创建 CourseType、RoomType、ScheduleSourceType 枚举 | ✅ |
| 15 | gradeName 不持久化 | 移除 @TableField(exist=false)，创建迁移脚本 | ✅ |
| 20 | 前端空 catch 块 | 全部添加 console.error | ✅ |
| 24 | 前端 API 缺少空值检查 | 51 处添加 r.data 空值检查 | ✅ |
| 25 | BaseLayout 相同图标 | 排课分析改用 PieChart 图标 | ✅ |
| 26 | HelloWorld.vue 残留 | 删除 | ✅ |
| 29 | updateStatus 缺少 @Valid | 全部 Controller 加 @Valid + StatusForm 加 @NotNull | ✅ |
| 30 | 软删除不统一 | 全部改为 deleteById() | ✅ |
| 34 | ClassTimetableView 加载态混淆 | 增加独立的加载中空状态 | ✅ |

### 🟡 轻微问题

| # | 问题 | 修复方式 | 状态 |
|---|------|---------|------|
| 23 | Controller Form 类用 @Data | 改为 @Getter（StatusForm 保持 @Data） | ✅ |

---

## 未修复问题（可接受的技术债务）

| # | 问题 | 未修复原因 |
|---|------|-----------|
| 9 | Controller 直接注入 Mapper | 架构层面改动量巨大，需创建 5 个 Service 类 |
| 14 | ScheduleRuleConfig 字符串存储 | 需改表结构，当前已有校验保护 |
| 16 | Schedule 实体混合字段 | 需创建 VO 类，改动量较大 |
| 18 | 前端 ID 用 number | 当前数据量不会超限，理论风险 |
| 21 | TimeSlot 缺少审计字段 | 预置数据不需要软删除，可接受差异 |
| 32 | JWT 弱密钥默认值 | 生产环境通过环境变量覆盖 |
| 33 | 默认弱密码 | 生产环境应修改，开发环境可接受 |
| 40 | Schedule 冗余字段无一致性保障 | 需数据库触发器或去掉冗余字段 |

---

## 新增文件

### 后端
- `backend/src/main/java/com/paike/scheduler/common/enums/CourseType.java`
- `backend/src/main/java/com/paike/scheduler/common/enums/RoomType.java`
- `backend/src/main/java/com/paike/scheduler/common/enums/ScheduleSourceType.java`
- `backend/src/main/java/com/paike/scheduler/service/dto/AutoScheduleRequest.java`
- `backend/src/main/java/com/paike/scheduler/service/dto/AutoScheduleResult.java`
- `backend/src/main/java/com/paike/scheduler/controller/vo/TimetableVo.java`
- `backend/src/main/resources/mapper/ScheduleMapper.xml`
- `backend/src/main/resources/mapper/TeachingTaskMapper.xml`
- `backend/src/main/resources/db/v2_alter_score_report.sql`

### 删除文件
- `backend/src/main/java/com/paike/scheduler/controller/TimetableVo.java`（旧位置）
- `frontend/src/components/HelloWorld.vue`

---

## 编译验证

```
mvn compile → BUILD SUCCESS ✅
```
