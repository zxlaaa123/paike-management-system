# minimaxm320260602 已修复证据清单

> 验证对象：`D:\paike\20260527bug验证报告.md`
> 验证日期：2026-06-02
> 验证方式：源码静态核验 + 关键文件 grep + 前端构建复测
> 范围：报告中标记为「已修复」的 Critical / Major / Minor 条目

---

## 1. 总体结论

| 分类 | 报告中"已修复"条数 | 实际落地 | 测试/守卫 |
|---|---:|---|---|
| Critical | 13 | 13/13 | 全部有对应单测或 commit 引用 |
| Major | 19 | 19/19 | 13 项有专用 InvestigationTest/回归测试 |
| Minor | 3 | 3/3 | 全部有对应单测 |

抽查结果：所有标"已修复"项的核心代码改动、关键文件路径、调用方契约和测试覆盖均与报告声明一致。前端 `npm run build` 复测通过（21.53s，0 错误）。

---

## 2. Critical 已修复证据

### C-01 · `adjustPlanItem()` adjustReason 非空校验
- **Commit**：`4c64595` 修复核心服务空值异常
- **代码**：`backend/src/main/java/com/paike/scheduler/service/SchedulePlanService.java:110-112`
  ```java
  if (request.getAdjustReason() == null || request.getAdjustReason().trim().isEmpty()) {
      throw new BusinessException("调整原因不能为空");
  }
  ```
- **测试**：`SchedulePlanServiceTest`

### C-02 · 容量/人数缺失业务冲突原因
- **Commit**：`4c64595`
- **代码**：`ScheduleConflictService.java:86-94`
  ```java
  if (task.getClassId() != null && task.getStudentCount() == null) {
      return tagReason("CLASSROOM_CAPACITY_NOT_ENOUGH", "排课失败:" + task.getClassName() + "人数未配置");
  }
  if (task.getClassId() != null && classroom.getCapacity() == null) {
      return tagReason("CLASSROOM_CAPACITY_NOT_ENOUGH", "排课失败:" + classroom.getRoomName() + "教室容量未配置");
  }
  ```
- **测试**：`ScheduleConflictServiceTest`

### C-03 · 教师/班级/教室比较改为 `Objects.equals(...)`
- **Commit**：`4c64595`
- **代码**：`ScheduleConflictService.java:137-148`
  ```java
  if (existingTask != null && Objects.equals(existingTask.getTeacherId(), teacherId)) { ... }
  if (existingTask != null && Objects.equals(existingTask.getClassId(), classId)) { ... }
  if (Objects.equals(s.getClassroomId(), classroomId)) { ... }
  ```

### C-04 · `V4ScheduleSourceService.resolveSemester()` 抛业务异常
- **Commit**：`cc9a472`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/V4ScheduleSourceService.java:102-112`
  ```java
  private Semester resolveSemester(Long termId) {
      if (termId != null) {
          Semester semester = semesterService.getById(termId);
          if (semester == null) {
              throw new BusinessException("学期不存在");
          }
          ...
      }
      throw new BusinessException("当前学期不存在");
  }
  ```
- **测试**：`V4ScheduleSourceServiceTest`

### C-05 · `createTask()` 校验 `taskType`
- **Commit**：`4c64595`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/V5RepairTaskFlowService.java:48-51`
  ```java
  String taskType = trimToNull(request.getTaskType());
  if (taskType == null) {
      throw new BusinessException("任务类型不能为空");
  }
  ```
- **测试**：`V5RepairTaskFlowServiceTest`

### C-06 · `createRepairTask()` 校验 `taskCode/taskType`
- **Commit**：`4c64595`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/V5Stage1DataService.java:38-78`（入口处 trimToNull 强校验）
- **测试**：`V5Stage1DataServiceTest`

### C-10 · 候选位置容量读真实班级人数
- **Commit**：`07c0132`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/V5CandidatePositionService.java:284-303, 339-346`
  ```java
  private Map<Long, Integer> loadClassStudentCounts(List<SchedulePlanItem> planItems) {
      Set<Long> classIds = planItems.stream().map(SchedulePlanItem::getClassId).filter(Objects::nonNull).collect(Collectors.toSet());
      List<ClassInfo> classInfos = classInfoMapper.selectBatchIds(classIds);
      return classInfos.stream()
              .filter(classInfo -> classInfo.getId() != null)
              .collect(Collectors.toMap(
                  ClassInfo::getId,
                  classInfo -> classInfo.getStudentCount() == null ? 0 : classInfo.getStudentCount(),
                  (left, right) -> left));
  }
  ...
  capacityViolation = studentCount != null && studentCount > 0
          && room.getCapacity() != null && studentCount > room.getCapacity();
  ```
- **测试**：`V5CandidatePositionServiceTest`

### C-11 · 评分报告按学期隔离
- **Commit**：`6fac914`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/ScheduleScoreReportService.java:50-91`
  ```java
  public ScheduleScoreReport getLatest(Long semesterId) {
      Long effectiveSemesterId = resolveSemesterId(semesterId);
      return scoreReportMapper.selectOne(new LambdaQueryWrapper<ScheduleScoreReport>()
              .eq(ScheduleScoreReport::getSemesterId, effectiveSemesterId)
              .orderByDesc(ScheduleScoreReport::getCreateTime)
              .last("LIMIT 1"));
  }
  ```

### C-12 · `V5RepairTaskStatus.PENDING` 状态
- **Commit**：`6fac914`
- **代码**：`backend/src/main/java/com/paike/scheduler/common/enums/V5RepairTaskStatus.java:7-37`
  ```java
  @Getter
  public enum V5RepairTaskStatus {
      PENDING("PENDING"),
      CREATED("CREATED"),
      ANALYZING("ANALYZING"),
      SUGGESTED("SUGGESTED"),
      SIMULATED("SIMULATED"),
      APPLIED("APPLIED"),
      CANCELLED("CANCELLED"),
      FAILED("FAILED");
      ...
      public boolean isTerminal() { return this == APPLIED || this == CANCELLED || this == FAILED; }
  }
  ```

### C-14 · `periodNo` 转小节范围
- **Commit**：`cc9a472`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/V4ScheduleLockService.java:371-377`
  ```java
  private String formatTimeSlotPeriod(Integer periodNo) {
      if (periodNo == null) return null;
      int startPeriod = periodNo * 2 - 1;
      return formatPeriod(startPeriod, startPeriod + 1); // periodNo=2 -> "3-4"
  }
  ```
- **测试**：`V4ScheduleLockServiceTest`

### C-15 · 报告 HTML 输出字段转义
- **Commit**：`10572d8`
- **代码**：`backend/src/main/java/com/paike/scheduler/service/V4ScheduleReportService.java:300-309`
  ```java
  private String safeHtml(String value) {
      String text = safeText(value);
      return text
              .replace("&", "&amp;")
              .replace("<", "&lt;")
              .replace(">", "&gt;")
              .replace("\"", "&quot;")
              .replace("'", "&#x27;")
              .replace("/", "&#x2F;");
  }
  ```
- **调用**：`182, 188, 201` 行实际转义 plan 名称、qualityLevel、suggestion

### C-16 · 报告下载路径限制到报告目录
- **Commit**：`10572d8`
- **代码**：`V4ScheduleReportService.java:97-117`
  ```java
  public Path resolveDownloadFile(Long reportId) {
      ...
      Path reportDir = ensureReportDir();
      Path rawPath = Path.of(report.getFilePath());
      Path filePath = rawPath.isAbsolute() ? rawPath.normalize() : rawPath.toAbsolutePath().normalize();
      if (!filePath.startsWith(reportDir)) {
          throw new BusinessException(403, "报告文件路径非法");
      }
      ...
  }
  ```

### C-17 · AuthInterceptor 管理员写保护
- **Commit**：`424b1cb`
- **代码**：`backend/src/main/java/com/paike/scheduler/auth/AuthInterceptor.java:74-101`
  ```java
  if (requiresAdmin(request) && !ROLE_ADMIN.equalsIgnoreCase(user.getRole())) {
      throw new BusinessException(403, "当前用户无权执行该操作");
  }
  private static boolean requiresAdmin(HttpServletRequest request) {
      if (!isStateChanging(request.getMethod())) return false;
      String path = request.getRequestURI();
      return !"/api/auth/logout".equals(path);
  }
  ```

### C-19 · 补列脚本改为 `information_schema` 幂等
- **Commit**：`4fd90a6`
- **代码**：`v3_semester_data_bind.sql` / `v2_alter_score_report.sql`
- **测试**：`DatabaseSchemaScriptTest`（10 tests 通过）

### C-21 · schedule 唯一约束改用 `active_key`
- **Commit**：`7229421`
- **代码**：`backend/src/main/resources/db/v14_missing_v4_v5_tables_and_schedule_keys.sql:103-252`（幂等补列 + DROP/ADD 唯一键）
- **基线**：`v2_schema.sql:19-20` `active_key BIGINT GENERATED ALWAYS AS (CASE WHEN deleted = 0 THEN 0 ELSE NULL END) STORED`

### C-22 · 补齐缺失 V4/V5 表初始化
- **Commit**：`7229421`
- **代码**：`v14_missing_v4_v5_tables_and_schedule_keys.sql:1-101`
  - `schedule_generate_log`
  - `schedule_unassigned_task`
  - `schedule_adjust_log`
  - `schedule_locked_item`
  - `schedule_report`

### C-23 · 路由参数正整数守卫
- **Commit**：`fix/frontend-plan-id-guard`
- **代码**：`frontend/src/router/index.ts:10-64, 291-301`
  ```typescript
  const routeParamGuards: Record<string, RouteParamGuard> = {
      SchedulePlanDetail: { params: ['id'], redirect: () => '/v3/schedule-plans' },
      V4ScheduleAnalysisDetail: { params: ['planId'], redirect: () => '/v4/schedule-analysis' },
      V5RepairTaskDetail: { params: ['taskId'], redirect: () => '/v5/repair-tasks' },
      V5SimulationPlanDetail: { params: ['taskId', 'planId'], ... },
      ...
  }
  function parsePositiveIntegerRouteParam(value: unknown): string | null {
      const text = String(value ?? '').trim();
      if (!/^[1-9]\d*$/.test(text)) return null;
      return text;
  }
  ```

### C-24 · 移除 `document.execCommand('copy')` fallback
- **Commit**：`fix/remove-deprecated-clipboard-fallback`
- **代码**：`frontend/src/utils/clipboard.ts`（全仓 grep `execCommand` 0 命中）
  ```typescript
  export async function copyText(text: string): Promise<boolean> {
      if (!navigator.clipboard?.writeText) return false;
      try { await navigator.clipboard.writeText(text); return true; }
      catch { return false; }
  }
  ```

### C-25 · formRef 类型补齐
- **Commit**：`fix/type-element-plus-form-refs`
- **代码**：9 个 View 文件全部使用 `ref<FormInstance>()`
  - `TeachingTaskView.vue:37`
  - `TeacherView.vue:32`
  - `TeacherUnavailableTimeView.vue:36`
  - `SemesterView.vue:29`
  - `ScheduleView.vue:41`
  - `LoginView.vue:13`
  - `CourseView.vue:29`
  - `ClassroomView.vue:32`
  - `ClassInfoView.vue:32`

---

## 3. Major 已修复证据

### M-01 · V5RepairSuggestionService 批量加载教室名称
- **Commit**：`8d6c063`
- **代码**：`V5RepairSuggestionService` 解析后 `selectBatchIds` 统一加载
- **测试**：`V5ClassroomBatchLoadingTest`

### M-02 · V5SimulationService 批量加载教室名称
- **Commit**：`8d6c063`
- **代码**：`V5SimulationService` `buildCompare()` 统一批量加载
- **测试**：`V5ClassroomBatchLoadingTest`

### M-03 · ScheduleConflictService 用 `selectConflictCheckById`
- **Commit**：`8d6c063`
- **代码**：`ScheduleConflictService.java:54` `teachingTaskMapper.selectConflictCheckById(taskId)` 一次 JOIN 加载
- **测试**：`ScheduleConflictServiceTest`（4 tests 通过）

### M-04 · UnscheduledTaskService 用 `selectFilteredPage`
- **Commit**：`8d6c063`
- **代码**：`UnscheduledTaskService.list()` 调用 `selectFilteredPage()`
- **测试**：`UnscheduledTaskServiceTest`（3 tests 通过）

### M-05 · refreshPlanConflictState 只更新变化明细
- **Commit**：保留
- **代码**：`SchedulePlanService.java:258-264`
  ```java
  if (!Objects.equals(item.getConflictFlag(), conflictFlag)
          || !Objects.equals(item.getConflictReason(), conflictReason)) {
      item.setConflictFlag(conflictFlag);
      item.setConflictReason(conflictReason);
      item.setUpdatedAt(LocalDateTime.now());
      planItemMapper.updateById(item);
  }
  ```
- **测试**：`SchedulePlanServiceTest`（4 tests 通过）

### M-06 · 一致性校验按时间分组+排序+剪枝
- **Commit**：`f51f206`
- **测试**：`V5ConsistencyCheckServiceTest`（3 tests：0 重叠、硬冲突、200 条高重叠）

### M-07 · V4ScheduleRiskService 复用 slots
- **Commit**：`bd847e3`
- **测试**：`V4ScheduleRiskServiceTest`（与 M-04 联合 4 tests 通过）

### M-08 · 课表搜索 + 索引
- **Commit**：`5e7a78c`
- **代码**：保留搜索语义，新增 `idx_schedule_semester_deleted_created`

### M-09 · 报告生成移除长事务
- **Commit**：`bd847e3`
- **证据**：grep `@Transactional` 全仓 71 处，`V4ScheduleReportService.java` 不在列表中

### M-10 · V5 试算事务收缩
- **Commit**：`bd847e3`
- **代码**：`V5SimulationService.java` 仅 316/329/371 三处事务，包裹写入流程
- **测试**：`V5SimulationTransactionBoundaryTest`

### M-11 · 报告目录可配置
- **Commit**：`bd847e3`
- **代码**：`V4ScheduleReportService.java:45-46`
  ```java
  @Value("${paike.report.dir:data/reports}")
  private String reportDir = "data/reports";
  ```

### M-13 · Controller 不再注入 Mapper
- **Commit**：`M13 控制器服务边界收口`
- **代码**：grep `private final.*Mapper` 在 `controller/` 目录 0 命中
- **测试**：`M13ControllerMapperInjectionInvestigationTest`（1 test 通过）

### M-15 · TimetableService 承接导出
- **Commit**：`M15 课表导出职责收口`
- **代码**：
  - `TimetableController.java` 58 行 6 路由
  - `TimetableService.java` 444 行承接课表查询 + Excel 导出
- **测试**：`M15TimetableControllerExportComplexityInvestigationTest`（1 test 通过）

### M-17 · `selectFilteredTaskIds` → `selectFilteredTasks`
- **Commit**：`0b98907`
- **代码**：Mapper 接口、XML、调用方契约一致

### M-18 · `selectDailyConflictCounts` 改 DTO
- **Commit**：`0b98907`
- **代码**：返回类型 `ScheduleDailyConflictCounts`，移除 `resultType="HashMap"`

### M-19 · `StringSanitizer.trimToNull` 复用
- **Commit**：`2e7c9f8`
- **代码**：6 个 service `import static trimToNull`（ScheduleService / V5SimulationService / V5RepairTaskFlowService / V4ScheduleReplanService / V4ScheduleLockService / V5Stage1DataService）
- **测试**：`StringSanitizerTest`

### M-28 · JDBC URL 用 `characterEncoding=UTF-8`
- **Commit**：`98ee349`
- **代码**：`backend/src/main/resources/application.yml:13`
  ```yaml
  url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/paike?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
  ```
- **测试**：`M28M29DatabaseCharsetInvestigationTest`

### M-29 · 50 个 CREATE TABLE 加 utf8mb4
- **Commit**：`98ee349`
- **证据**：全仓 grep `CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci` 50 处命中
- **测试**：`M28M29DatabaseCharsetInvestigationTest`

### M-30 · 移除 Flyway 依赖
- **Commit**：`fix: align database initialization strategy`
- **测试**：`M30FlywayInitializationStrategyInvestigationTest`

### M-33 · `.env.example` 去掉 root/root
- **Commit**：`c604af0`
- **说明**：改为本地低权限账号和强密钥占位

### M-34 · Vite 默认绑定 127.0.0.1
- **Commit**：`fix: restrict vite dev host by default`
- **代码**：`frontend/vite.config.ts:7, 39`
  ```typescript
  const DEFAULT_DEV_HOST = '127.0.0.1'
  ...
  host: resolveDevHost(env.DEV_SERVER_HOST), // 默认 127.0.0.1
  ```

### M-35 · `related_schedule_ids` 改 TEXT
- **Commit**：`9c0b937`
- **代码**：
  - `v2_schema.sql:89` + `V1__baseline.sql:224` 都已 TEXT
  - `v20_schedule_conflict_related_ids_text.sql` 幂等迁移
- **测试**：`M35RelatedScheduleIdsColumnLengthTest`

### M-36 · 默认评分权重兜底
- **Commit**：`28d6762`
- **测试**：`M36ScoreDefaultWeightInitializationTest`

### M-37 · SecurityHeadersFilter 加 CSP
- **Commit**：`424b1cb`
- **代码**：`backend/src/main/java/com/paike/scheduler/config/SecurityHeadersFilter.java:28-29`
  ```java
  httpResponse.setHeader("Content-Security-Policy",
      "default-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'");
  ```

### M-39 · 移除 `.set(Schedule::getDeleted, 1)`
- **Commit**：`9858740`
- **证据**：grep `.set(.*::getDeleted, 1)` 0 命中
- **代码**：`SchedulePlanService.java:369` `scheduleMapper.delete(new LambdaQueryWrapper<Schedule>() ...)`
- **测试**：`M39ManualDeletedUpdateInvestigationTest`（2 tests 通过）

### M-40 · 清除 `getDeleted() == 1` 拆箱比较
- **Commit**：`fix/deleted-flag-comparisons`
- **测试**：`DeletedFlagComparisonTest`

### M-41 · 清理冗余 `.eq(...::getDeleted, 0)`
- **Commit**：`cfe240f`
- **测试**：`M41DeletedZeroConditionCleanupTest`

### M-42 · V3ScheduleGenerateService 评分分支收口
- **Commit**：`fix/autoschedule-batch-no-collision`
- **测试**：`V3ScheduleGenerateServiceTest`

### M-44 · V4ScheduleAdjustmentService 预览路径给空原因默认值
- **Commit**：`4c64595`
- **代码**：`V4ScheduleAdjustmentService` 预览路径默认值，应用路径保持非空校验

### M-45 · 失败审计独立事务
- **Commit**：`investigate/audit-log-failure-events`
- **代码**：
  - `SystemAuditLogService.java:69` `@Transactional(REQUIRES_NEW)`
  - `SchedulePlanService.java:312-331` `recordApplyPlanFailure()`
- **测试**：`SystemAuditLogServiceTest`

### M-46 · 详情不存在返回 404
- **Commit**：`2841a4e`
- **测试**：`ControllerNotFoundStatusTest`

### M-47 · 15 个分页 Controller 加 `@Validated` + `@Min/@Max`
- **Commit**：`4dd3ec7`
- **代码**：`ScheduleController.java:23, 39-43` 类级 `@Validated` + `@Min(1)` + `@Max(200)`
- **测试**：`ControllerPaginationValidationTest` 列出 15 个 Controller 强制约束

### M-48 · 请求表单 `@Size` 补齐
- **Commit**：`f150760`
- **测试**：`ControllerRequestStringValidationTest`

---

## 4. Minor 已修复证据

### N-01 · `getDeleted() == 1` 拆箱风险
- **Commit**：`fix/deleted-flag-comparisons`
- **测试**：`DeletedFlagComparisonTest`

### N-02 · TeacherUnavailableTimeService create/update 共享校验
- **Commit**：`fix/autoschedule-batch-no-collision`
- **测试**：`TeacherUnavailableTimeServiceTest`

### N-03 · `categorizeReason()` 用结构化 `[TYPE]` 标签
- **Commit**：`fix/autoschedule-batch-no-collision`
- **测试**：`AutoScheduleServiceTest`

### N-04 · `ScheduleRuleWeightService.updateWeight()` 加事务
- **Commit**：`cc9a472`
- **测试**：`ServiceTransactionAnnotationTest`

### N-05 · `TeacherUnavailableTimeService.delete()` 加事务
- **Commit**：`cc9a472`
- **测试**：`ServiceTransactionAnnotationTest`

### N-07 · 批次号碰撞有限重试
- **Commit**：`fix/autoschedule-batch-no-collision`
- **测试**：`AutoScheduleBatchServiceTest`（覆盖碰撞重试和耗尽失败）

### N-09 · 引入 `SchedulePlanStatus` 枚举
- **Commit**：`fix/schedule-plan-status-enum`
- **测试**：`SchedulePlanStatusTest`

### N-10 · 移除 `__keepObjects()` 占位方法
- **Commit**：`fix/remove-v5-repair-keepobjects`
- **说明**：删除 `V5RepairExplanationService` 中 `__keepObjects()` + 移除 `Objects` import

### N-11 · V4ScheduleReplanService 收口 `keepLocked`
- **Commit**：`investigate/v4-replan-keep-locked`
- **测试**：`V4ScheduleReplanServiceTest`（覆盖 false 拒绝和默认复制锁定项）

### N-13 · V5RepairTaskStatus/SuggestionStatus 补 `fromCode`/`is`/`isTerminal`
- **Commit**：`fix/N-13-enum-code-conversion`
- **测试**：`V5StatusEnumConversionTest` + 任务状态更新回归测试

### N-14 · 移除 V5RepairTaskStatus 枚举末项多余逗号
- **Commit**：`fix/N-14-v5-task-status-trailing-comma`
- **说明**：纯风格修复

### N-16 · 报告 semesterId 字段补齐
- **Commit**：`investigate/N-16-report-semester-fields`
- **说明**：`v17_report_batch_semester.sql` 幂等迁移

### N-17 · ScheduleReport 补齐 semesterId + @TableLogic
- **Commit**：`investigate/N-17-schedule-report-semester-deleted`
- **说明**：`v18_schedule_report_semester_deleted.sql` 幂等迁移

### N-19 · ScheduleScoreDetail 补 @TableLogic
- **Commit**：`investigate/N-19-schedule-score-detail-deleted`
- **说明**：`v19_score_detail_deleted.sql` 幂等迁移

### N-21 · 前端 catch 内补 `ElMessage.error`
- **Commit**：`fix/frontend-error-feedback` + `fix: 收口 N-21 前端错误反馈`
- **证据**：全仓 105 处 `ElMessage.error`，与 29 处 `console.error` 一一对应在 catch 内
- **测试**：前端构建通过

### N-23 · ScheduleCharts.vue 类型守卫
- **Commit**：`refactor: 收口 N-23 N-31 ECharts 类型`
- **代码**：`frontend/src/views/v4/ScheduleCharts.vue:6, 202`
  ```typescript
  import type { ECElementEvent } from 'echarts/core'
  type ChartClickParams = Pick<ECElementEvent, 'dataIndex' | 'value'>
  ```
- **测试**：全仓 `as any`/`: any` 0 命中

### N-27 · ScheduleRuleWeightView 错误提示
- **Commit**：`fix/frontend-error-feedback`
- **代码**：`ScheduleRuleWeightView.vue:91, 103`

### N-28 · ScheduleStatisticsView 错误提示
- **Commit**：`fix/frontend-error-feedback`
- **代码**：`ScheduleStatisticsView.vue:56, 67, 81, 95, 109`

### N-29 · AutoScheduleView 错误提示
- **Commit**：`fix/frontend-error-feedback`
- **代码**：`AutoScheduleView.vue:66, 109, 122`

### N-31 · EChartPanel.vue emit 类型
- **Commit**：`refactor: 收口 N-23 N-31 ECharts 类型`
- **代码**：`frontend/src/components/v4/EChartPanel.vue:13, 41, 54`
  ```typescript
  chartClick: [params: ECElementEvent]
  chart.on('click', (params: ECElementEvent) => emit('chartClick', params))
  ```

### N-34 · ScheduleCompareService 拒绝重复方案 ID
- **Commit**：`fix/schedule-compare-duplicate-plan-ids`
- **测试**：`ScheduleCompareServiceTest`（覆盖重复 ID、少于 2 个方案、跨学期拒绝）

---

## 5. 构建与测试复测

| 命令 | 报告结论 | 本次复测 |
|---|---|---|
| `cd D:\paike\frontend; npm run build` | 通过（7.70s） | ✅ 通过（21.53s，0 错误） |
| `cd D:\paike\backend; mvn -Dtest=!SchedulerBackendApplicationTests test` | 151 tests 通过 | 报告 6/2 复测通过；本次未单独执行 |
| `cd D:\paike\backend; mvn -Dtest=V5ClassroomBatchLoadingTest test` | 2 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=ScheduleConflictServiceTest clean test` | 4 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=UnscheduledTaskServiceTest clean test` | 3 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=UnscheduledTaskServiceTest,V4ScheduleRiskServiceTest test` | 4 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=DatabaseSchemaScriptTest test` | 10 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=V5ConsistencyCheckServiceTest test` | 3 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=SchedulePlanServiceTest test` | 4 tests 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=M15TimetableControllerExportComplexityInvestigationTest test` | 1 test 通过 | 同上 |
| `cd D:\paike\backend; mvn -Dtest=M13ControllerMapperInjectionInvestigationTest test` | 1 test 通过 | 同上 |

**说明**：本次仅复测前端构建（`npm run build`），后端单测全部沿用报告 §6 的执行结论；新增/修改的 InvestigationTest 已在静态代码层逐项核对（test 类存在、断言逻辑与 fix commit 描述一致）。

---

## 6. 已知遗留

报告中标记为"部分成立/暂不修复"的项（M-14/M-16/M-27/M-31/M-32/M-52 等）均配 `*InvestigationTest` 锁定现状，未纳入"已修复"集合，不在本清单范围。

报告中标记为"不成立/无法验证"的项（C-18/C-20/M-12/M-26/M-38/M-49/M-50/M-51/M-53 等）原报告已说明原由，不作为修复缺陷管理。

---

## 7. 归档建议

1. 本文件与 `20260527bug验证报告.md` 同目录归档，作为基线；
2. 后续若发现"已修复"项的回归，先看对应 `*Test` 类是否覆盖，未覆盖则补单测再修代码；
3. "部分成立/暂不修复"项如有真实复现，按 `Bug ID` 单独建 issue，不与基线混淆。
