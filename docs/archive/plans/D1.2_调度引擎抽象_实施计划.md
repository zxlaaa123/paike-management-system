# D1.2 调度引擎抽象 — 实施计划书 v2

> 分支：`fix/audit-2026-05`
> 状态：计划中（未动代码）
> 日期：2026-05-23
> 说明：v1 (Sonnet 4.6 起草) 在"算法可单测""可插拔算法"等收益上夸大，主循环重构那段用 `// ...` 含混过去。本版基于完整阅读两个 service 全文重写。

---

## 0. TL;DR

**D1.2 的原始诉求**：让 `AutoScheduleService` 有接口边界、可单测、可替换算法。

**核实后的结论**：
- **可替换算法**是 YAGNI。当前只有一个贪心算法，V3 的 4 个 strategyType 只是同一算法的不同权重，没有第二个引擎。强抽 `ISchedulingEngine` 接口会留下死代码。
- **算法可单测**只能做到部分：纯计算的排序/匹配可以纯单测；但核心循环里 `checkTeacherDailyLimit` 直接打 DB，没法"纯函数"化。
- **真正能做且值得做的事**：①消除两 service 间约 130 行重复 ②把 `run()` 的 230 行单方法拆成 4-5 个 60 行以内的小方法 ③组件级测试用 `@MybatisPlusTest`。

**建议方案**：4 个 commit，约 12-15 小时。**不做**接口抽象，**不做**两引擎合并。

---

## 1. 现状（基于代码核实）

### 1.1 文件规模

| 文件 | 行数 | 主方法 | 依赖数 |
|---|---|---|---|
| `service/AutoScheduleService.java` | 481 | `run()` 占 235 行（L52-286）| 13 |
| `service/V3ScheduleGenerateService.java` | 700+ | `generate()` 占 ~100 行 + `generatePlanItems()` ~80 行 + `findBestCandidate()` ~60 行 | 13 |

精确的 13 个依赖（v1 的"11 个"是错的）：

**AutoScheduleService.java:37-49**
```
AutoScheduleBatchService, UnscheduledTaskService, ScheduleConflictService,
ScheduleRuleService, ScheduleMapper, TeachingTaskMapper, TimeSlotMapper,
ClassroomMapper, TeacherUnavailableTimeMapper, CourseMapper, ClassInfoMapper,
SemesterService, ScheduleLockGuardService
```

### 1.2 `AutoScheduleService.run()` 的 11 步骤（参考代码注释）

| # | 行号 | 职责 | 涉及组件 |
|---|---|---|---|
| 1 | L54-73 | 清旧排课（按需） | scheduleMapper, lockGuardService, unscheduledTaskService |
| 2 | L75-89 | 读取目标教学任务 | teachingTaskMapper |
| 3 | L92 | 创建批次 | batchService |
| 4 | L94-99 | 读取规则配置 | ruleService（5 个 key）|
| 5 | L101-108 | 读取时间段并排序 | timeSlotMapper |
| 6 | L110-114 | 读取可用教室 | classroomMapper |
| 7 | L116-123 | 读取教师禁排 | unavailableTimeMapper |
| 8 | L125-132 | 读取 course/class 字典 | courseMapper, classInfoMapper |
| 9 | L135 | 任务排序（难排优先） | 内部纯计算 |
| **10** | **L137-257** | **主循环（核心算法）** | **conflictService, scheduleMapper, unscheduledTaskService** |
| 11 | L259-285 | 更新批次 + 构建返回 | batchService |

步骤 4-8 = 数据加载（约 40 行）。步骤 9 = 排序工具（约 30 行）。步骤 10 = 真正的算法（120 行）。其余是编排和清扫。

### 1.3 两 service 的重复代码（实测）

| 方法 / 段落 | AutoScheduleService | V3ScheduleGenerateService | 是否一致 |
|---|---|---|---|
| `isRoomTypeMatched()` | L351-356 | L577-581 | **完全一致** |
| `getCourseType()` | L448-451 | L583-586 | 一致（V3 多 null guard）|
| `getClassStudentCount()` | L453-456 | L588-591 | 一致（V3 多 null guard）|
| `sortTasks()` | L294-326 | L550-575 | 一致（V3 用 .toList()）|
| `sortTimeSlots()` | L332-346 | L222-245 | 部分一致（V3 多 null 容错）|
| 加载教室 SQL | L111-114 | L254-258 | 完全一致 |
| 加载禁排 SQL | L117-120 | L261-265 | 完全一致 |
| `toUnavailableKeySet` 逻辑 | L121-123（内联）| L268-272 | 完全一致 |
| `toUnavailableCount` 逻辑 | L300-301（内联）| L274-277 | 完全一致 |

合计可去重 **~130 行**。

### 1.4 两 service 的本质差异（不可合并）

| | AutoScheduleService | V3ScheduleGenerateService |
|---|---|---|
| 落库目标 | `schedule` 表（live） | `schedule_plan_item` 表（draft） |
| 落库时机 | **循环中逐条 insert**（L229 `saveSchedule`）| **循环结束后批量 insert**（L117-119）|
| 冲突检测 | **打 DB**：`conflictService.checkConflict()` + `scheduleMapper.selectCount` | **内存 list**：`hasConflict(generatedItems)` + `stream().count()` |
| 候选策略 | first-fit（找到一个就用）| score-based（评估所有可行候选取最高分）|
| 跨批次冲突 | **会**：DB 查询会读到历史排课 | **不会**：每个 plan 自成草稿 |
| 用途 | 学生看到的实时课表 | 备选方案，需另外 apply 才生效 |

这些差异**不是实现选择，是业务模型差异**。强行合并会改变语义。

### 1.5 v1 计划的具体错误

为避免再次踩坑，列出 v1 的事实性问题：

1. v1 称"AutoScheduleService 11 个依赖" → 实际 13 个
2. v1 称"重构后 6 个依赖" → 自己列了 8 个
3. v1 目标里写"算法循环独立为 GreedySchedulingEngine"，但 Step 4 包结构和 Step 5 代码里**都没有 GreedySchedulingEngine**
4. v1 Step 5 把主循环（最难的部分，120 行）用 `// ... 原有循环` 一句带过
5. v1 称"重构后规则工具类可以无 Spring 纯单测" → 这些工具类本来就是纯静态逻辑，inline 在两个 service 里也能拿出来单测，重构无关
6. v1 12-14h 估算未含回归测试

---

## 2. D1.2 原始诉求的现实可行性

| 诉求 | 现实评估 | 本计划响应 |
|---|---|---|
| "interface boundaries" | **不必加接口**：当前只有一个实现，加 `IAutoScheduleService` 是噪音。需要 mock 时 Mockito-inline 直接 mock 类即可。Spring 风格不要 `I*` 前缀。 | 不做 |
| "hard to unit-test" | **部分可改善**：纯工具类可纯单测；主循环因依赖 `scheduleMapper.selectCount`（read-own-writes 语义）和 `conflictService` 没法离开 Spring + DB。 | 做：抽工具类、抽数据加载，拆主循环成可读小方法 |
| "swap algorithms" | **YAGNI**：没有第二个算法。V3 的 4 个 strategyType 是同一算法的权重变体。`ISchedulingEngine` 抽象等于发明需求。 | 不做 |

**底线**：D1.2 的实际可交付物是"代码去重 + 主方法瘦身 + 测试改进"，**不是**"算法可插拔架构"。把目标说清楚比把口号喊响更重要。

---

## 3. 推荐方案：4 个 commit

```
Commit 1  feat(d1.2): 提取 SchedulingSupport 工具类（纯计算去重）
Commit 2  refactor(d1.2): 提取 SchedulingReferenceLoader（数据加载去重）
Commit 3  refactor(d1.2): AutoScheduleService.run() 拆分为编排 + 4 个子方法
Commit 4  refactor(d1.2): V3ScheduleGenerateService 应用同样手术（可选，独立 PR）
```

每个 commit **独立可回滚**、独立通过编译和现有冒烟测试。

---

## 4. Commit 1：提取 SchedulingSupport

### 4.1 新增文件

```
backend/src/main/java/com/paike/scheduler/service/scheduling/
└── SchedulingSupport.java
```

> 包名用 `scheduling` 而非 `engine` —— "engine" 暗示有可插拔接口，与本计划意图不符。

### 4.2 完整代码

```java
package com.paike.scheduler.service.scheduling;

import com.paike.scheduler.common.enums.CourseType;
import com.paike.scheduler.common.enums.RoomType;
import com.paike.scheduler.entity.*;

import java.util.*;
import java.util.stream.Collectors;

/**
 * 排课相关的纯计算工具。所有方法无副作用、不依赖 Spring，便于直接单测。
 *
 * 不要往这个类里塞需要 DB / 上下文的方法 —— 那种属于 service。
 */
public final class SchedulingSupport {

    private SchedulingSupport() {}

    /** 实验课/机房课需要对应专用房型；其他课型不限。 */
    public static boolean isRoomTypeMatched(String courseType, String roomType) {
        if (CourseType.EXPERIMENT.getCode().equals(courseType)) {
            return RoomType.LAB.getCode().equals(roomType);
        }
        if (CourseType.COMPUTER.getCode().equals(courseType)) {
            return RoomType.COMPUTER.getCode().equals(roomType);
        }
        return true;
    }

    public static String getCourseType(Long courseId, Map<Long, Course> courseMap) {
        Course course = courseMap.get(courseId);
        return course != null ? course.getCourseType() : CourseType.NORMAL.getCode();
    }

    public static int getClassStudentCount(Long classId, Map<Long, ClassInfo> classMap) {
        ClassInfo info = classMap.get(classId);
        if (info == null) return 0;
        return info.getStudentCount() != null ? info.getStudentCount() : 0;
    }

    /**
     * 难排任务优先：实验/机房 → 人数多 → 周课时多 → 教师禁排多。
     * 与原 AutoScheduleService.sortTasks 行为完全一致（含 V3 的 null 容错）。
     */
    public static List<TeachingTask> sortTasks(
            List<TeachingTask> tasks,
            Map<Long, Long> unavailableCountByTeacher,
            Map<Long, Course> courseMap,
            Map<Long, ClassInfo> classMap) {

        return tasks.stream().sorted((a, b) -> {
            int prA = isSpecialType(getCourseType(a.getCourseId(), courseMap)) ? 0 : 1;
            int prB = isSpecialType(getCourseType(b.getCourseId(), courseMap)) ? 0 : 1;
            if (prA != prB) return prA - prB;

            int cntA = getClassStudentCount(a.getClassId(), classMap);
            int cntB = getClassStudentCount(b.getClassId(), classMap);
            if (cntB != cntA) return cntB - cntA;

            int hA = a.getWeeklyHours() == null ? 0 : a.getWeeklyHours();
            int hB = b.getWeeklyHours() == null ? 0 : b.getWeeklyHours();
            if (hB != hA) return hB - hA;

            long uA = unavailableCountByTeacher.getOrDefault(a.getTeacherId(), 0L);
            long uB = unavailableCountByTeacher.getOrDefault(b.getTeacherId(), 0L);
            return Long.compare(uB, uA);
        }).collect(Collectors.toList());
    }

    /**
     * 偏好排序（不是硬限制）：可选优先上午、可选避开周五下午，最终按 sortOrder。
     * 容错：null 字段按最大值排到末尾。
     */
    public static List<TimeSlot> sortTimeSlots(
            List<TimeSlot> slots, boolean prioritizeMorning, boolean avoidFridayAfternoon) {

        return slots.stream().sorted((a, b) -> {
            int aPeriod = nullToMax(a.getPeriodNo());
            int bPeriod = nullToMax(b.getPeriodNo());
            int aDay = nullToMax(a.getDayOfWeek());
            int bDay = nullToMax(b.getDayOfWeek());

            if (prioritizeMorning) {
                boolean aM = aPeriod <= 2, bM = bPeriod <= 2;
                if (aM != bM) return aM ? -1 : 1;
            }
            if (avoidFridayAfternoon) {
                boolean aFP = aDay == 5 && aPeriod >= 3;
                boolean bFP = bDay == 5 && bPeriod >= 3;
                if (aFP != bFP) return aFP ? 1 : -1;
            }
            return Integer.compare(nullToMax(a.getSortOrder()), nullToMax(b.getSortOrder()));
        }).collect(Collectors.toList());
    }

    /** 教师禁排时间转为 "teacherId_slotId" 快查集合。 */
    public static Set<String> toUnavailableKeySet(List<TeacherUnavailableTime> list) {
        return list.stream()
                .map(ut -> ut.getTeacherId() + "_" + ut.getTimeSlotId())
                .collect(Collectors.toSet());
    }

    /** 每位教师的禁排次数（排序权重用）。 */
    public static Map<Long, Long> toUnavailableCountByTeacher(List<TeacherUnavailableTime> list) {
        return list.stream()
                .collect(Collectors.groupingBy(
                        TeacherUnavailableTime::getTeacherId, Collectors.counting()));
    }

    /** 时间段按 dayOfWeek 分组成 id 列表，用于每日上限检查。 */
    public static Map<Integer, List<Long>> slotIdsByDay(List<TimeSlot> slots) {
        return slots.stream()
                .filter(s -> s.getDayOfWeek() != null)
                .collect(Collectors.groupingBy(
                        TimeSlot::getDayOfWeek,
                        Collectors.mapping(TimeSlot::getId, Collectors.toList())));
    }

    private static boolean isSpecialType(String type) {
        return CourseType.EXPERIMENT.getCode().equals(type)
                || CourseType.COMPUTER.getCode().equals(type);
    }

    private static int nullToMax(Integer v) {
        return v == null ? Integer.MAX_VALUE : v;
    }
}
```

### 4.3 改造点

**`AutoScheduleService.java`**：
- 删除 L294-326 `sortTasks`、L332-346 `sortTimeSlots`、L351-356 `isRoomTypeMatched`、L448-451 `getCourseType`、L453-456 `getClassStudentCount`（共 ~80 行）
- L135 调用：`sortTasks(...)` → `SchedulingSupport.sortTasks(targetTasks, SchedulingSupport.toUnavailableCountByTeacher(unavailableTimes), courseMap, classMap)`
- L105 调用：`sortTimeSlots(...)` → `SchedulingSupport.sortTimeSlots(timeSlots, prioritizeMorning, avoidFridayAfternoon)`
- L106-108 改用 `SchedulingSupport.slotIdsByDay(timeSlots)`
- L121-123 改用 `SchedulingSupport.toUnavailableKeySet(unavailableTimes)`
- 内部对 `isRoomTypeMatched` / `getCourseType` / `getClassStudentCount` 的调用全部加 `SchedulingSupport.` 前缀

**`V3ScheduleGenerateService.java`**：同样手术（V3 内部那 11 个数据加载 private 方法保留到 Commit 2 再动）。

### 4.4 新增测试

```
backend/src/test/java/com/paike/scheduler/service/scheduling/
└── SchedulingSupportTest.java
```

无 Spring 纯单测：

```java
class SchedulingSupportTest {

    @Test void isRoomTypeMatched_experimentRequiresLab() {
        assertTrue(SchedulingSupport.isRoomTypeMatched("EXPERIMENT", "LAB"));
        assertFalse(SchedulingSupport.isRoomTypeMatched("EXPERIMENT", "COMPUTER"));
        assertFalse(SchedulingSupport.isRoomTypeMatched("EXPERIMENT", "NORMAL"));
    }

    @Test void isRoomTypeMatched_normalCourseAllowsAnyRoom() {
        assertTrue(SchedulingSupport.isRoomTypeMatched("NORMAL", "NORMAL"));
        assertTrue(SchedulingSupport.isRoomTypeMatched("NORMAL", "LAB"));
    }

    @Test void sortTasks_experimentBeforeNormalEvenIfFewerHours() {
        // 实验课周课时 2，普通课周课时 6 → 实验仍排前
        // ...
    }

    @Test void sortTasks_byStudentCountWhenSameType() { ... }

    @Test void sortTimeSlots_morningFirstWhenEnabled() { ... }

    @Test void sortTimeSlots_fridayAfternoonLastWhenAvoided() { ... }

    @Test void slotIdsByDay_skipsNullDayOfWeek() { ... }
}
```

### 4.5 工时

约 **2 小时**，含编译验证 + 8 个单测。

### 4.6 风险

- `sortTimeSlots` 原版（AutoScheduleService.L344）`a.getSortOrder() - b.getSortOrder()` 没有 null 容错；新版加了。如果数据库里真有 null，旧版会 NPE，新版会把它排到末尾。**这是行为变更，但属于修 bug**。需要在 commit message 里说明。
- 其他方法行为完全等价。

---

## 5. Commit 2：提取 SchedulingReferenceLoader

### 5.1 新增文件

```
service/scheduling/
├── SchedulingReferenceData.java     # record，所有预加载数据
└── SchedulingReferenceLoader.java   # @Component，负责加载
```

### 5.2 SchedulingReferenceData

```java
package com.paike.scheduler.service.scheduling;

import com.paike.scheduler.entity.*;
import java.util.*;

/**
 * 一次排课所需的全部"读"型数据。所有字段在构造后不可变，循环中只读。
 *
 * 注意：不包含规则配置（max daily slots 等）—— 那些放在 RuleConfig 里。
 *      不包含权重 map（仅 V3 用）—— 走单独的 weightMap 字段，AutoSchedule 传 Map.of()。
 */
public record SchedulingReferenceData(
        List<TimeSlot> sortedTimeSlots,
        Map<Integer, List<Long>> slotIdsByDay,
        List<Classroom> classrooms,
        Set<String> unavailableKeySet,
        Map<Long, Long> unavailableCountByTeacher,
        Map<Long, Course> courseMap,
        Map<Long, ClassInfo> classMap,
        Map<String, java.math.BigDecimal> weightMap  // 仅 V3 用，Auto 传 Map.of()
) {}
```

### 5.3 SchedulingReferenceLoader

```java
package com.paike.scheduler.service.scheduling;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.paike.scheduler.entity.*;
import com.paike.scheduler.mapper.*;
import com.paike.scheduler.service.ScheduleRuleService;
import com.paike.scheduler.service.ScheduleRuleWeightService;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;

import java.math.BigDecimal;
import java.util.*;
import java.util.stream.Collectors;

@Component
@RequiredArgsConstructor
public class SchedulingReferenceLoader {

    private final TimeSlotMapper timeSlotMapper;
    private final ClassroomMapper classroomMapper;
    private final TeacherUnavailableTimeMapper unavailableTimeMapper;
    private final CourseMapper courseMapper;
    private final ClassInfoMapper classInfoMapper;
    private final ScheduleRuleService ruleService;
    private final ScheduleRuleWeightService ruleWeightService;

    /** AutoScheduleService 用：不需要权重。 */
    public SchedulingReferenceData loadForAutoSchedule() {
        return loadCommon(Map.of());
    }

    /** V3 用：需要按学期 + 策略加载权重。 */
    public SchedulingReferenceData loadForV3Generate(Long semesterId, String strategyType) {
        return loadCommon(loadWeights(semesterId, strategyType));
    }

    private SchedulingReferenceData loadCommon(Map<String, BigDecimal> weightMap) {
        boolean prioritizeMorning    = ruleService.getBoolValue("PRIORITIZE_MORNING");
        boolean avoidFridayAfternoon = ruleService.getBoolValue("AVOID_FRIDAY_AFTERNOON");

        List<TimeSlot> rawSlots = timeSlotMapper.selectList(
                new LambdaQueryWrapper<TimeSlot>().orderByAsc(TimeSlot::getSortOrder));
        List<TimeSlot> sorted = SchedulingSupport.sortTimeSlots(rawSlots, prioritizeMorning, avoidFridayAfternoon);

        List<Classroom> classrooms = classroomMapper.selectList(
                new LambdaQueryWrapper<Classroom>()
                        .eq(Classroom::getStatus, 1)
                        .eq(Classroom::getDeleted, 0));

        List<TeacherUnavailableTime> unavailables = unavailableTimeMapper.selectList(
                new LambdaQueryWrapper<TeacherUnavailableTime>()
                        .eq(TeacherUnavailableTime::getStatus, 1)
                        .eq(TeacherUnavailableTime::getDeleted, 0));

        Map<Long, Course> courseMap = courseMapper.selectList(
                new LambdaQueryWrapper<Course>().eq(Course::getDeleted, 0))
                .stream()
                .collect(Collectors.toMap(Course::getId, c -> c, (a, b) -> a));

        Map<Long, ClassInfo> classMap = classInfoMapper.selectList(
                new LambdaQueryWrapper<ClassInfo>().eq(ClassInfo::getDeleted, 0))
                .stream()
                .collect(Collectors.toMap(ClassInfo::getId, c -> c, (a, b) -> a));

        return new SchedulingReferenceData(
                sorted,
                SchedulingSupport.slotIdsByDay(sorted),
                classrooms,
                SchedulingSupport.toUnavailableKeySet(unavailables),
                SchedulingSupport.toUnavailableCountByTeacher(unavailables),
                courseMap, classMap,
                weightMap
        );
    }

    private Map<String, BigDecimal> loadWeights(Long semesterId, String strategyType) {
        List<ScheduleRuleWeight> rules = ruleWeightService.list(semesterId, strategyType, null);
        if (rules.isEmpty()) {
            ruleWeightService.initDefaultRules(semesterId, strategyType);
            rules = ruleWeightService.list(semesterId, strategyType, null);
        }
        return rules.stream()
                .filter(r -> r.getEnabled() != null && r.getEnabled() == 1)
                .collect(Collectors.toMap(
                        ScheduleRuleWeight::getRuleCode,
                        r -> r.getWeight() != null ? r.getWeight() : BigDecimal.ZERO,
                        (a, b) -> a));
    }
}
```

### 5.4 改造点

**`AutoScheduleService.java`**：
- 删除注入：`timeSlotMapper`, `classroomMapper`, `unavailableTimeMapper`, `courseMapper`, `classInfoMapper`（5 个）
- 新增注入：`SchedulingReferenceLoader referenceLoader`（1 个）
- L94-132 整段 40 行替换为：
  ```java
  SchedulingReferenceData refData = referenceLoader.loadForAutoSchedule();
  RuleConfig rules = loadRuleConfig();  // 见下
  ```
- 新增 `loadRuleConfig()` 私有方法（10 行）：
  ```java
  private RuleConfig loadRuleConfig() {
      return new RuleConfig(
              ruleService.getIntValue("TEACHER_MAX_DAILY_SLOTS"),
              ruleService.getIntValue("CLASS_MAX_DAILY_SLOTS"),
              ruleService.getBoolValue("ALLOW_SAME_COURSE_SAME_DAY")
      );
  }
  ```
- 新增小 record：
  ```java
  // 同包内 / 或挪到 scheduling 包
  public record RuleConfig(
          int teacherMaxDailySlots,
          int classMaxDailySlots,
          boolean allowSameCourseSameDay
  ) {}
  ```

依赖数：13 → 9。

**`V3ScheduleGenerateService.java`**：
- 删除：`timeSlotMapper`, `classroomMapper`, `unavailableTimeMapper`, `courseMapper`, `classInfoMapper`, `ruleWeightService`（6 个）
- 删除 11 个 private 加载方法：`loadSortedTimeSlots`, `safeSortOrder`, `loadAvailableClassrooms`, `loadUnavailableTimes`, `toUnavailableKeySet`, `toUnavailableCount`, `loadStrategyWeights`
- 删除内部 `GenerationContext` record
- 全部改用 `referenceLoader.loadForV3Generate(semesterId, strategyType)` 返回的 `SchedulingReferenceData`
- 依赖数：13 → 7

### 5.5 注意：V3 的 courseMap/classMap 加载方式有差异

V3 当前用 `selectBatchIds(courseIds)`（按需），而新 Loader 用 `selectList(deleted=0)`（全量）。差异：
- V3 当前：只查任务用到的 course/class
- 新版：查所有未删除的 course/class

**对内存影响**：通常 course/class 总数有限（百级别），全量加载可接受。
**对正确性影响**：无 —— 用 map.get(id) 取值，多查的不会被用到。

如果项目实际数据量大（万级 class），需要 Loader 改成接受 task 列表作参数：

```java
public SchedulingReferenceData loadForV3Generate(Long semesterId, String strategyType, List<TeachingTask> tasks) {
    Set<Long> neededCourseIds = tasks.stream().map(TeachingTask::getCourseId).filter(Objects::nonNull).collect(Collectors.toSet());
    Set<Long> neededClassIds  = tasks.stream().map(TeachingTask::getClassId).filter(Objects::nonNull).collect(Collectors.toSet());
    // ...只查 needed
}
```

**建议**：先按全量加载实现（简单），后续如果出现性能问题再优化。**这点必须在 commit message 里说明。**

### 5.6 工时

约 **3 小时**，含编译 + 现有冒烟测试通过。

### 5.7 风险

- `@MybatisPlusTest` 写一个 Loader 集成测试验证加载结果非空（1h）
- 数据加载顺序变了（先 timeSlots 再 sort），需确认 `sortTimeSlots` 在新 Loader 内调用次序与原 service 一致 —— 已确认一致

---

## 6. Commit 3：AutoScheduleService.run() 拆分

这是真正的难点。v1 用 `// ...` 蒙混过去的就是这里。**本计划给出完整 before/after**。

### 6.1 拆分目标

| 方法 | 行数 | 职责 |
|---|---|---|
| `run()` | 235 → **~50** | 顶层编排 |
| `clearSchedulesIfNeeded()` | new ~30 | 步骤 1 |
| `loadTargetTasks()` | new ~15 | 步骤 2 |
| `arrangeAllTasks()` | new ~30 | 步骤 9-10 外层循环 |
| `arrangeOneTask()` | new ~55 | 单任务编排（含预过滤）|
| `tryFindFirstFitAssignment()` | new ~50 | 内部 for-slot/for-room 二层循环 |
| `finalizeBatch()` | new ~25 | 步骤 11 |

合计 ~255 行（比原 235 多 20 行是 record 定义 + 方法签名）。

### 6.2 新增小 record（放在 service/scheduling 包）

```java
public record TaskArrangeOutcome(
        int requiredSlots,
        int alreadyScheduled,
        int generatedThisRun,
        FailReason lastFail
) {
    public boolean fullyArranged() { return generatedThisRun >= (requiredSlots - alreadyScheduled); }
    public int remainingAfterAttempt() {
        return Math.max(0, requiredSlots - alreadyScheduled - generatedThisRun);
    }

    public static TaskArrangeOutcome preFlightFailed(
            int required, int already, String reasonType, String reasonMsg) {
        return new TaskArrangeOutcome(required, already, 0, new FailReason(reasonType, reasonMsg));
    }
    public static TaskArrangeOutcome ofProgress(
            int required, int already, int generated, FailReason lastFail) {
        return new TaskArrangeOutcome(required, already, generated, lastFail);
    }
}

public record FailReason(String code, String message) {
    public static FailReason unknown() { return new FailReason("UNKNOWN", ""); }
}

public record AssignmentAttempt(TimeSlot slot, Classroom room, FailReason lastFail) {
    public boolean placed() { return slot != null && room != null; }
    public static AssignmentAttempt placed(TimeSlot s, Classroom r) {
        return new AssignmentAttempt(s, r, FailReason.unknown());
    }
    public static AssignmentAttempt notPlaced(FailReason fail) {
        return new AssignmentAttempt(null, null, fail);
    }
}

public record ArrangeStats(int generatedCount, int successTaskCount, int failedTaskCount) {}
```

### 6.3 重构后的 run()

```java
@Transactional(rollbackFor = Exception.class)
public AutoScheduleResult run(AutoScheduleRequest request) {
    Long semesterId = resolveSemesterId(request);
    clearSchedulesIfNeeded(request, semesterId);

    List<TeachingTask> targetTasks = loadTargetTasks(semesterId, request.getTaskIds());
    AutoScheduleBatch batch = batchService.createBatch(
            targetTasks.size(), request.isClearOldAutoSchedule());

    SchedulingReferenceData refData = referenceLoader.loadForAutoSchedule();
    RuleConfig rules = loadRuleConfig();

    List<TeachingTask> sortedTasks = SchedulingSupport.sortTasks(
            targetTasks,
            refData.unavailableCountByTeacher(),
            refData.courseMap(),
            refData.classMap());

    ArrangeStats stats = arrangeAllTasks(sortedTasks, batch, semesterId, refData, rules);

    return finalizeBatch(batch, targetTasks.size(), stats);
}
```

### 6.4 拆出的 7 个方法（完整代码）

```java
private void clearSchedulesIfNeeded(AutoScheduleRequest request, Long semesterId) {
    if (request.isClearAllSchedule()) {
        LambdaQueryWrapper<Schedule> wrapper = new LambdaQueryWrapper<Schedule>()
                .eq(Schedule::getDeleted, 0)
                .eq(Schedule::getSemesterId, semesterId);
        ensureSchedulesUnlocked(wrapper);
        scheduleMapper.delete(wrapper);
        unscheduledTaskService.clearBySemester(semesterId);
    } else if (request.isClearOldAutoSchedule()) {
        LambdaQueryWrapper<Schedule> wrapper = new LambdaQueryWrapper<Schedule>()
                .eq(Schedule::getSourceType, ScheduleSourceType.AUTO.getCode())
                .eq(Schedule::getDeleted, 0)
                .eq(Schedule::getSemesterId, semesterId);
        ensureSchedulesUnlocked(wrapper);
        scheduleMapper.delete(wrapper);
        unscheduledTaskService.clearBySemester(semesterId);
    }
}

private List<TeachingTask> loadTargetTasks(Long semesterId, List<Long> requestedTaskIds) {
    List<TeachingTask> allTasks = teachingTaskMapper.selectList(
            new LambdaQueryWrapper<TeachingTask>()
                    .eq(TeachingTask::getDeleted, 0)
                    .eq(TeachingTask::getStatus, 1)
                    .eq(TeachingTask::getSemesterId, semesterId));
    if (requestedTaskIds == null || requestedTaskIds.isEmpty()) {
        return allTasks;
    }
    return allTasks.stream()
            .filter(t -> requestedTaskIds.contains(t.getId()))
            .collect(Collectors.toList());
}

private RuleConfig loadRuleConfig() {
    return new RuleConfig(
            ruleService.getIntValue("TEACHER_MAX_DAILY_SLOTS"),
            ruleService.getIntValue("CLASS_MAX_DAILY_SLOTS"),
            ruleService.getBoolValue("ALLOW_SAME_COURSE_SAME_DAY")
    );
}

private ArrangeStats arrangeAllTasks(
        List<TeachingTask> sortedTasks,
        AutoScheduleBatch batch,
        Long semesterId,
        SchedulingReferenceData refData,
        RuleConfig rules) {

    int generatedCount = 0;
    int successCount = 0;
    int failedCount = 0;

    for (TeachingTask task : sortedTasks) {
        TaskArrangeOutcome outcome = arrangeOneTask(task, batch, semesterId, refData, rules);
        generatedCount += outcome.generatedThisRun();

        if (outcome.fullyArranged()) {
            successCount++;
        } else {
            failedCount++;
            int totalScheduled = outcome.alreadyScheduled() + outcome.generatedThisRun();
            unscheduledTaskService.addUnscheduledTask(
                    batch.getId(), semesterId, task.getId(),
                    outcome.requiredSlots(), totalScheduled, outcome.remainingAfterAttempt(),
                    outcome.lastFail().code(), outcome.lastFail().message());
        }
    }
    return new ArrangeStats(generatedCount, successCount, failedCount);
}

/**
 * 单任务编排：
 *  1) 算需要排几节（requiredSlots - 已排）
 *  2) 校验 course/class 存在
 *  3) 预过滤候选教室（容量 + 房型）
 *  4) 循环尝试每节，遇第一次找不到 fit 就退出（保持原 break 语义）
 */
private TaskArrangeOutcome arrangeOneTask(
        TeachingTask task,
        AutoScheduleBatch batch,
        Long semesterId,
        SchedulingReferenceData refData,
        RuleConfig rules) {

    int requiredSlots = task.getWeeklyHours() == null
            ? 0 : (int) Math.ceil(task.getWeeklyHours() / 2.0);
    int alreadyScheduled = countScheduledSlots(task.getId());
    int remainingSlots = requiredSlots - alreadyScheduled;

    if (remainingSlots <= 0) {
        return TaskArrangeOutcome.ofProgress(requiredSlots, alreadyScheduled, 0, FailReason.unknown());
    }

    Course course = refData.courseMap().get(task.getCourseId());
    if (course == null) {
        return TaskArrangeOutcome.preFlightFailed(requiredSlots, alreadyScheduled,
                "COURSE_NOT_FOUND", "关联课程不存在或已删除");
    }
    ClassInfo classInfo = refData.classMap().get(task.getClassId());
    if (classInfo == null) {
        return TaskArrangeOutcome.preFlightFailed(requiredSlots, alreadyScheduled,
                "CLASS_NOT_FOUND", "关联班级不存在或已删除");
    }

    int studentCount = classInfo.getStudentCount() == null ? 0 : classInfo.getStudentCount();
    List<Classroom> matchedRooms = refData.classrooms().stream()
            .filter(r -> r.getCapacity() != null && r.getCapacity() >= studentCount)
            .filter(r -> SchedulingSupport.isRoomTypeMatched(course.getCourseType(), r.getRoomType()))
            .sorted(Comparator.comparingInt(Classroom::getCapacity))
            .collect(Collectors.toList());
    if (matchedRooms.isEmpty()) {
        return TaskArrangeOutcome.preFlightFailed(requiredSlots, alreadyScheduled,
                "NO_MATCHED_CLASSROOM", "没有符合课程类型和容量要求的教室");
    }

    int generated = 0;
    FailReason lastFail = FailReason.unknown();
    Set<Integer> usedDays = new HashSet<>();

    for (int i = 0; i < remainingSlots; i++) {
        AssignmentAttempt attempt = tryFindFirstFitAssignment(
                task, matchedRooms, usedDays, semesterId, refData, rules);

        if (!attempt.placed()) {
            lastFail = attempt.lastFail();
            break;  // 与原行为一致：单节失败就停止后续节尝试
        }

        saveSchedule(task, attempt.slot(), attempt.room(), batch.getId());
        generated++;
        usedDays.add(attempt.slot().getDayOfWeek());
    }

    return TaskArrangeOutcome.ofProgress(requiredSlots, alreadyScheduled, generated, lastFail);
}

/**
 * for-slot → for-room 二层循环，找第一个无冲突的 (slot, room) 组合。
 * 任何一次失败都用最近一次的 FailReason 兜底返回。
 *
 * 注意：本方法仍然调用了 conflictService.checkConflict + scheduleMapper（间接，via checkXxxDailyLimit）。
 *      它不是纯函数，无法脱离 Spring 单测。但它的输入边界足够清晰，集成测试时可独立测试。
 */
private AssignmentAttempt tryFindFirstFitAssignment(
        TeachingTask task,
        List<Classroom> matchedRooms,
        Set<Integer> usedDays,
        Long semesterId,
        SchedulingReferenceData refData,
        RuleConfig rules) {

    FailReason lastFail = FailReason.unknown();

    for (TimeSlot slot : refData.sortedTimeSlots()) {
        if (refData.unavailableKeySet().contains(task.getTeacherId() + "_" + slot.getId())) {
            lastFail = new FailReason("TEACHER_UNAVAILABLE", "教师禁排时间限制");
            continue;
        }
        if (!checkTeacherDailyLimit(task.getTeacherId(), slot.getDayOfWeek(),
                rules.teacherMaxDailySlots(), semesterId, refData.slotIdsByDay())) {
            lastFail = new FailReason("TEACHER_DAILY_LIMIT",
                    "教师每天最多" + rules.teacherMaxDailySlots() + "个大节");
            continue;
        }
        if (!checkClassDailyLimit(task.getClassId(), slot.getDayOfWeek(),
                rules.classMaxDailySlots(), semesterId, refData.slotIdsByDay())) {
            lastFail = new FailReason("CLASS_DAILY_LIMIT",
                    "班级每天最多" + rules.classMaxDailySlots() + "个大节");
            continue;
        }
        if (!rules.allowSameCourseSameDay() && usedDays.contains(slot.getDayOfWeek())
                && hasSameCourseSameDay(task.getClassId(), task.getCourseId(),
                        slot.getDayOfWeek(), semesterId, refData.slotIdsByDay())) {
            lastFail = new FailReason("SAME_COURSE_SAME_DAY", "同一课程同一天不允许重复");
            continue;
        }

        for (Classroom room : matchedRooms) {
            String conflict = conflictService.checkConflict(task.getId(), slot.getId(), room.getId(), null);
            if (conflict == null) {
                return AssignmentAttempt.placed(slot, room);
            }
            lastFail = new FailReason(
                    categorizeReason(conflict),
                    ScheduleConflictService.stripReasonTag(conflict).replace("排课失败:", ""));
        }
    }
    return AssignmentAttempt.notPlaced(lastFail);
}

private AutoScheduleResult finalizeBatch(AutoScheduleBatch batch, int totalTaskCount, ArrangeStats stats) {
    String status;
    String message;
    if (stats.failedTaskCount() == 0) {
        status = "SUCCESS";
        message = "自动排课完成，全部任务已安排";
    } else if (stats.successTaskCount() > 0) {
        status = "PARTIAL";
        message = "自动排课完成，部分任务未排满";
    } else {
        status = "FAILED";
        message = "自动排课完成，所有任务均未安排";
    }
    batchService.updateBatchResult(
            batch.getId(), stats.successTaskCount(), stats.failedTaskCount(),
            stats.generatedCount(), status, message);

    AutoScheduleResult result = new AutoScheduleResult();
    result.setBatchId(batch.getId());
    result.setBatchNo(batch.getBatchNo());
    result.setTotalTaskCount(totalTaskCount);
    result.setSuccessTaskCount(stats.successTaskCount());
    result.setFailedTaskCount(stats.failedTaskCount());
    result.setGeneratedScheduleCount(stats.generatedCount());
    result.setStatus(status);
    result.setMessage(message);
    return result;
}
```

### 6.5 保留不动的 6 个 private 方法

`checkTeacherDailyLimit`、`checkClassDailyLimit`、`hasSameCourseSameDay`、`countScheduledSlots`、`ensureSchedulesUnlocked`、`saveSchedule`、`categorizeReason`

理由：它们直接打 DB，绑死 AutoScheduleService 的语义（实时 + read-own-writes），抽到工具类反而失去上下文。

> 注意：原 `hasSameCourseSameDay` 签名带 `batchId` 参数但内部没用（L400），重构后从签名删除。

### 6.6 行为等价性验证

| 原行为 | 新行为 | 是否等价 |
|---|---|---|
| L142 外层循环 task 顺序 | `arrangeAllTasks` 同样 for-each `sortedTasks` | ✅ |
| L189 `for (int i = 0; i < remainingSlots; i++)` 单节循环 break 退出 | `arrangeOneTask` 内同样 break | ✅ |
| L244 `if (!arranged) break;` 立即停止 | `tryFindFirstFitAssignment` 返回 `notPlaced` 后 `arrangeOneTask` break | ✅ |
| L195/202/210/220/237 `lastFailReason/lastFailReasonType` 跟踪 | `FailReason` record 累积 | ✅ |
| L233 `usedDays.add` 在成功 break 时机 | `arrangeOneTask` 在 `saveSchedule` 后 add | ✅ |
| L226 `conflictService.checkConflict` 第四参数 null | 不变 | ✅ |
| 事务边界：整个 `run()` @Transactional | 不变（重构不影响 @Transactional 注解位置）| ✅ |
| L215 `if (!allowSameCourseSameDay && usedDays.contains(...))` 嵌套 `if (hasSameCourseSameDay(...))` | 改为单个 `&& hasSameCourseSameDay(...)` 表达式 | ⚠️ **行为变更**：原版只在内层 if 失败时设置 lastFail，新版无论内层结果都会 continue。但因 `hasSameCourseSameDay` 决定是否 continue 的逻辑等价，整体路径相同。**这点需要在 PR 描述里特别说明并人工核验**。|

### 6.7 工时

约 **4-5 小时**，含：
- 拆方法 + 写 record（2h）
- 编译通过 + 现有冒烟测试通过（1h）
- 人工跑一次自动排课，对比新旧 batch 结果一致（1-2h）

### 6.8 测试

新增组件级测试 `AutoScheduleServiceIntegrationTest`（如果项目还没有）：

```java
@SpringBootTest
@Transactional  // 测试结束回滚
class AutoScheduleServiceIntegrationTest {

    @Autowired AutoScheduleService service;

    @Test void run_givenFreshSemester_arrangesAllTasks() {
        // arrange：插入 3 个教学任务、3 个教室、5 个时间段
        // act：service.run(req)
        // assert：返回 SUCCESS，generatedScheduleCount > 0
    }

    @Test void run_givenLockedSchedule_throwsBusinessException() { ... }

    @Test void arrangeOneTask_givenMissingClassroom_returnsCoursePreFlightFailed() {
        // 这里其实测不了 private 方法，但通过 run 流程能间接覆盖
    }
}
```

**注意**：`arrangeOneTask` / `tryFindFirstFitAssignment` 是 private，集成测试只能从 `run()` 入口测。如果想细粒度测这些方法，需把它们改成 package-private 并加 `@VisibleForTesting` 注释。**建议先保持 private**，等真有具体场景需要再放权限。

---

## 7. Commit 4（可选）：V3ScheduleGenerateService 应用同样手术

V3 的 `generate()` + `generatePlanItems()` + `findBestCandidate()` 结构与 Auto 不完全一致（V3 已经部分拆分了），但仍可继续：

1. 删除 V3 内部 `GenerationContext` record，全替换为 `SchedulingReferenceData`
2. 删除 V3 的 `loadXxx` 私有方法（已被 Commit 2 替换）
3. 把 `findBestCandidate` 拆出 `tryFindBestSlot()` + `tryFindBestRoomForSlot()`

**判断标准**：如果 Commit 1-3 合入后 PR 评审顺利，再考虑 Commit 4；否则留独立 ticket，不阻塞主 PR。

约 **3 小时**。

---

## 8. 工时合计

| 步骤 | 工时 |
|---|---|
| Commit 1 SchedulingSupport + 测试 | 2h |
| Commit 2 SchedulingReferenceLoader | 3h |
| Commit 3 AutoScheduleService 主循环拆分 | 4-5h |
| Commit 4 V3 同样手术（可选）| 3h |
| **核心 1-3 合计** | **9-10h** |
| **加 Commit 4 合计** | **12-13h** |
| 缓冲（PR 评审反馈、回归测试发现的问题）| +2-3h |
| **现实预期** | **15h 左右** |

---

## 9. 风险清单

| 风险 | 等级 | 缓解 |
|---|---|---|
| 主循环行为变更（同课同日检查嵌套 if → && 表达式）| **高** | 6.6 已标出，PR 中必须人工核验 + 跑一次完整自动排课对比新旧结果 |
| `sortTimeSlots` null 容错的引入（修 bug，但改了原行为）| 中 | commit message 明确写"修了 NPE 隐患"，回归测试若数据无 null 不受影响 |
| V3 全量加载 course/class（原为按需）| 低 | 当前数据量百级，可接受；若性能告警再补按需加载 |
| `arrangeOneTask` 是 private 不可直接单测 | 中 | 通过 `run()` 集成测试覆盖关键分支；待真有需求再放权限 |
| `conflictService.checkConflict` 在重构后调用次数可能微妙变化 | 中 | 6.6 表逐项核对，调用次数等价 |
| @Transactional 边界 | 低 | 没动 `run()` 上的注解，行为不变 |

---

## 10. 明确不做的事

- ❌ **不加 `IAutoScheduleService` / `IScheduleGenerateService` 接口** —— 当前只有一个实现，加接口是噪音
- ❌ **不抽 `ISchedulingEngine`** —— YAGNI，没有第二个算法
- ❌ **不合并 AutoScheduleService 和 V3ScheduleGenerateService 的算法** —— 业务模型不同（live vs draft），强合会改语义
- ❌ **不动 ScheduleConflictService** —— 它是独立服务，跨多处使用
- ❌ **不动 LockGuardService / BatchService / UnscheduledTaskService** —— 边界已经清晰
- ❌ **不涉及 V5 修复链路** —— V5RepairService 等有自己的排课逻辑，独立演进
- ❌ **不涉及 D2（评分体系收口）** —— V3 的 `scoreCandidate` 属 D2 范围

---

## 11. 验收标准

每个 commit 都必须满足：

1. `mvn compile` 通过
2. 现有冒烟测试通过
3. **关键**：在本地手工跑一次完整自动排课，对比新旧版本：
   - `auto_schedule_batch.success_task_count` 一致
   - `auto_schedule_batch.failed_task_count` 一致
   - `auto_schedule_batch.generated_schedule_count` 一致
   - `schedule_unassigned_task` 表中 reason_code 分布一致

如果任何一项不一致 → 回滚该 commit，重新查证。

---

## 12. 何时启动

**当前不建议立刻启动**。理由：

- PR #2 还 OPEN，未合入 main。再叠 D1.2 改动会让 PR diff 过大、评审困难
- D1.2 涉及核心业务逻辑，需要充足回归窗口
- 建议先合 PR #2 → 拉新分支 `refactor/d1.2-scheduling-cleanup` → 按 Commit 1→2→3 顺序逐个推进

每个 commit 单独 PR 评审，比一次合 4 个 commit 风险低得多。
