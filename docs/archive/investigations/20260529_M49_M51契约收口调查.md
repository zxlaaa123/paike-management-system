# M-49 / M-51 契约收口调查

## 结论

M-49 不成立，已验证。

`ScheduleRuleWeightController.WeightUpdateRequest.weight` 不加 `@NotNull` 是接口契约设计。单条权重更新接口是部分更新语义：`weight`、`enabled`、`description` 都允许只提交其中一部分；服务层只更新非空字段，前端 API 类型也将 `weight` 声明为可选。

M-51 不成立，已验证。

`AdminUserInitializer` 在未配置 `ADMIN_DEFAULT_PASSWORD` 时，把随机初始密码输出到 stdout 是首次 admin 凭据交付机制。代码已避免写入日志文件；README 和 `.env.example` 均说明可通过 `ADMIN_DEFAULT_PASSWORD` 显式配置，未配置时查看首次启动输出。

## 本轮验证

- 新增 `M49M51ContractClosureTest`：
  - `ScheduleRuleWeightService.updateWeight(id, null, enabled, description)` 保留原 `weight`，只更新非空字段。
  - 前端 `updateRuleWeight()` 类型继续声明 `weight?: number`。
  - 后端 `WeightUpdateRequest` 不包含 `@NotNull`，保持部分更新契约。
  - `AdminUserInitializer` 未配置默认密码时会向 stdout 输出随机初始密码提示。
  - 配置默认密码时不输出随机密码提示，并使用配置值编码。

## 后续触发条件

- M-49：产品确认单条更新必须变为全量提交，再给 `weight` 加 `@NotNull`，同时同步前端类型。
- M-51：运维确认新的首次凭据交付渠道，再移除 stdout 随机密码输出或改为强制配置。

