# M-14 阶段1 收口报告 — 低风险端点 `Map<String,Object>` → VO

> 任务：D-14 / 20260527bug验证报告 M-14 项
> 日期：2026-06-03
> 分支：`refactor/m14-mapstringobject-dto-ify`
> 蓝图：`20260602_M14_MapStringObject风险评估.md` §4.1 阶段1
> 范围：仅 Service + Controller + VO 三层；不碰 SQL/Entity/Mapper；前端零改动
> 结论：**6 个低风险端点已机械替换为 VO，JSON 契约逐字段不变，序列化测试锁定通过**

---

## 1. 做了什么

按蓝图阶段1（"测试先行 + 低风险收割"），把 6 个返回 `Map<String,Object>` / `List<Map<String,Object>>` 的端点替换为强类型 VO。**字段名、字段顺序、值类型与历史 JSON 完全一致**，前端无需改动。

| # | 端点 / 方法 | 原 Map 字段 | 新 VO | VO 包 |
|---|---|---|---|---|
| 1 | `HealthController.health` | status / service / time | `HealthInfoVo` | `controller.vo` |
| 2 | `AuthController.logout` | success | `LogoutResultVo` | `auth.vo` |
| 3 | `ScheduleController.checkConflict` | hasConflict / message | `ConflictCheckResultVo` | `controller.vo` |
| 4 | `ScheduleScoreController.getScoreSummary` | planId / totalScore / hardViolationCount / softViolationCount / scoreLevel（**5 字段**） | `ScoreSummaryVo` | `controller.vo` |
| 5 | `ScheduleScoreController.rescore` | planId / totalScore / conflictCount / scoreLevel | `RescoreResultVo` | `controller.vo` |
| 6 | `SchedulePlanExplainService.summarizeUnassignedTasks`（+ `SchedulePlanController.getUnassignedSummary` 返回类型） | reasonCode / reasonName / count(Long) | `UnassignedSummaryVo` | `service.vo` |

- **新增 VO**：6 个（`@Data @AllArgsConstructor @NoArgsConstructor`，对齐 `Result.java` 风格）。
- **改动文件**：5 个 controller（Health/Auth/Schedule/ScheduleScore + SchedulePlan 仅签名）+ 1 个 service（SchedulePlanExplainService），并清理 ScheduleController/AuthController 的 `import java.util.Map`。
- **唯一服务方法调用方**：`summarizeUnassignedTasks` 仅被 `SchedulePlanController.getUnassignedSummary` 调用（已核验），返回类型变更无其他牵连。

## 2. 两个决策点

### 2.1 `getScoreSummary` 的 conflictCount —— 维持现状（5 字段）
前端 `ScoreSummary` 类型声明了 6 个字段（含 `conflictCount`），但后端历史上只发 5 个，前端用 `?? plan.conflictCount` 兜底。

**决定（已确认）**：阶段1 维持后端 5 字段、不补发 `conflictCount`。理由：
- 阶段1 原则是"先锁现有 JSON 输出再机械替换"，补发属于行为变更；
- 补发需额外 `planService.getById(planId)` 查询（getScoreSummary 当前不加载 plan），不再是纯机械替换；
- 契约不一致是历史既存项，留作后续单独定案，不在本阶段固化或扩散。

`ScoreSummaryVo` 的 Javadoc 已显式记录该不一致点。

### 2.2 顺带消除隐性 NPE（白送的健壮性改进）
`getScoreSummary` / `rescore` 原用 `Map.of(...)`，而 `Map.of` **不允许 null 值**——一旦 `totalScore` / `conflictCount` 为 null 会直接抛 NPE（500），尽管 `getScoreLevel(null)` 本身兼容 null。转 VO 后 null 安全。**happy-path JSON 完全一致**，仅消除了 null 路径的崩溃。

## 3. 测试

### 3.1 新增序列化守卫 `M14Phase1VoSerializationTest`（6 用例，全绿）
用各 controller 当前的取值构造 6 个 VO，断言序列化后的**字段集 == 替换前 Map 的键集**、标量值 == 替换前 put 的值。
- 走真实 wire 路径（`writeValueAsString` + `readTree`），而非 `valueToTree`——后者的 TokenBuffer 会对 BigDecimal 调 `stripTrailingZeros`（把 `90` 写成 `9E+1`），与 HTTP 实际输出不符。
- BigDecimal 一律用 `decimalValue().compareTo()` 数值比较，忽略 scale。
- `time`(LocalDateTime) 用注册 JavaTimeModule 的 mapper，仅断言非 null（不耦合日期格式）。

### 3.2 更新现状计数 `M14MapStringObjectUsageInvestigationTest`（棘轮基线，全绿）
阶段1 移除 9 行 `Map<String,Object>`、5 个文件清零：

| 维度 | 阶段1 前 | 阶段1 后 |
|---|---:|---:|
| 总处数 | 66 | **57** |
| 文件数 | 16 | **11** |
| controller 处数 | 21 | **14** |
| service 处数 | 45 | **43** |
| 公开端点契约（`public Result<...Map...>`） | 18 | **12** |
| ScheduleStatisticsService（阶段3 目标） | 23 | 23（不变） |
| SchedulePlanService（阶段2 目标） | 10 | 10（不变） |
| ScheduleCompareService（阶段2 目标） | 7 | 7（不变） |

> 注：6 个新 VO 的 Javadoc 最初写了 `Map<String,Object>` 字面量，被计数正则（`Map\s*<\s*String\s*,\s*Object\s*>`，`\s*` 也匹配无空格形式）误计；已改写为"Map 弱类型返回"措辞，避免污染基线。

### 3.3 前端
**零改动**：6 个端点的 JSON 字段名/类型不变，TS 接口（`schedule.ts` / `scheduleScore.ts` / `UnassignedSummaryItem[]`）继续对齐；logout/health 前端不读 body。沿用上次（N-24/N-30）的绿色构建，无需重建。

## 4. 验证命令

```powershell
mvn -f D:\paike\backend\pom.xml "-Dtest=M14Phase1VoSerializationTest,M14MapStringObjectUsageInvestigationTest" test
# Tests run: 8, Failures: 0, Errors: 0 — BUILD SUCCESS（含全量 main 重新编译 263 文件）
```

## 5. 剩余（后续阶段）

| 阶段 | 范围 | 待建 VO |
|---|---|---|
| 阶段2 | `SchedulePlanService` 4 方法 + `ScheduleCompareService.compare` + `V5SimulationService.apply` 透传 | AdjustPlanResultVo / ApplyPlanResultVo / RollbackPlanResultVo / ComparePlanVo / CompareResultVo |
| 阶段3 | `ScheduleStatisticsService` 5 方法 + `loadItems()` 多态收口 | TeacherWorkloadVo / ClassroomUtilizationVo / ClassBalanceVo / PlanOverviewVo / DashboardStatsVo |
| 阶段4 | 计数测试最终归零 / 仅保留内部聚合用法；补齐各阶段 VO 序列化测试 | — |

剩余 57 处中：`ScheduleStatisticsService`（23）的内部聚合 + 多态 `loadItems()` 仍是真正难点，放阶段3。两个 refresh 端点（`refreshPlanSummary` / `refreshPlanRisks`）是活端点，按活端点改 VO，不可删。
