# M-16 实体视图字段调查

## 结论

部分成立，暂不修复生产代码。

原问题指出多个 Entity 使用 `@TableField(exist = false)` 保存视图字段。复查后确认问题方向成立，但当前数量已不是原记录的 40 处，而是 9 个实体、43 个真实注解字段。

当前分布：

- `Schedule`：10 处。
- `ScheduleAdjustLog`：5 处。
- `ScheduleConflictReport`：1 处。
- `SchedulePlan`：2 处。
- `SchedulePlanItem`：5 处。
- `ScheduleUnassignedTask`：3 处。
- `TeacherUnavailableTime`：5 处。
- `TeachingTask`：8 处。
- `UnscheduledTask`：4 处。

这些字段主要分为三类：

- 关联展示名称：`courseName`、`teacherName`、`className`、`roomName`、`timeLabel`、`batchNo` 等。
- 关联表派生属性：`courseType`、`studentCount`、`teacherStatus`、`classStatus`、`dayOfWeek`、`periodNo` 等。
- 页面展示/解释字段：`sourceTypeName`、`oldClassroomName`、`newClassroomName` 等。

其中 `TeachingTaskMapper.xml` 和 `UnscheduledTaskMapper.xml` 通过 SQL alias 直接填充部分字段，例如 `courseType`、`teacherName`、`className`、`batchNo`。其他字段则由 Service/Controller 在批量加载关联数据后组装。

## 判断

这属于实体层和视图层职责混杂问题，不是单点运行 bug。

本轮不直接删除或迁移字段，原因：

- 多个接口当前直接返回 Entity，字段已经成为前端 JSON 契约的一部分。
- `Schedule`、`TeachingTask`、`SchedulePlanItem` 等核心实体在排课、统计、解释、冲突检测等路径被复用，直接拆 VO 回归面大。
- 仅删除少量看似未使用字段不能解决结构问题，反而容易造成前端展示字段缺失。

## 后续建议

如要继续治理，建议按模块逐步迁移：

1. 先为 `Schedule` 列表/详情定义 `ScheduleView` 或 `ScheduleVO`，替代最集中的展示字段。
2. 再为 `TeachingTask` 查询结果定义独立 VO，承接 Mapper alias 字段。
3. 最后处理 `SchedulePlanItem`、`UnscheduledTask`、`TeacherUnavailableTime` 等周边展示模型。

## 本轮验证

新增 `M16TableFieldViewFieldsInvestigationTest` 锁定当前事实：

- 后端 Entity 中有 43 处真实 `@TableField(exist = false)`。
- 分布在 9 个实体。
- `Schedule`、`TeachingTask`、`SchedulePlanItem` 等为主要集中点。
- `TeachingTaskMapper.xml` / `UnscheduledTaskMapper.xml` 仍通过 SQL alias 填充部分视图字段。

