# M-31 逻辑删除实体覆盖调查

## 结论

部分成立，暂不修复生产代码。

项目确实启用了 MyBatis-Plus 全局逻辑删除配置：

- `logic-delete-field: deleted`
- `logic-delete-value: 1`
- `logic-not-delete-value: 0`

但当前缺少 `deleted` 字段的映射实体，对应 SQL 表也没有 `deleted` 列。它们不是“实体漏映射已有列”，而是这些表目前未纳入软删除模型。

## 当前缺少 deleted 的映射实体

- `AutoScheduleBatch`
- `ScheduleCandidatePosition`
- `ScheduleConflictReport`
- `ScheduleConsistencyCheck`
- `ScheduleGenerateLog`
- `ScheduleOptimizationCompare`
- `ScheduleRegressionTest`
- `ScheduleRepairSuggestion`
- `ScheduleRepairTask`
- `ScheduleRuleConfig`
- `ScheduleRuleWeight`
- `ScheduleScoreReport`
- `SystemAuditLog`
- `TimeSlot`
- `UnscheduledTask`

## 判断

这些表包含日志、报告、配置、V5 修复过程数据、候选位置和时间段等类型。直接补 `deleted` 字段会改变删除语义和查询语义，需要逐表确认产品/业务期望，不适合作为本轮机械修复。

本轮补充 `M31LogicDeleteEntityCoverageInvestigationTest` 锁定当前事实：

- 全局逻辑删除配置存在。
- 有 `deleted` 字段的实体必须带 `@TableLogic`。
- 缺少 `deleted` 字段的 15 个实体，其 SQL 表声明也不包含 `deleted`。
