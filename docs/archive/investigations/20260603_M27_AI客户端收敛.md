# M-27 远程 AI 客户端收敛

> 对象：`20260527bug验证报告.md` M-27（远程 AI 调用重复）
> 日期：2026-06-03
> 分支：`refactor/m27-ai-client-consolidation`
> 结论：**部分成立 → 已收敛**。按 `20260529_M27_AI客户端复用调查.md` §"如后续再抽"预设的蓝图实施。

---

## 1. 背景

`20260529_M27_AI客户端复用调查.md` 已查明：原条目"每次请求新建 HttpClient"不成立（两处均用静态 `HTTP_CLIENT`），真正成立的是**可维护性重复**——`V4ScheduleAiAnalysisService` 与 `V5RepairExplanationService` 各自重复：

- 注入同一组 `app.ai.*`（api-key / base-url / model / timeout-ms 共 4 项 ×2）；
- 静态 `HTTP_CLIENT` 与 chat completion 的 HTTP envelope（URI / 超时 / 鉴权头 / POST / 状态码校验 / 取 `choices[0].message.content`）；
- `extractJson(...)` 响应 JSON 子串提取；
- `sanitizeForPrompt(...)` prompt 字段清洗（防注入 + 截断）、`FIELD_MAX_LEN`、`isRemoteAiEnabled()`。

该调查当时建议"暂不修复"，并预留了收敛蓝图。本轮按蓝图实施。

## 2. 改造

**新增（通用层）：**

| 类 | 职责 |
|---|---|
| `config/AiChatProperties`（`@ConfigurationProperties(prefix="app.ai")`） | 集中承载 `app.ai.*` 配置；默认值与历史 `@Value` 默认值一致。 |
| `service/RemoteAiChatClient`（`@Component`） | 只负责通用部分：静态 `HTTP_CLIENT`、`chat(system, user)` 发送 + 状态码/空内容校验 + 原始 content 提取、`extractJson(content)`、`sanitizeForPrompt(value)`、`isEnabled()`。 |

**改造（业务层）：** `V4ScheduleAiAnalysisService`、`V5RepairExplanationService`

- 删除各自的 `HTTP_CLIENT`、`FIELD_MAX_LEN`、4 个 `@Value`、`isRemoteAiEnabled()`、`sanitizeForPrompt()`、`extractJson()`；
- 改为 `private final RemoteAiChatClient aiChatClient` 依赖，调用 `aiChatClient.isEnabled()` / `chat(...)` / `extractJson(...)` / `sanitizeForPrompt(...)`；
- **保留各自的 prompt 组装、system 提示词、业务 JSON schema 解析（V4 的 analysisText/suggestions、V5 的 7 字段）和本地兜底**，避免业务 schema 混入通用客户端。

## 3. 行为等价性

- 请求 envelope、鉴权、超时下限（`Math.max(timeout, 5000)`）、状态码区间、content 提取路径（`choices[0].message.content`）逐字保持。
- prompt 文本（system + user）与 `sanitizeForPrompt` / `extractJson` 逻辑逐字保持。
- 远程失败仍被各服务 `catch` 后 `log.warn` 并回退本地兜底——唯一可见差异是异常 message 由"AI 分析/解释调用失败"统一为"AI 调用失败"，该 message 仅出现在内部日志、不外泄给用户，不改变回退行为。
- 配置读取：`AiChatProperties` 经 relaxed binding 绑定 `app.ai.api-key/base-url/model/timeout-ms`，与历史 `@Value` 取值一致。

## 4. 验证

- 测试由 `M27AiHttpClientDuplicationInvestigationTest`（锁定收敛前重复现状）改写为 `M27AiClientConsolidationTest`（锁定收敛后状态）：
  - 全 `main/java` 中 `java.net.http.HttpClient` 仅 `RemoteAiChatClient` 一处使用；`HttpClient.newBuilder()` 计 1、`newHttpClient()` 计 0；
  - `AiChatProperties` 以 `@ConfigurationProperties(prefix="app.ai")` 承载 4 字段；两业务服务不再含 `@Value("${app.ai...`、不再各自持 `HttpClient`、改依赖 `RemoteAiChatClient`；
  - `sanitizeForPrompt` / `extractJson` / `chat` 收敛到 `RemoteAiChatClient`，两业务服务不再各自定义。
- 命令：`cd D:\paike\backend; mvn -Dtest=M27AiClientConsolidationTest,M50RequestBodyOptionalityInvestigationTest test`
  - 结果：4 tests，0 failures，0 errors，BUILD SUCCESS（M50 验证 V4 请求可选性契约未受影响）。

## 5. 关联文件

| 用途 | 路径 |
|---|---|
| 配置 | `backend/src/main/java/com/paike/scheduler/config/AiChatProperties.java` |
| 共享客户端 | `backend/src/main/java/com/paike/scheduler/service/RemoteAiChatClient.java` |
| 业务服务 | `backend/.../service/V4ScheduleAiAnalysisService.java`、`V5RepairExplanationService.java` |
| 测试 | `backend/.../architecture/M27AiClientConsolidationTest.java` |
| 前置调查 | `20260529_M27_AI客户端复用调查.md` |
