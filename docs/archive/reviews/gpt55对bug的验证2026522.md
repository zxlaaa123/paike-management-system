# GPT-5.5 对 bug 的验证报告 2026-05-22

验证对象：`D:\paike\代码深度审查报告_2026-05-22.md`

验证范围：只做静态核验，不修改代码，不启动后端。

结论汇总：

| 判定 | 数量 |
|---|---:|
| 真实 | 31 |
| 部分真实 / 表述需修正 | 7 |
| 不成立 / 当前代码已不符合原描述 | 1 |

## 逐项验证

| 编号 | 原问题 | 判定 | 验证依据 |
|---|---|---|---|
| P0-1 | `AutoScheduleService.saveSchedule` 没写 `semesterId`，导致同批次内冲突检测失效 | 部分真实 | `saveSchedule` 确实未调用 `schedule.setSemesterId(...)`。但当前 `ScheduleConflictService.checkConflict` 已用 `task.getSemesterId()` 过滤正式课表，因此“同批次内冲突检测失效”表述不准。真实影响是自动排课落库记录缺少学期归属，后续按 `schedule.semester_id` 查询、报表、清理、统计会异常。 |
| P0-2 | 编辑场景 daily-count 漏 `excludeScheduleId` | 真实 | `checkConflict` 前面的时间段冲突和课时上限查询会 `ne(Schedule::getId, excludeScheduleId)`；但每日软规则调用 `selectDailyConflictCounts(..., task.getSemesterId(), null)`，Mapper 参数没有 `excludeScheduleId`，XML 也没有排除当前记录。 |
| P0-3 | 前端把 httpOnly Cookie 设计废掉 | 真实 | `AuthController` 设置 `paike_token` 为 `httpOnly(true)`；前端 `request.ts` 从 `localStorage` 读取 token 并写 `Authorization`，且未配置 `withCredentials`。`auth.ts` 也使用 `localStorage` 保存 token。 |
| P0-4 | `SchedulePlanService.applyPlan` 应用时不重新冲突检测 | 真实 | `applyPlan` 只校验状态和明细数量，然后直接把旧方案置 `DRAFT`、插入正式 `schedule`；没有调用 `checkConflict`、`refreshPlanConflictState` 或类似硬约束复检。 |
| P1-1 | `ScheduleController.create` 使用当前学期，`checkConflict` 使用任务学期 | 真实 | `create` 从当前学期写入 `schedule.semesterId`；`ScheduleConflictService.checkConflict` 基于 `TeachingTask.task.semesterId` 过滤。任务学期和当前学期不一致时，校验与落库口径不一致。 |
| P1-2 | `AutoScheduleService` daily-limit / sameDay 不带 `semester_id` | 真实 | `checkTeacherDailyLimit`、`checkClassDailyLimit`、`hasSameCourseSameDay` 查询 `Schedule` 时只按教师/班级/课程、日期、deleted 过滤，无 `semesterId`。 |
| P1-3 | 旧 `APPLIED` 改 `DRAFT` 后可被 `delete()` 物理删除 | 真实 | `applyPlan` 将旧应用方案设为 `DRAFT`；`delete` 只要求状态为 `DRAFT`，随后删除 plan items、清 artifacts、`planMapper.deleteById(id)`。`SchedulePlan` 无 `@TableLogic`。 |
| P1-4 | `rollbackPlan` 调 `applyPlan(id)` 会把自身列为 oldAppliedPlans | 部分真实 | `rollbackPlan` 确实直接 `return applyPlan(id)`；若目标方案当前就是 `APPLIED`，`applyPlan` 会把同学期所有 `APPLIED` 包括自身先改 `DRAFT` 再重插 schedule。是否造成实际错误取决于入口是否允许对当前应用方案回滚，风险成立但触发语义需明确。 |
| P1-5 | `checkConflict` 软规则只看 `plan_id IS NULL` | 真实 | `baseWrapper` 和 `taskWrapper` 都 `.isNull(Schedule::getPlanId)`；每日统计 SQL 在 `planId == null` 时也走 `AND s.plan_id IS NULL`。应用方案生成的正式课表带 `plan_id`，会被排除。 |
| P1-6 | `AuthService.login` 密码正确后才检查 status，泄露停用账号枚举 | 真实 | `login` 先校验 `passwordEncoder.matches(...)`，再判断 `user.getStatus()`；停用账号密码正确与密码错误返回路径不同。 |
| P2-1 | `applyPlan` 与 `applySimulationPlan` 重复 60+ 行 | 部分真实 | 当前代码已有 `applyPlanInternal(plan)`，`applySimulationPlan` 只做状态校验后调用 internal；但普通 `applyPlan` 仍保留一整份应用逻辑，和 `applyPlanInternal` 高度重复。原“applySimulationPlan 重复 60+ 行”不完全符合当前代码。 |
| P2-2 | `ScheduleConflictService` reasonTag 协议依赖字符串前缀 | 真实 | `tagReason` / `stripReasonTag` 使用字符串协议，冲突原因中包含前缀标签，后续依赖解析。 |
| P2-3 | `ScheduleController.ScheduleForm` 只有 `@Getter` 没有 `@Setter` | 真实 | 内部 `ScheduleForm` 类标注 `@Getter`，未见对应 `@Setter`。Jackson 反序列化可受影响。 |
| P2-4 | `WebMvcConfig` 注释和实际路径冲突 | 部分真实 | `WebMvcConfig` 对 auth 路径做 exclude 配置，注释/路径需要与实际 `/api/auth/**` 口径核对。风险偏文档一致性，不是直接功能缺陷。 |
| P2-5 | `SemesterSchemaInitializer` 与 `schema.sql` / SQL 初始化并存 | 部分真实 | `schema.sql` 存在，`application.yml` 启用 SQL init；项目也有 `SemesterSchemaInitializer`。多套 schema 演进方式并存属维护风险，但是否冲突取决于部署策略。 |
| P2-6 | `AuthInterceptor` 每请求查 DB 的 `user.status` | 真实 | `preHandle` 解析 token 后执行 `sysUserMapper.selectOne(...id...)`，再检查 `user.getStatus()`。每次认证请求都会访问数据库。 |
| P2-7 | `RateLimiterService.cleanupExpiredKeys` O(N) 每次登录调用 | 真实 | `isRateLimited` 每次先调用 `cleanupExpiredKeys`；`cleanupExpiredKeys` 对 `attempts.forEach(...)` 全量遍历。 |
| P2-8 | `useCrudForm.openAdd` 浅拷贝 `formDefaults` | 真实 | `openAdd` 中存在 `Object.assign(form, options.formDefaults)`，嵌套对象/数组会共享引用。 |
| P0-5 | `SchedulePlan` / `SchedulePlanItem` / `Semester` 物理删除 | 真实 | 三个实体均未见 `@TableLogic`；相关 `deleteById` 或 mapper delete 是物理删除语义。 |
| P0-6 | 锁定功能只能创建记录，写入前未检查锁定状态 | 真实 | `V4ScheduleLockService` 能创建/解除锁；`AutoScheduleService`、`SchedulePlanService.applyPlan` 等排课写入路径未引用锁表或锁检查。V5 局部重排有部分锁读取，但不是全局写保护。 |
| P0-7 | `V4ScheduleLockService.lock` 双锁定竞态 | 真实 | `lock` 先 `findActiveLock`，再 `insert`；`findActiveLock` 是普通 select，schema 未见针对 active lock 的唯一约束。并发下可插入重复 active 记录。 |
| P1-7 | `ScheduleScoreService.buildScoreContext` 用 `conflictReason.contains` 判冲突类型 | 真实 | `teacherUnavailableCount`、`capacityViolationCount`、`roomTypeMismatchCount` 通过 `conflictReason.contains("禁排"/"容量"/"类型")` 统计。 |
| P1-8 | `ScheduleConflictReportService.generate` 不按 semester 过滤 | 真实 | `generate()` 查询所有 `deleted=0` 的 `Schedule`，无 `semesterId` 参数和过滤。 |
| P1-9 | `AutoScheduleService.run` 单事务包整个批次 | 真实 | `run` 标注 `@Transactional(rollbackFor = Exception.class)`，内部包含清理旧排课、读取任务、遍历排课、批次状态更新。 |
| P1-10 | `checkAdjustment` / `applyAdjustment` 之间 TOCTOU | 真实 | `applyAdjustment` 先调用 `checkAdjustment(request)`，无行锁/版本号/唯一约束保护，随后再解析上下文并写入。检查后到写入前可被并发修改。 |
| P1-11 | 前端 `handleApply` 只 warning，后端 `applyPlan` 不再 check | 真实 | 前端存在 warning 流程；后端 `applyPlan` 无冲突复检。因此冲突方案仍可能被直接应用。 |
| P2-9 | schema 无外键约束 | 真实 | `backend/src/main/resources/db/schema.sql` 未见 `FOREIGN KEY` / `REFERENCES`。 |
| P2-10 | `ScheduleRuleService.ruleCache` 是进程内 `ConcurrentHashMap` | 真实 | 服务内使用 `ConcurrentHashMap` 缓存规则，多实例部署会有缓存一致性问题。 |
| P2-11 | `V4ScheduleAdjustmentService.resolveScheduleContext` 硬编码 period 映射 | 真实 | 存在 `context.currentStartPeriod = context.currentSlot.getPeriodNo() * 2 - 1`。 |
| P2-12 | `weeklyHours=0` 时错误文案迷惑 | 部分真实 | `checkConflict` 将 null 视为 0，并计算 `requiredSlots = ceil(weeklyHoursVal / 2.0)`；当为 0 时新增排课必然触发“最多排 0 个大节”类提示。属于体验/数据校验问题，非崩溃 bug。 |
| P2-13 | `ScheduleConflictReportService.clear` 用 LIKE 前缀匹配误删 | 真实 | `clear(reportNo)` 中使用 `report_no LIKE normalized + "%" ESCAPE ...` 删除，传入短前缀会删除多个报告编号。 |
| P0-8 | `V5SimulationService.copyLocks` 复制锁记录无上限，且缺唯一约束放大 | 真实 | `copyLocks` 遍历源 locks 后直接 `lockedItemMapper.insert(target)`；schema 未见 active lock 唯一约束。多次生成 simulation plan 会复制出多批锁副本。 |
| P1-12 | `V5SimulationService.localReplan` 可反复创建 simulation plan 且不清理旧的 | 真实 | `localReplan` 每次 `createLocalReplanPlan`、`copyItems`、`copyLocks`，方法内无删除旧 simulation plan / items / locks / score detail 的逻辑。 |
| P1-13 | `findBestLocalPlacement` 双循环并同步调 `ruleEvaluationService` | 真实 | 方法遍历 `timeSlots` × `classrooms`，每个候选调用 `ruleEvaluationService.evaluateCandidate(evalReq)`，直到 `candidateLimit`。 |
| P1-14 | V5 又一次硬编码 `slot.getPeriodNo() * 2 - 1` | 真实 | `V5SimulationService.findBestLocalPlacement` 中存在该公式，文件中同类公式出现 2 次。 |
| P1-15 | `localReplan` appendAdjustLog 后判新硬冲突并 throw，日志回滚 | 真实 | `localReplan` 是事务方法；循环中可能先 `appendAdjustLog`，之后 `refreshPlanConflictState`、`buildCompare`，若 `compare.getHasNewHardConflicts()` 为 true 则抛异常，事务回滚包括已写日志。 |
| P2-14 | V5 `apply` 在 `ensurePassBeforeApply` 前不 refresh/rescore | 真实 | `V5SimulationService.apply` 先调用 `ensurePassBeforeApply`，之后才调用 `refreshPlanConflictState` 和 `rescore`。 |
| P2-15 | V5 `discard` 只改状态，不清理附属数据 | 真实 | `discard` 仅将 plan 状态设为 `DISCARDED` 并更新 task 状态；无删除 simulation plan items、锁副本、score detail 等逻辑。 |
| P2-16 | `candidateLimit` 默认 600 / 上限 2000 硬编码 | 真实 | `localReplan` 内直接写 `? 600 : Math.min(..., 2000)`，未见 `@Value` 或配置属性注入。 |

## 需要修正原报告表述的点

1. P0-1：代码问题真实，但“导致同批次内冲突检测失效”当前不准确。`checkConflict` 已按 `task.semesterId` 过滤；更准确说法是“自动排课落库漏写 `semester_id`，导致后续按学期查询/统计/报表/清理不可靠”。
2. P2-1：当前已有 `applyPlanInternal`，`applySimulationPlan` 本身不再重复 60+ 行；重复点主要是 `applyPlan` 与 `applyPlanInternal`。
3. P1-4：风险成立，但需要限定触发条件：目标回滚方案若已处于 `APPLIED`，会被 `applyPlan` 先列入旧应用方案处理。

## 唯一不成立项

无完全不成立项。P2-7 经复核为真实：`cleanupExpiredKeys` 使用 `attempts.forEach` 全量遍历，且由 `isRateLimited` 每次调用触发。
