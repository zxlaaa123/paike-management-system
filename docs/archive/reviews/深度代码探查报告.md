# 深度代码探查报告

> 生成时间：2026-05-23 | 审查范围：backend 227 个 Java 文件 + frontend 84 个 Vue/TS 文件 + schema.sql 及迁移脚本
> 审查方式：6 个并行 Agent 分层深度审计（auth/config、entity/mapper、service、controller、前端 API/utils、前端 views/components）

---

## 总览

| 优先级 | 数量 | 说明 |
|--------|------|------|
| **P0 严重** | 9 | 安全漏洞、数据完整性、运行时必崩 |
| **P1 高** | 19 | 逻辑缺陷、性能问题、缺失保护 |
| **P2 中** | 28 | 不一致、硬编码、代码质量 |
| **P3 低** | 8 | 代码异味、可维护性 |
| **合计** | **64** | |

---

## 一、P0 严重问题（9 项）

### 1.1 [安全] /v3/** 接口完全绕过认证

- **文件**: `WebMvcConfig.java:27`
- **问题**: `addPathPatterns("/api/**")` 仅拦截 `/api/**`，但项目存在 `/v3/` 前缀接口。这些端点无需任何认证即可直接访问。
- **修复**: 将拦截器路径扩展为 `"/api/**", "/v3/**"` 或统一接口前缀。

### 1.2 [安全] 全部控制器缺少细粒度授权

- **文件**: 全部 38 个 Controller
- **问题**: 没有任何 `@PreAuthorize`、`@Secured` 或自定义权限注解。任何已登录用户可执行所有操作，包括删除、批量修改、排课生成等。
- **修复**: 至少在 DELETE 端点、排课生成、规则批量修改、方案发布等关键操作添加权限注解。

### 1.3 [安全] LoginResponse 的 token 被 @JsonIgnore

- **文件**: `LoginResponse.java:10`
- **问题**: `@JsonIgnore private String token` 导致序列化时 token 不输出。如果未通过 `Set-Cookie` 传递，客户端拿不到 JWT，登录功能形同虚设。
- **修复**: 移除 `@JsonIgnore`，或确认 token 通过 HttpOnly Cookie 传递。

### 1.4 [数据] schema.sql 仅定义 8/30+ 张表

- **文件**: `schema.sql`
- **问题**: 实际有 30+ 张表，其余分散在 `v2_schema.sql`、`v3_*.sql`、`v5_stage1.sql`、`SemesterSchemaInitializer.java` 中。`schema.sql` 不能作为唯一真实来源，维护时极易引入偏差。
- **修复**: 合并所有迁移脚本为统一 schema，或引入 Flyway/Liquibase 正式管理。

### 1.5 [数据] 多个迁移脚本使用 DELIMITER 语法，Spring sql.init 不支持

- **文件**: `v2_alter_schedule.sql`、`v6_bugfix_constraints.sql` 等
- **问题**: 这些脚本使用存储过程语法（`DELIMITER //`），Spring `sql.init` 模式下执行失败。`source_type`、`batch_id` 等列可能永远无法添加。
- **修复**: 改用纯 DDL 语句，或引入 Flyway 管理迁移。

### 1.6 [数据] 6 个 Entity 字段在 schema.sql 中无对应列

- **文件**: `Schedule.java:21,40,42,44`、`TeachingTask.java:21`、`ScheduleScoreReport.java:41`
- **问题**: `semesterId`、`sourceType`、`batchId`、`planId` 等字段依赖迁移脚本添加，如果跳过迁移直接用 schema.sql 初始化，运行时必崩。
- **修复**: 将所有 ALTER TABLE 合并到主 schema.sql。

### 1.7 [逻辑] 自动排课事务隔离级别导致日排课上限检查失效

- **文件**: `AutoScheduleService.java:370-379`
- **问题**: `checkTeacherDailyLimit` 和 `checkClassDailyLimit` 在同一 `@Transactional` 内先插入再 selectCount。MySQL REPEATABLE READ 隔离级别下，事务内看不到本轮刚插入的记录，导致教师/班级每日上限可能被突破。
- **修复**: 将排课主循环拆分为每条 schedule 独立事务，或使用编程式事务在每轮插入后 flush。

### 1.8 [逻辑] AutoScheduleService.clearAll() 跨学期误删

- **文件**: `AutoScheduleService.java:62`
- **问题**: `unscheduledTaskService.clearAll()` 无学期过滤，删除所有未排课记录，导致其他学期数据被连带清除。
- **修复**: 添加 `semesterId` 或 `batchId` 过滤条件。

### 1.9 [逻辑] SchedulePlanService.adjustPlanItem NPE

- **文件**: `SchedulePlanService.java:180`
- **问题**: `request.getAdjustReason()` 可能为 null，直接调用 `.trim()` 抛 NullPointerException。
- **修复**: 添加 null 检查：`request.getAdjustReason() != null ? request.getAdjustReason().trim() : null`。

---

## 二、P1 高优先级问题（19 项）

### 2.1 [安全] JwtService 未校验密钥最小长度

- **文件**: `JwtService.java:25-28`
- **问题**: 注释要求 ">= 32 字节"，但只检查是否为空/空白/默认值，不检查实际长度。弱密钥降低 HMAC 安全性。
- **修复**: 添加 `secret.length() >= 32` 校验。

### 2.2 [安全] 明文管理员密码输出到 stdout

- **文件**: `AdminUserInitializer.java:71-76`
- **问题**: `System.out.println("随机初始密码：" + password)` 在容器化部署中会被日志收集系统永久保存。
- **修复**: 写入文件而非 stdout，或要求环境变量必须配置。

### 2.3 [安全] 12 个端点 @RequestBody 缺少 @Valid

- **文件**: `ScheduleGenerateController.java`、`ScheduleLockController.java`、`ScheduleRuleWeightController.java`、`ScheduleReplanController.java`、`V5SimulationController.java`、`V5CandidatePositionController.java`、`ScheduleReportController.java`、`ScheduleAiAnalysisController.java`、`V5RepairSuggestionController.java` 等
- **问题**: DTO 上的 `@NotNull`/`@NotBlank` 校验注解不会生效，恶意请求可直接穿透到 Service 层。
- **修复**: 在所有 `@RequestBody` 参数添加 `@Valid`。

### 2.4 [安全] 裸实体直接作为 @RequestBody

- **文件**: `TeacherUnavailableTimeController.java`（POST/PUT）、`ScheduleRuleWeightController.java`（PUT batch）
- **问题**: MyBatis Plus 实体类直接暴露为请求体，攻击者可覆盖 `id`、`deleted`、`createTime` 等字段。
- **修复**: 使用专用 Form DTO 隔离实体。

### 2.5 [安全] RateLimiterService 内存限流器无容量上限

- **文件**: `RateLimiterService.java:17`
- **问题**: `ConcurrentHashMap<String, Deque<Long>>` 的 key 由用户名/IP 组成，攻击者可轮换 key 生成大量条目导致 OOM。
- **修复**: 添加全局容量上限或使用 Redis 替代内存存储。

### 2.6 [性能] 排课循环内 N+1 查询（4 处）

- **文件**: `AutoScheduleService.java:397-401`（getTimeSlotIdsByDay）、`V3ScheduleGenerateService.java:568-575`（getCourseType/getClassStudentCount）、`UnscheduledTaskService.java:130-143`（fillRelationFields）、`ScheduleStatisticsService.java:77-78`（teacherWorkload）
- **问题**: 循环内逐条 selectById，产生大量冗余 SQL。
- **修复**: 改为批量查询 + Map 缓存。

### 2.7 [性能] TeacherUnavailableTimeService 缺失事务边界

- **文件**: `TeacherUnavailableTimeService.java:103-139`
- **问题**: `create`/`update`/`updateStatus` 方法执行校验查询 + 写操作，无 `@Transactional`，并发场景下唯一性校验存在竞态条件。
- **修复**: 添加 `@Transactional`。

### 2.8 [性能] ScheduleRuleService.updateRules 无事务保护

- **文件**: `ScheduleRuleService.java:29-52`
- **问题**: 循环内逐条更新规则配置，部分成功部分失败时数据不一致。
- **修复**: 添加 `@Transactional`。

### 2.9 [性能] AutoScheduleBatchService.updateBatchResult 无事务

- **文件**: `AutoScheduleBatchService.java:54-65`
- **问题**: 先 selectById 再 updateById，无事务保护，可能出现脏写。
- **修复**: 添加 `@Transactional`。

### 2.10 [逻辑] 前端 auth store token 残留状态

- **文件**: `frontend/src/stores/auth.ts:14-29`
- **问题**: `login()` 成功后立即调用 `clearToken()` 将 token 置空。如果任何代码依赖 `token.value` 做判断，将永远得到空值。
- **修复**: 移除 token ref 或修正 clearToken 调用位置。

### 2.11 [逻辑] 响应拦截器对 blob 下载请求误处理

- **文件**: `frontend/src/utils/request.ts:69-91`
- **问题**: `responseType: 'blob'` 的请求如果后端返回 JSON 错误响应，拦截器将 Blob 当作 ApiResponse 处理，`payload.code` 为 undefined，错误响应被静默放过。
- **修复**: 对 blob 请求做特殊处理，检查 response.data 是否为 Blob。

### 2.12 [逻辑] ScheduleScoreController.rescore 缺少 null 检查

- **文件**: `ScheduleScoreController.java:43-45`
- **问题**: `planService.getById(planId)` 可能返回 null，直接传给 `scoreService.rescore(plan)` 会 NPE。
- **修复**: 添加 null 检查。

### 2.13 [逻辑] ScheduleController.create 中 task 未做 null 检查

- **文件**: `ScheduleController.java:约65`
- **问题**: `teachingTaskMapper.selectById()` 返回 null 时直接调用 `task.getSemesterId()` 会 NPE。
- **修复**: 添加 null 检查。

### 2.14 [逻辑] 错误处理不一致（3 种模式混用）

- **文件**: 多个 Controller
- **问题**: 存在 `throw BusinessException`、`return Result.fail()`、`catch + 静默返回空` 三种模式，增加前端适配成本。`ScheduleAdjustLogController` 和 `ScheduleRuleWeightController` 吞掉异常返回空数据。
- **修复**: 统一使用 `throw BusinessException` + 全局异常处理器。

### 2.15 [前端] 多个页面 fetchData 缺少 catch 块

- **文件**: `ScheduleRuleWeightView.vue`、`ScheduleConflictReportView.vue`、`ScheduleView.vue`、`SchedulePlanView.vue`
- **问题**: API 失败时用户看不到任何错误提示。
- **修复**: 添加 catch 块并显示 ElMessage.error。

### 2.16 [前端] handleSubmit 未 await fetchData

- **文件**: `frontend/src/composables/useCrudForm.ts:121`
- **问题**: 提交成功后立即刷新列表但不等待完成，快速连续提交可能产生竞态条件。
- **修复**: 改为 `await fetchData()`。

### 2.17 [前端] 全局超时 10 秒对长操作不合理

- **文件**: `frontend/src/utils/request.ts:24`
- **问题**: `timeout: 10000` 对自动排课、报告生成等长时间操作太短。
- **修复**: 增大默认 timeout 或为长操作统一设置更长 timeout。

### 2.18 [前端] 路由守卫 fetchCurrentUser 竞态条件

- **文件**: `frontend/src/router/index.ts:211-229`
- **问题**: 每次路由切换到需认证页面时，如果 userInfo 为 null 都发 `/auth/me` 请求。快速连续导航会发出多个并发请求。
- **修复**: 添加 fetching 状态标记避免重复请求。

### 2.19 [前端] useCrudForm handleDelete 重复显示错误消息

- **文件**: `frontend/src/composables/useCrudForm.ts:138-139`
- **问题**: 拦截器已在 error 路径调用 `ElMessage.error()`，handleDelete 的 catch 又调用一次，用户看到两次错误弹窗。
- **修复**: 统一错误处理策略，catch 块中不再重复显示。

---

## 三、P2 中等问题（28 项）

### 3.1-3.6 安全与配置

| # | 文件 | 问题 |
|---|------|------|
| 3.1 | `CorsConfig.java:32` | `allowedHeaders("*")` 过宽，应限制为实际使用的头 |
| 3.2 | `application.yml:47` | `cookie-secure` 默认 false，生产环境 JWT cookie 可被中间人截获 |
| 3.3 | `LoginRequest.java` | 无 `@Size` 输入长度限制，可提交超长字符串 |
| 3.4 | `SecurityConfig.java` | 未配置 Spring Security FilterChain，缺少标准安全响应头 |
| 3.5 | `GlobalExceptionHandler.java:27` | BusinessException 的 code 既表示业务错误码又表示 HTTP 状态码，职责混淆 |
| 3.6 | `Result.java:12` | `Integer code` 允许 null，序列化出 `"code": null` |

### 3.7-3.10 硬编码魔法数字

| # | 文件 | 问题 |
|---|------|------|
| 3.7 | `V4ScheduleRiskService.java:49-54` | 风险阈值硬编码（18/22/8/10），应从规则表读取 |
| 3.8 | `V4ScheduleChartService.java:43` | `TOTAL_AVAILABLE_PERIODS = 20` 硬编码，假设 5 天 x 4 大节 |
| 3.9 | `ScheduleScoreService.java:315` | `>= 5` 作为下午分界线硬编码 |
| 3.10 | `V4ScheduleAnalysisService.java:90-98` | 过载阈值硬编码 |

### 3.11-3.14 空指针风险

| # | 文件 | 问题 |
|---|------|------|
| 3.11 | `ScheduleConflictReportService.java:382/413` | `getDayOfWeek` 可能返回 null，导致分组 key 异常 |
| 3.12 | `V4ScheduleAdjustmentService.java:363` | `context.currentSlot.getPeriodNo()` 可能 null |
| 3.13 | `SchedulePlanService.java:570` | slotMap 查询可能返回 null，slot.getId() NPE |
| 3.14 | `CourseType/RoomType/ScheduleSourceType` | `fromCode` 方法对未知 code 返回 null，调用方不检查会 NPE |

### 3.15-3.18 数据库与 Entity

| # | 文件 | 问题 |
|---|------|------|
| 3.15 | 多个 Entity | 时间戳命名不一致：V1 表用 `create_time/update_time`，V3+ 表用 `created_at/updated_at` |
| 3.16 | 14 张表 | 无软删除支持，`deleteById()` 物理删除，ScheduleLockedItem 风险最高 |
| 3.17 | `AutoScheduleBatch.java` | `createTime` 无 `@TableField` 注解，且缺少 `updateTime` 字段 |
| 3.18 | `teaching_task` 表 | `course_id`、`teacher_id`、`class_id` 外键无索引，JOIN 查询全表扫描 |

### 3.19-3.22 代码重复

| # | 文件 | 问题 |
|---|------|------|
| 3.19 | `SchedulePlanService.java:267-446` | `applyPlan` 与 `applyPlanInternal` 高度重复 |
| 3.20 | `AutoScheduleService` vs `V3ScheduleGenerateService` | 排课核心逻辑重复，应抽取为共享引擎 |
| 3.21 | `ScheduleScoreReportService` vs `ScheduleScoreService` | 两套评分体系并行，可能产生不一致结果 |
| 3.22 | 5 个 Vue 文件 | `fallback<T>()` 辅助函数重复定义，应提取到 utils |

### 3.23-3.28 前端

| # | 文件 | 问题 |
|---|------|------|
| 3.23 | `BaseLayout.vue` | `router.currentRoute.value.path` 不是响应式的，菜单高亮不自动更新 |
| 3.24 | `useCrudForm.ts:113` | `if (editingId.value)` 对 id=0 误判为 false |
| 3.25 | `request.ts:5-9` vs `api/types.ts` | ApiResponse 接口重复定义 |
| 3.26 | `ScheduleRuleView.vue:11,31` | `rules` 变量名冲突，第一个 ref 被第二个声明覆盖 |
| 3.27 | `api/index.ts` | barrel 文件缺少多个 v4/v5 API 模块的 re-export |
| 3.28 | `errors.ts:8-10` | `isCancel` 函数未被任何代码引用，属于死代码 |

---

## 四、P3 低优先级问题（8 项）

| # | 文件 | 问题 |
|---|------|------|
| 4.1 | `SemesterSchemaInitializer.java` | DDL 异常全部 catch + warn，仅输出 getMessage() 丢失堆栈 |
| 4.2 | `V5RepairExplanationService.java:444-447` | `__keepObjects()` 纯死代码 |
| 4.3 | 所有 Vue 文件 | 全局缺少无障碍（Accessibility）属性 |
| 4.4 | `ScheduleView.vue` 等 | 大量内联样式，应提取为 scoped CSS |
| 4.5 | `EChartPanel.vue` | ResizeObserver 回调无 debounce |
| 4.6 | `ScheduleAiAnalysis.vue` | 使用已废弃的 `document.execCommand('copy')` |
| 4.7 | `SimulationPlanDetailView.vue` | 超过 700 行，应拆分为子组件 |
| 4.8 | `App.vue` | 缺少 Vue 错误边界，未捕获异常导致白屏 |

---

## 五、修复优先级建议

### 立即修复（影响安全和数据完整性）

1. `WebMvcConfig.java` — 将 `/v3/**` 纳入认证拦截器
2. `LoginResponse.java` — 确认 token 传递机制，移除 @JsonIgnore 或确认 Cookie 传递
3. `JwtService.java` — 添加密钥长度 >= 32 校验
4. `AutoScheduleService.java:370-379` — 修复事务隔离级别导致的上限检查失效
5. `AutoScheduleService.java:62` — clearAll() 添加学期过滤
6. `SchedulePlanService.java:180` — adjustReason null 检查
7. 合并 schema.sql + 所有迁移脚本，或引入 Flyway

### 本迭代修复（影响功能正确性）

8. 12 个 Controller 添加 @Valid
9. ScheduleScoreController.rescore 添加 null 检查
10. 排课循环内 N+1 查询改为批量查询
11. TeacherUnavailableTimeService 添加 @Transactional
12. 前端 auth store 移除残留 token 逻辑
13. 前端响应拦截器修复 blob 下载误处理
14. 统一错误处理模式

### 后续优化（影响可维护性）

15. 添加细粒度权限控制
16. 统一时间戳命名和 @TableField 风格
17. 提取公共排课引擎，消除代码重复
18. 硬编码阈值改为可配置
19. 添加 teaching_task 外键索引
20. 前端组件拆分和错误边界
