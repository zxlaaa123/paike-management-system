# V4 浏览器自动化测试进度

更新时间：2026-05-19  
记录目的：方便压缩上下文后恢复 `V4` 浏览器自动化测试任务。

---

## 1. 本次测试环境

- 前端地址：`http://localhost:5173`
- 后端服务：已启动，可通过前端代理访问
- 浏览器自动化方式：`chrome-devtools` MCP
- 登录账号：`admin`
- 登录密码：`123456`

---

## 2. 当前已确认的提交状态

`main` 上已经包含：

- `57d5bfd` `feat(v4): add phase 0 analysis entry scaffolding`
- `ce03c58` `feat(v4): add phase 1 quality analysis summary`
- `9868090` `feat(v4): add phase 2 score detail explanation`
- `d3c7f28` `feat(v4): add phase 3 risk diagnosis center`
- `5ff7f9d` `feat(v4): add phase 4 chart analysis`
- `25381bf` `feat(v4): add phase 5 schedule source visibility`

---

## 4. 2026-05-19 批次A（P0冒烟）执行记录

执行入口：`http://127.0.0.1:5173/login`  
账号：`admin / 123456`

### 4.1 环境与连通性

- 前端端口：`5173` 监听正常
- 后端端口：`8090` 监听正常（前端代理目标）
- 登录接口：`POST /api/auth/login` 返回 `200`
- 页面登录：成功进入 `/dashboard`，显示 `当前登录：管理员`

### 4.2 主流程A快跑（已完成项）

- 进入 `V4 分析总览`：`/v4/schedule-analysis`
- 进入方案 `planId=36` 分析详情：`/v4/schedule-analysis/36`
- 关键接口成功：
  - `GET /api/v3/schedule-plans/36` -> `200`
  - `GET /api/v4/schedule-analysis/plans/36/summary` -> `200`
- 详情页可见：总分、已排任务、未排任务、冲突数、建议动作、快捷入口

### 4.3 V4增量能力快跑

- 报告导出页：`/v4/schedule-analysis/36/reports`
- 生成 HTML 报告：`POST /api/v4/schedule-reports/plans/36/generate` -> `200`
- 生成 EXCEL 报告：`POST /api/v4/schedule-reports/plans/36/generate` -> `200`
- 页面提示：`报告生成成功`，列表新增导出记录

- AI 建议页：`/v4/schedule-analysis/36/ai`
- 生成 AI 分析：`POST /api/v4/ai/schedule-analysis/plans/36` -> `200`
- 页面展示：总体分析文本 + 建议列表，提示 `AI 分析已生成`

### 4.4 主流程C快跑（已完成项）

- 课程锁定页：`/v4/schedule-analysis/36/locks`
- 关键接口成功：
  - `GET /api/v3/schedule-plans/36/items` -> `200`
  - `GET /api/v4/schedule-locks/plans/36` -> `200`
- 页面可见：已锁定课程统计、未锁定课程列表、`锁定课程` 操作入口、`局部重排生成新方案` 按钮

### 4.5 待补测项（批次A剩余）

- 主流程A中的“自动排课 -> 应用正式课表”端到端动作本轮未执行（本轮使用既有方案做冒烟）
- 主流程C中的“冲突校验触发验证 + 局部重排执行结果验证”本轮未执行

---

## 3. 本轮浏览器自动化已完成项

### 3.1 登录与基础可访问性

已完成：

- 打开 `http://localhost:5173`
- 成功进入登录页
- 使用 `admin / 123456` 登录成功
- 成功进入 `/dashboard`
- 顶部显示 `当前登录：管理员`
- 当前学期显示正常

### 3.2 V4 阶段 0：分析总览页

已完成：

- 直接访问 `/v4/schedule-analysis`
- 页面正常加载
- 页面显示：
  - `V4 阶段 0`
  - `排课质量分析入口已就绪`
  - 当前学期信息
  - 方案数量、已应用方案、含未排任务方案、含冲突方案
- 页面展示了多条方案卡片

备注：

- 页面数据来自当前学期方案列表，加载正常
- 本次测试中发现页面展示文案仍保留“阶段 0”风格说明，但页面功能本身已经能承接后续阶段入口

### 3.3 选用的测试方案

本轮后续页面测试使用的 `planId`：

- `36`

对应方案：

- 名称：`方案A-教师优先-096550`
- 状态：`DRAFT`
- 学期 ID：`2`

该 `planId` 不是靠按钮点击拿到的，而是从页面网络请求中确认：

- `/api/v3/schedule-plans/36`
- `/api/v4/schedule-analysis/plans/36/summary`

### 3.4 V4 阶段 1：质量分析详情页

已完成：

- 直接访问 `/v4/schedule-analysis/36`
- 页面正常加载
- 已看到以下关键内容：
  - 方案名称与策略
  - `刷新分析`
  - `核心指标卡片`
  - `质量结论`
  - `建议动作`
  - `快捷入口`

已确认数据示例：

- 总分：`93.38`
- 已排任务：`233`
- 未排任务：`2`
- 冲突数量：`0`
- 教师数量：`74`
- 班级数量：`74`
- 教室数量：`76`

### 3.5 V4 阶段 3：风险诊断页

已完成：

- 直接访问 `/v4/schedule-analysis/36/risks`
- 页面正常加载
- 已看到以下关键内容：
  - `V4 冲突风险诊断`
  - `刷新风险`
  - 风险统计卡片
  - 筛选区
  - 风险列表表格

已确认数据示例：

- 高风险数量：`1`
- 中风险数量：`1`
- 低风险数量：`73`
- 未处理风险：`75`

已验证交互：

- 点击第一条 `查看详情`
- 风险详情抽屉成功打开
- 抽屉中显示：
  - 风险标题
  - 风险类型
  - 处理建议
  - 详细说明

### 3.6 V4 阶段 2：评分详情解释页

已完成：

- 直接访问 `/v4/schedule-analysis/36/score`
- 页面正常加载
- 已看到以下关键内容：
  - `V4 评分详情解释`
  - `评分计算来源`
  - `评分维度总览`
  - `怎么看这张表`
  - `后续阶段入口`

已确认页面中有评分数据，例如：

- `硬性冲突评分`
- `教师课时均衡评分`
- `教室利用率评分`
- `时间分布评分`

已确认存在维度编码、当前得分、满分、权重、说明、计算结果说明。

### 3.7 V4 阶段 4：图表分析页

已完成基础验证：

- 直接访问 `/v4/schedule-analysis/36/charts`
- 页面正常加载
- 已看到以下关键内容：
  - `V4 可视化分析`
  - `刷新图表`
  - `教师课时`
  - `教室利用`
  - `班级负载`
  - `时间密度`
  - `评分雷达`

已确认：

- 默认 `教师课时` Tab 正常显示
- 图表区域已渲染，不是空白页

---

## 4. 本轮补充完成项

### 4.1 V4 阶段 4 图表页深度验证

已完成：

- 在 `/v4/schedule-analysis/36/charts` 依次点击：
  - `教室利用`
  - `班级负载`
  - `时间密度`
  - `评分雷达`
- 已确认 Tab 切换正常
- 已确认 5 个图表接口均返回 `200`

本次从网络请求确认：

- `/api/v4/schedule-charts/plans/36/teacher-hours`
- `/api/v4/schedule-charts/plans/36/room-utilization`
- `/api/v4/schedule-charts/plans/36/class-daily-load`
- `/api/v4/schedule-charts/plans/36/time-density`
- `/api/v4/schedule-charts/plans/36/score-radar`

说明：

- `chrome-devtools` 的 a11y 快照里没有直接暴露图表内部数据文本
- 但页面已正常切换标签，且 5 个后端图表接口全部命中并成功返回
- 页面内存在图表抽屉容器，但这轮没有稳定拿到图表点击后可读文本，因此“图表点选后的明细内容”仍建议后续人工补看一眼

### 4.2 V4 阶段 5 正式课表来源增强

已完成：

- 打开 `/schedule`
- 顶部来源卡片正常渲染
- 已命中来源接口：`/api/v4/schedules/current/source?termId=2`
- 当前环境命中的是“无来源方案”场景

已确认页面表现：

- 显示 `当前正式课表来源`
- 显示 `来源方案：暂无来源方案`
- 显示 `是否人工调整：未人工调整`
- 显示 `人工调整次数：0`
- 显示说明文案：`旧数据或手动录入课表可能没有来源方案。`
- `查看来源方案`
- `查看质量分析`
- `查看风险诊断`

以上 3 个按钮在当前场景下均为禁用状态，符合预期。

### 4.3 页面内按钮点击链路

本轮补测结果：

- 分析总览页首条方案卡片 `进入 V4 分析`：可跳转到 `/v4/schedule-analysis/36`
- 质量分析页 `评分详情解释`：可跳转到 `/v4/schedule-analysis/36/score`
- 评分页 `风险诊断中心`：可跳转到 `/v4/schedule-analysis/36/risks`
- 风险页 `查看图表分析`：可跳转到 `/v4/schedule-analysis/36/charts`
- 图表页 `回到质量分析`：可跳转回 `/v4/schedule-analysis/36`

---

## 5. 当前未完成项

以下内容还没来得及继续测：

### 5.1 图表点选后的明细内容

尚未稳定确认：

- 图表内部点选后是否总能打开明细抽屉
- 抽屉中的图表详情文案是否完整可读

说明：

- Tab 切换和后端接口返回都已确认正常
- 这部分更像是“图表内部交互细节补验”，不影响阶段 4 主链路通过判断

### 5.2 有来源方案场景

当前环境还未覆盖：

- `/schedule` 页面出现真实 `sourcePlanId`
- `查看来源方案`
- `查看质量分析`
- `查看风险诊断`

说明：

- 当前正式课表数据主要是手动排课/旧数据，所以命中了“暂无来源方案”分支
- 若后续有通过方案应用生成的正式课表，建议再补测一次“有来源方案”场景

---

## 6. 建议的恢复步骤

压缩上下文后恢复任务时，建议按下面顺序继续：

1. 打开 `http://localhost:5173`
2. 如未登录，用 `admin / 123456` 登录
3. 若要补图表细节，直接访问 `http://localhost:5173/v4/schedule-analysis/36/charts`
4. 尝试人工补看图表点选后的抽屉内容
5. 若要补来源方案正向链路，准备一份真正“由方案应用而来”的正式课表数据
6. 再访问 `http://localhost:5173/schedule`
7. 重点验证“有来源方案”场景下的三个跳转按钮

---

## 7. 恢复时可直接复用的关键 URL

- 登录页：`http://localhost:5173/login`
- 仪表盘：`http://localhost:5173/dashboard`
- V4 分析总览：`http://localhost:5173/v4/schedule-analysis`
- V4 质量分析详情：`http://localhost:5173/v4/schedule-analysis/36`
- V4 评分详情：`http://localhost:5173/v4/schedule-analysis/36/score`
- V4 风险诊断：`http://localhost:5173/v4/schedule-analysis/36/risks`
- V4 图表分析：`http://localhost:5173/v4/schedule-analysis/36/charts`
- 正式课表页：`http://localhost:5173/schedule`

---

## 8. 阶段 6 补充进度

### 8.1 已完成开发落地

已新增：

- `POST /api/v4/schedule-adjustments/check`
- `POST /api/v4/schedule-adjustments/apply`
- 通用前端调整弹窗 `ScheduleAdjustDialog.vue`

当前已接入入口：

- `V3` 方案详情页
- 正式课表页 `/schedule`
- `V4` 风险诊断页

### 8.2 已完成的轻量浏览器验证

已确认：

- `/schedule` 页面能正常加载
- 正式课表列表中已经出现 `调整` 按钮
- 点击后可打开 `调整正式课表` 弹窗
- 弹窗中可见：
  - 当前课程/教师/班级/教室/时间
  - `检测冲突`
  - `保存调整`

说明：

- 本轮按“轻量冒烟”方式测试，没有实际保存课表改动
- 风险页本轮能正常打开，但当前测试数据未稳定命中可直接调整的风险项，因此没有把“风险页发起调整并成功保存”作为已通过项

### 8.3 阶段 6 当前结论

- `阶段 6` 代码编译通过
- 正式课表页的局部调整弹窗已确认可打开
- 当前还未做“真实保存 + 冲突确认 + 日志回看”的完整端到端回归
- 如果后续继续测，建议优先补：
  - `V3` 方案详情页发起调整
  - 点击 `检测冲突`
  - 构造一次明确冲突并验证二次确认
  - 保存后回看调整日志

---

## 9. 当前结论

截至本记录生成时：

- `V4 阶段 0-5` 已完成主链路浏览器回归
- `阶段 0` 总览页入口正常
- `阶段 1` 质量分析详情正常
- `阶段 2` 评分详情解释正常
- `阶段 3` 风险诊断页及风险详情抽屉正常
- `阶段 4` 图表页 5 个标签切换正常，5 个图表接口均返回 `200`
- `阶段 5` 正式课表页的“暂无来源方案”场景正常
- 页面内主跳转链路本轮已验证可用
- `阶段 6` 已完成开发，且 `/schedule` 页调整弹窗已完成轻量冒烟验证

剩余未闭环点主要有两项：

- 图表点选后的明细内容仍建议人工补看
- 正式课表“有来源方案”场景仍需等待合适测试数据再补测

---

## 10. 阶段 7 开发补充进度

### 10.1 已完成开发落地

已新增：

- `POST /api/v4/schedule-locks/lock`
- `POST /api/v4/schedule-locks/unlock`
- `GET /api/v4/schedule-locks/plans/{planId}`
- `frontend/src/views/v4/ScheduleLockManage.vue`

当前页面入口已接入：

- `V3` 方案详情页顶部按钮 `课程锁定管理`
- `V4` 质量分析详情页快捷入口 `课程锁定管理`

### 10.2 当前实现范围

已支持：

- 为方案明细创建锁定记录
- 查询某个方案当前生效的锁定列表
- 在锁定管理页查看：
  - 方案信息
  - 已锁定课程数量
  - 已锁定课程列表
  - 未锁定课程列表
  - 锁定原因
- 在锁定管理页执行：
  - 锁定课程
  - 取消锁定

说明：

- 锁定信息保存在后端 `schedule_locked_item` 表中，不依赖前端本地状态
- 当前阶段 7 仍然只做“锁定管理”，还没有进入阶段 8 的局部重排逻辑
- 锁定不会修改正式课表，也不会应用方案

### 10.3 已完成的代码级验证

已确认：

- `D:\paike\backend` 下 `mvn -q -DskipTests compile` 通过
- `D:\paike\frontend` 下 `npm run build` 通过

### 10.4 还未补的联调项

本轮已完成浏览器自动化补测：

1. 打开 `/v4/schedule-analysis/36/locks`
2. 确认锁定管理页正常加载
3. 选中第一条未锁定课程执行锁定
4. 验证 `POST /api/v4/schedule-locks/lock` 返回 `200`
5. 验证锁定课程出现在“已锁定课程列表”
6. 执行取消锁定
7. 验证“已锁定课程”恢复为 `0`
8. 验证页面重新显示“当前还没有锁定课程”

本轮还发现并修复了一个真实后端问题：

- 症状：锁定成功后，`GET /api/v4/schedule-locks/plans/36` 在“已有锁定记录”场景下返回 `500`
- 原因：`scheduleId` 为空的方案锁定记录在空 `scheduleMap` 上触发了 `get(null)` 异常
- 处理：已修复 [D:\paike\backend\src\main\java\com\paike\scheduler\service\V4ScheduleLockService.java](D:/paike/backend/src/main/java/com/paike/scheduler/service/V4ScheduleLockService.java)
- 结果：重启后端后，锁定列表和取消锁定链路均恢复正常

说明：

- 本轮浏览器测试产生的那条测试锁定记录已经取消，不存在残留测试数据

---

## 11. 阶段 8 浏览器自动化进度

### 11.1 已完成开发落地

已新增：

- `POST /api/v4/schedule-replan/plans/{planId}`
- `frontend/src/components/v4/LocalReplanDialog.vue`
- `frontend/src/api/v4ScheduleReplanApi.ts`

当前入口已接入：

- `V3` 方案详情页顶部按钮 `局部重排生成新方案`
- `V4` 质量分析详情页快捷入口 `局部重排`
- `V4` 课程锁定管理页顶部按钮 `局部重排生成新方案`
- `V4` 风险诊断页顶部按钮 `局部重排`

### 11.2 当前实现范围

本阶段严格按 `docs/v4` 的阶段 8 最小可用版实现：

- 基于旧方案生成一个独立新方案
- 复制原方案 `schedule_plan_item`
- 保留并同步当前锁定课程记录
- 复制未排任务说明
- 重新计算冲突数量和评分
- 跳转到新方案详情页

说明：

- 当前不会覆盖原方案
- 当前不会删除原方案
- 当前不会直接修改正式课表
- 新方案是否应用，仍然走 `V3 apply`
- 当前版本属于文档允许的“最小可用版”，未实现真正的未锁定课程重新求解

### 11.3 已完成的代码级验证

已确认：

- `D:\paike\backend` 下 `mvn -q -DskipTests compile` 通过
- `D:\paike\frontend` 下 `npm run build` 通过

### 11.4 已完成浏览器自动化验证

本轮已完成浏览器联调：

1. 打开 `/v4/schedule-analysis/36/locks`
2. 验证锁定管理页顶部出现 `局部重排生成新方案`
3. 点击按钮打开 `LocalReplanDialog`
4. 验证弹窗正确显示：
   - 来源方案信息
   - 已锁定课程数量
   - 可重排课程数量
   - 已排课程数量
   - 未排任务数量
   - 新方案名称输入框
   - `LOCAL_REPLAN` 策略
   - “不会覆盖原方案，也不会直接修改正式课表”的提示
5. 填写测试方案名并提交生成
6. 验证成功跳转到 `V3` 方案详情页
7. 验证新方案信息正确：
   - 方案名为测试输入值
   - 状态为 `草稿`
   - 方案说明中包含 `来源方案 ID=36`
   - 总分、已排、未排、冲突字段均正常展示

### 11.5 测试数据清理

本轮浏览器测试一度生成测试方案：

- `planId=40`
- 名称：`V4阶段8浏览器回归测试-待清理`

随后已完成清理：

- 使用携带 `X-CSRF-Token` 的删除请求删除该草稿方案
- 删除接口返回 `200`
- 删除后刷新详情页，原 `planId=40` 已无法再正常展示方案内容

说明：

- 阶段 8 浏览器测试没有留下残留测试方案数据

---

## 12. 阶段 9 浏览器自动化进度（排课报告导出）

### 12.1 已完成开发落地

已新增：

- `POST /api/v4/schedule-reports/plans/{planId}/generate`
- `GET /api/v4/schedule-reports/plans/{planId}`
- `GET /api/v4/schedule-reports/{reportId}/download`
- `frontend/src/views/v4/ScheduleReports.vue`
- `frontend/src/api/v4ScheduleReportApi.ts`

当前入口已接入：

- `V4` 质量分析详情页快捷入口 `排课报告导出`
- 路由：`/v4/schedule-analysis/:planId/reports`

### 12.2 已完成浏览器自动化验证

本轮联调时间：`2026-05-19`  
测试方案：`planId=36`（方案A-教师优先-096550）

已完成主链路验证：

1. 从 `/v4/schedule-analysis/36` 点击 `排课报告导出` 成功进入 `/v4/schedule-analysis/36/reports`
2. 页面正确显示：
   - 报告类型选择
   - 导出格式（HTML / EXCEL）
   - 包含图表/风险/建议开关
   - 报告列表区
3. 使用默认参数生成 `ANALYSIS + HTML` 报告成功
4. 报告列表新增记录，`downloadUrl` 正常显示
5. 点击下载成功
6. 切换 `EXCEL` 后再次生成成功
7. 下载 `EXCEL` 报告成功
8. 切换报告类型为 `冲突风险报告（RISK）` 后生成成功

### 12.3 本轮已确认接口命中

本轮网络请求确认以下接口均返回 `200`：

- `GET /api/v4/schedule-reports/plans/36`
- `POST /api/v4/schedule-reports/plans/36/generate`（多次）
- `GET /api/v4/schedule-reports/1/download`
- `GET /api/v4/schedule-reports/2/download`
- `GET /api/v4/schedule-reports/3/download`

### 12.4 导出文件落盘检查

已确认导出文件写入后端目录（未写入前端目录）：

- `D:\paike\backend\data\reports\schedule-report-plan-36-analysis-20260519070419.html`
- `D:\paike\backend\data\reports\schedule-report-plan-36-analysis-20260519070501.xlsx`
- `D:\paike\backend\data\reports\schedule-report-plan-36-risk-20260519070600.xlsx`

结论：

- 阶段 9 主链路（生成、列表、下载）浏览器联调通过
- 本轮未发现需要额外修复的问题

---

## 13. 阶段 10 浏览器自动化进度（AI 分析建议）

### 13.1 已完成开发落地

已新增：

- `POST /api/v4/ai/schedule-analysis/plans/{planId}`
- `frontend/src/api/v4ScheduleAiApi.ts`
- `frontend/src/views/v4/ScheduleAiAnalysis.vue`

当前入口已接入：

- `V4` 质量分析详情页快捷入口 `AI 分析建议`
- 路由：`/v4/schedule-analysis/:planId/ai`

### 13.2 已完成代码级验证

已确认：

- `D:\paike\backend` 下 `mvn -q -DskipTests compile` 通过
- `D:\paike\frontend` 下 `npm run build` 通过

### 13.3 已完成浏览器自动化验证

本轮联调时间：`2026-05-19`  
测试方案：`planId=36`（方案A-教师优先-096550）

已完成主链路验证：

1. 打开 `/v4/schedule-analysis/36/ai`，页面正常加载
2. 页面已正确显示：
   - 方案信息区
   - 分析类型选择（总体分析等）
   - 包含风险 / 包含建议开关
   - `生成 AI 分析` 按钮
   - 分析结果区、建议列表区、复制按钮
   - 提示文案 `AI 建议仅供参考，不会自动修改课表。`
3. 使用默认参数（总体分析 + 包含风险 + 包含建议）生成成功
4. 已渲染分析文本与建议列表
5. 切换为 `风险分析` 且关闭“包含建议”后再次生成成功
6. 已正确显示“当前未返回建议”
7. 从 `/v4/schedule-analysis/36` 点击快捷入口 `AI 分析建议` 跳转成功

### 13.4 本轮已确认接口命中

本轮网络请求确认以下关键接口返回 `200`：

- `POST /api/v4/ai/schedule-analysis/plans/36`（多次）
- `GET /api/v3/schedule-plans/36`（页面方案信息加载）

### 13.5 当前结论

- 阶段 10 主链路（进入页面、生成分析、展示结果、开关控制）浏览器联调通过
- AI 建议页行为符合“只读建议，不自动修改课表”的阶段边界
- 本轮未发现需要额外修复的问题

### 2026-05-19 批次A继续回归（重启后）

- 自动排课复测：
  - 页面触发与脚本直调均可命中接口。
  - `POST /api/v3/schedule-generate` 仍返回 `500`（响应：`{"code":500,"message":"系统异常，请联系管理员","data":null}`）。
- 应用方案：
  - 先前已确认 `POST /api/v3/schedule-plans/36/apply` 返回 `200`。
- 局部调整链路复测：
  - 锁定课程成功：`POST /api/v4/schedule-locks/lock` 返回 `200`，锁定数从 0 -> 1。
  - 局部重排生成新方案失败：`POST /api/v4/schedule-replan/plans/36` 返回 `500`（同统一异常响应）。

结论（批次A当前）：
- 存在 P0 阻塞，自动排课与局部重排均出现后端 500，批次A暂不通过。
- 下一步需基于后端异常堆栈定位并修复 `schedule-generate` 与 `schedule-replan` 两个 500，再做最小回归。

### 2026-05-19 批次A继续回归（死锁降压补丁后）

- 补丁后复测：
  - `POST /api/v3/schedule-generate`：未立即报 500，30s 内未返回（前端超时中断）。
  - `POST /api/v4/schedule-replan/plans/36`：未立即报 500，30s 内未返回（前端超时中断）。
- 现象变化：从“快速 500”转为“长耗时/无响应”，说明死锁导致的快速失败已缓解，但主流程仍存在性能或锁等待瓶颈。

### 2026-05-19 批次A收口回归（重启后）

- 核心阻塞接口复测（已恢复）：
  - `POST /api/v3/schedule-generate` -> `200`（约 23.3s）
    - 返回示例：`planId=50, scheduledCount=237, unscheduledCount=2, conflictCount=0`
  - `POST /api/v4/schedule-replan/plans/36` -> `200`（约 1.8s）
    - 返回示例：`newPlanId=51, lockedCount=1, replanableCount=232, conflictCount=0`

批次A结论更新：
- 原先 P0 阻塞（generate/replan 500）已解除。
- 批次A（P0冒烟）通过，可进入批次B。

### 2026-05-19 批次B执行进度（第一轮）

执行方式：基于已登录会话的接口联调（含 CSRF），按 V1->V4 优先级验证“成功路径 + 失败/边界路径”。

#### B-1 V1 手动排课与冲突校验（P1-最高）

- 成功路径：`GET /api/schedules?page=1&size=2` -> `200`（总记录 `277`）
- 边界路径：`POST /api/schedules/check-conflict`（使用现有课表同一教师同一时段参数）-> `200`
  - 返回：`hasConflict=true`
  - 返回消息：`排课失败:李老师老师在周一 第1-2节已有课程`

结论：V1 冲突校验可用，失败路径提示清晰。

#### B-2 V2 规则与自动排课（P1-最高）

- 成功路径：`GET /api/schedule-rules` -> `200`（规则数 `5`）
- 成功路径：`PUT /api/schedule-rules`（回写现有规则值）-> `200`，`message=保存成功`
- 成功路径：`GET /api/auto-schedule/batches?page=1&size=5` -> `200`（批次数 `9`）
- 成功路径：`POST /api/v3/schedule-generate` -> `200`（新生成 `planId=52`）

备注：尝试用不存在规则键更新未触发失败（接口接受并返回保存成功），当前判定为“宽松更新策略”，非阻塞。

#### B-3 V3 方案生命周期（P1-最高）

- 成功路径：`GET /api/v3/schedule-plans?page=1&size=10` -> `200`（总数 `37`）
- 成功路径：`POST /api/v3/schedule-plans/compare`（`planIds=[52,51]`）-> `200`
  - 返回：`bestPlanId=52`
- 失败路径：`POST /api/v3/schedule-plans/compare`（仅1个 planId）-> `400`
  - 返回消息：`至少需要选择两个方案进行对比`

结论：V3 对比成功/失败路径均符合预期，失败提示清晰。

#### B-4 V4 分析与风险（P1-高）

- 成功路径：`GET /api/v4/schedule-analysis/plans/36/summary` -> `200`（`score=93.38`）
- 成功路径：`GET /api/v4/schedule-analysis/plans/36/score-details` -> `200`（评分项 `6`）
- 成功路径：`GET /api/v4/schedule-risks/plans/36` -> `200`
- 边界路径：`GET /api/v4/schedule-risks/plans/36?riskType=NOT_EXIST_LEVEL` -> `200`（空结果场景可返回）
- 失败路径：`POST /api/v4/schedule-replan/plans/999999` -> `400`
  - 返回消息：`来源排课方案不存在`

结论：V4 读接口稳定，错误 planId 失败提示清晰。

#### 批次B第一轮小结

- 当前未发现新增 P0/P1 阻塞缺陷。
- 已覆盖：V1 冲突校验、V2 自动排课、V3 方案对比、V4 分析/风险/重排失败路径。
- 待执行（批次B第二轮）：
  - V3 应用/回滚链路最小回归
  - V4 局部调整保存链路（成功+冲突失败）
  - V2/V4 导出链路复核（本批次口径重跑）

### 2026-05-19 批次B执行进度（第二轮）

#### B-5 V3 方案应用/回滚链路复核

- 方案扫描：`GET /api/v3/schedule-plans?page=1&size=20` -> `200`
  - 识别到：`draftId=52`，`appliedId=36`
- 成功路径：`POST /api/v3/schedule-plans/52/apply` -> `200`
  - 返回：`appliedCount=237`
  - 返回消息：`方案已应用为正式课表`
- 成功路径：`POST /api/v3/schedule-plans/52/rollback` -> `200`
  - 返回消息：`已回滚到该方案`
- 失败路径：`POST /api/v3/schedule-plans/999999/apply` -> `400`
  - 返回消息：`排课方案不存在`

结论：V3 应用/回滚主链路可用，失败路径提示清晰。

#### B-6 V4 局部调整复核（接口层）

- 准备：`GET /api/v3/schedule-plans/36/items` -> `200`（items=`233`）
- 边界失败路径（同时间+同教室）：
  - `POST /api/v4/schedule-adjustments/check` -> `400`
  - 返回消息：`调整后的时间和教室与原记录一致`
- 失败路径（非法教室）：
  - `POST /api/v4/schedule-adjustments/apply`（`newRoomId=999999`）-> `400`
  - 返回消息：`所选教室不存在`

结论：V4 调整失败/边界路径提示明确，且未出现脏写。

#### B-7 V4 报告导出复核

- 成功路径：`POST /api/v4/schedule-reports/plans/36/generate` -> `200`
  - 新报告：`reportId=6`
- 成功路径：`GET /api/v4/schedule-reports/6/download` -> `200`
  - 返回类型：`text/html;charset=UTF-8`
- 失败路径：`POST /api/v4/schedule-reports/plans/999999/generate` -> `400`
  - 返回消息：`排课方案不存在`

结论：导出生成/下载链路稳定，失败路径提示清晰。

#### 批次B收口结论

- 批次B两轮执行后，未发现新增 P0/P1 阻塞缺陷。
- V1->V4 关键模块“成功路径 + 失败/边界路径”覆盖完成。
- 可进入批次C（一致性与安全专项）。

### 2026-05-19 批次C执行进度（收口回归）

#### C-缺陷修复复测：回滚一致性

背景：
- 上一轮发现 `rollback` 文案与行为不一致：回滚后 `sourcePlanId` 可能变 `null`，不符合“回滚到该方案”。
- 已修复 `SchedulePlanService.rollbackPlan`：改为复用 `applyPlan(id)`，确保回滚语义为“重新应用目标方案”。

重启后最小回归结果：

1. `POST /api/v3/schedule-plans/53/apply` -> `200`
   - `appliedCount=237`
2. `GET /api/v4/schedules/current/source?termId=2` -> `200`
   - `sourcePlanId=53`
3. `POST /api/v3/schedule-plans/36/rollback` -> `200`
   - `appliedCount=233`
   - 返回消息：`已回滚到该方案`
4. `GET /api/v4/schedules/current/source?termId=2` -> `200`
   - `sourcePlanId=36`

结论：
- 回滚语义已与接口文案一致：回滚后正式课表来源正确切回目标方案。
- 批次C阻塞项关闭。
