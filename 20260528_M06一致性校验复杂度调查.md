# 20260528 M-06 一致性校验复杂度调查

## 结论

`M-06` 部分成立，不建议直接作为缺陷修复项进入代码修改。

原因：

- `V5ConsistencyCheckService.checkHardConflicts()` 确实存在嵌套循环。
- 当前实现不是全表两两比较，已按 `weekday` 分组、按 `startPeriod/endPeriod/id` 排序，并用 `active` 窗口移除已结束课程。
- 复杂度不是固定 `O(n^2)`，而是接近 `O(n log n + p)`，其中 `p` 为同一天节次重叠的候选对数量。
- 最坏情况下，如果大量课程在同一天同一时间段重叠，`active` 会很大，仍可能退化为 `O(k^2)`；但这类输入本身会产生大量真实冲突，需用数据量测试证明性能风险。

## 源码证据

目标文件：

- `backend/src/main/java/com/paike/scheduler/service/V5ConsistencyCheckService.java`

关键方法：

- `checkHardConflicts()`：约第 369 行。
- `addHardConflictIssues()`：约第 393 行。

当前流程：

1. 过滤掉缺少 `weekday/startPeriod/endPeriod` 的明细。
2. 按 `weekday` 分组。
3. 每天内部按 `startPeriod`、`endPeriod`、`id` 排序。
4. 维护 `active` 列表。
5. 每处理一个 `current`，先移除 `endPeriod < current.startPeriod` 的旧项。
6. 只和仍可能重叠的 `active` 项比较。
7. 实际重叠时才调用 `addHardConflictIssues()`。

这说明原报告中“明确为双重循环”的观察只覆盖代码形态，没有覆盖 active 窗口剪枝语义。

## 风险判断

可确认风险：

- 在大量课程集中于同一天同一节次时，`active` 列表可能接近当天课程数。
- `addHardConflictIssues()` 在冲突对上会按教师、班级、教室做判断，并可能触发缓存加载。

暂未确认风险：

- 当前没有数据量测试证明该方法会在真实规模下超时。
- 没有现成 `V5ConsistencyCheckServiceTest` 覆盖该方法。
- 报告未给出触发慢查询或接口超时的复现数据。

## 建议

下一步不直接重写算法。建议先做验证用例：

1. 新建 `V5ConsistencyCheckServiceTest`。
2. 覆盖三个输入规模：
   - 非重叠明细：验证不会误报冲突。
   - 少量重叠明细：验证教师、班级、教室冲突均能报出。
   - 高重叠明细：记录执行耗时或至少锁定输出数量，作为后续优化基线。
3. 如果高重叠数据表现差，再考虑把冲突检查改为按 `weekday + period + teacher/class/classroom` 建索引，直接定位冲突组。

## 当前建议状态

`M-06` 从“确认缺陷”调整为“部分成立，需数据量验证后再决定是否优化”。
