# 20260527 Bug 验证报告

## 1. 验证说明

- 验证对象：`D:\paike\CODE_REVIEW_REPORT.md`
- 验证日期：2026-05-27
- 验证方式：对当前工作区源码、SQL、配置和前端代码进行静态核验，并执行可运行的构建/单元测试命令。
- 状态定义：
  - **已修复**：当前分支已有对应修复提交，且已通过相应单测、静态验证或构建验证。
  - **确认**：当前源码中存在可直接支持原结论的代码或配置证据。
  - **部分成立**：代码现象存在，但原报告的严重级别、影响描述或可复现结果未被当前验证直接证明。
  - **不成立/当前不存在**：当前源码与原结论冲突，或该现象不足以构成报告所述问题。
  - **无法验证**：原报告缺少可对应的具体条目，或必须依赖未执行的运行场景。

## 2. 总体结论

原报告的明细数量与概览数量不一致：

截至 2026-05-28，`fix/code-review-critical-bugs` 已合并到 `main`，当前维护分支 `chore/review-maintenance-followups` 继续处理低优先维护项。

已完成或收敛的确认/部分成立条目：

- Critical：`C-01`、`C-02`、`C-03`、`C-05`、`C-06`、`C-10`、`C-11`、`C-12`、`C-15`、`C-16`、`C-17`、`C-21`、`C-22`。
- Major：`M-01`、`M-02`、`M-03`、`M-04`、`M-05`、`M-06`、`M-07`、`M-08`、`M-09`、`M-10`、`M-11`、`M-17`、`M-18`、`M-33`、`M-37`、`M-44`、`M-47`。
- 最新已提交维护修复：`f51f206 merge: 合并 M-06 一致性校验复杂度调查`。
- 剩余维护项不再进入严重问题修复计划，按收益和风险单独立项处理。

| 分类 | 报告宣称数量 | 可识别明细数量 | 结论 |
|---|---:|---:|---|
| Critical | 25 | 25 | 可逐条核验 |
| Major | 53 | 53 | 可逐条核验 |
| Minor | 59 | 47 | 缺少 `N-48` 至 `N-59` 共 12 条明细 |
| 合计 | 137 | 125 | 原报告多计 12 项 |

优先处理结论：

| 优先级 | 条目 | 验证结论 | 直接证据 |
|---|---|---|---|
| P0 | C-01 | 已修复 | `4c64595` 增加空值校验，空原因改为业务异常。 |
| P0 | C-10 | 已修复 | `07c0132` 修复候选位置容量校验，班级人数不再固定为 0。 |
| P0 | C-11 | 已修复 | `6fac914` 评分报告按学期隔离排课数据。 |
| P1 | C-15 | 已修复，已复核 | `10572d8` 对报告 HTML 输出字段做转义；本轮针对 Claude 复核意见确认 HTML 输出路径仍存在且 `safeHtml()` 覆盖方案名、质量等级、优化建议，补 `20260529_C15_HTML报告转义复核.md` 与优化建议断言。 |
| P1 | C-17 | 已修复 | `424b1cb` 增加管理员写保护；读接口保持登录态访问。 |
| P1 | C-22 | 已修复 | `7229421` 补齐缺失 V4/V5 表初始化脚本。 |
| P2 | C-20 | 不成立 | `v2_schema.sql` 与 `v6_bugfix_constraints.sql` 均使用 `ELSE NULL` |
| P2 | M-39 | 当前不存在 | 当前源码未检出 `.setDeleted(1)` |

## 3. Critical 条目验证

| 编号 | 状态 | 当前证据与判定 |
|---|---|---|
| C-01 | 已修复 | `4c64595`：`SchedulePlanService.adjustPlanItem()` 对 `adjustReason` 增加非空校验，避免 NPE。 |
| C-02 | 已修复 | `4c64595`：`ScheduleConflictService.checkConflict()` 改为安全比较，并对容量/人数缺失返回业务冲突原因。 |
| C-03 | 已修复 | `4c64595`：`ScheduleConflictService.checkConflict()` 对教师/班级/教室等比较改为 `Objects.equals(...)`。 |
| C-04 | 已修复 | `cc9a472`：`V4ScheduleSourceService.resolveSemester()` 对指定 `termId` 和当前学期缺失均抛业务异常；新增 `V4ScheduleSourceServiceTest`。 |
| C-05 | 已修复 | `4c64595`：`V5RepairTaskFlowService.createTask()` 对 `taskType` 增加空值校验。 |
| C-06 | 已修复 | `4c64595`：`V5Stage1DataService.createRepairTask()` 对 `taskCode`、`taskType` 增加空值校验。 |
| C-07 | 部分成立 | `AutoScheduleService.java:51` 存在入口级 `@Transactional`；事务过长或超时需用大批量数据实测。 |
| C-08 | 部分成立 | `V3ScheduleGenerateService.java:122-123` 的 `generateMultiple` 仍为事务方法；持续时间影响未实测。 |
| C-09 | 当前不存在 | 当前 `V4ScheduleLockService` 使用 `TransactionTemplate`，未检出报告所述 `synchronized` 锁释放顺序代码。 |
| C-10 | 已修复 | `07c0132`：候选位置容量校验改为读取真实班级人数，并补 `V5CandidatePositionServiceTest`。 |
| C-11 | 已修复 | `6fac914`：评分报告按学期过滤排课数据，避免跨学期污染。 |
| C-12 | 已修复 | `6fac914`：`V5RepairTaskStatus` 增加 `PENDING`，V5 修复任务初始状态对齐。 |
| C-13 | 已验证，暂不修复 | `V1__baseline.sql:153` 为旧写法 `ELSE id`，后续 `v2_schema.sql`/`v6_bugfix_constraints.sql` 已统一到 `active_key`；本轮补 `20260528_C13_baseline_unique_key_mismatch调查.md` 与 `DatabaseSchemaScriptTest` 回归。 |
| C-14 | 已修复 | `cc9a472`：正式课表锁定项将大节 `periodNo` 转换为小节范围，例如 `2 -> 3-4`；补充 `V4ScheduleLockServiceTest` 覆盖。 |
| C-15 | 已修复 | `10572d8`：报告 HTML 输出字段统一转义，阻断脚本注入展示。 |
| C-16 | 已修复 | `10572d8`：报告下载路径限制到报告目录，拒绝越界路径。 |
| C-17 | 已修复 | `424b1cb`：`AuthInterceptor` 增加管理员写保护，`POST/PUT/PATCH/DELETE` 默认要求 `ADMIN`。 |
| C-18 | 不成立 | `LoginResponse.token` 的 `@JsonIgnore` 事实存在，但项目采用 httpOnly cookie 时不向响应体返回 token 是接口设计，不构成 Critical 漏洞。 |
| C-19 | 已修复 | `4fd90a6`：`v3_semester_data_bind.sql` 与 `v2_alter_score_report.sql` 改为 `information_schema` 检查后幂等补列，补 `DatabaseSchemaScriptTest` 回归。 |
| C-20 | 不成立 | `v2_schema.sql:19` 与 `v6_bugfix_constraints.sql:16` 均为 `CASE WHEN deleted = 0 THEN 0 ELSE NULL END`；不一致实际位于 `V1__baseline.sql`。 |
| C-21 | 已修复 | `7229421`：`schedule` 唯一约束改用 `active_key`，允许软删除历史记录共存。 |
| C-22 | 已修复 | `7229421`：新增缺失表初始化脚本并注册到 `application.yml`。 |
| C-23 | 已修复 | `fix/frontend-plan-id-guard`：路由层统一校验 `id`、`taskId`、`planId` 为正整数，非法参数回到对应列表或父页面，避免详情页以 `NaN` 调接口。 |
| C-24 | 部分成立 | 前端检出 2 处 `document.execCommand('copy')`；其为废弃 fallback，但通常不构成 Critical 故障。 |
| C-25 | 部分成立 | 前端检出 9 处无泛型的 `formRef = ref()`；属于类型约束问题，不是 Critical 运行故障。 |

## 4. Major 条目验证

| 编号 | 状态 | 当前证据与判定 |
|---|---|---|
| M-01 | 已修复 | `8d6c063`：`V5RepairSuggestionService` 解析建议明细后统一 `selectBatchIds` 加载教室名称。 |
| M-02 | 已修复 | `8d6c063`：`V5SimulationService` 在 `buildCompare()` 中统一批量加载教室名称，变更明细和利用率共用名称 Map。 |
| M-03 | 已修复 | `ScheduleConflictService.checkConflict()` 改用 `TeachingTaskMapper.selectConflictCheckById()`，一次 JOIN 加载课程/教师/班级字段。 |
| M-04 | 已修复 | `UnscheduledTaskService.list()` 统一调用 `UnscheduledTaskMapper.selectFilteredPage()`，关联名称筛选和分页均下推数据库。 |
| M-05 | 已验证，暂不修复 | `SchedulePlanService.refreshPlanConflictState()` 已批量加载关联数据，且只更新冲突状态变化的明细；本轮补 `20260528_M05冲突状态刷新复杂度调查.md` 与大样本无变化回归测试。 |
| M-06 | 已验证，暂不修复 | `f51f206`：复核发现 `checkHardConflicts()` 已按星期分组、节次排序并使用 active 窗口剪枝；补 `V5ConsistencyCheckServiceTest` 覆盖无重叠、硬冲突和 200 条高重叠输入。 |
| M-07 | 已修复 | `V4ScheduleRiskService.buildContext()` 复用已加载的 `slots` 列表计算 `totalTimeSlots`，不再额外 `selectCount`。 |
| M-08 | 已收敛 | `5e7a78c`：保留课表包含搜索语义，新增 `idx_schedule_semester_deleted_created` 优化默认学期列表过滤/排序；若改为前缀/全文搜索仍需产品确认。 |
| M-09 | 已修复 | `bd847e3`：报告生成移除长事务边界，文件生成不再包在事务方法内。 |
| M-10 | 已修复 | `bd847e3`：V5 试算生成/局部重排只用事务包裹写入流程，成功后的详情构建移出事务。 |
| M-11 | 已修复 | `bd847e3`：报告目录支持 `paike.report.dir` 配置，不再硬编码 `data/reports`。 |
| M-12 | 不成立 | `ScheduleMapper.xml` 的 JOIN 被过滤条件和查询条件使用，并非“JOIN 结果未利用”；是否优化需以 SQL 执行计划判断。 |
| M-13 | 部分成立 | 检出 4 个 Controller 直接注入 Mapper、共 21 个字段；现象成立，但更像结构性可维护性问题，不直接构成功能 bug。 |
| M-14 | 确认 | 后端检出 16 个文件、66 处 `Map<String, Object>`。 |
| M-15 | 部分成立 | `TimetableController` 同时承载查询、装配和 Excel 导出；属于可维护性问题，不直接构成功能 bug。 |
| M-16 | 确认 | 9 个 Entity 检出 40 处 `@TableField(exist = false)` 视图字段。 |
| M-17 | 已修复 | `0b98907`：`selectFilteredTaskIds` 更名为 `selectFilteredTasks`，Mapper 接口、XML 和调用方契约一致。 |
| M-18 | 已修复 | `0b98907`：`selectDailyConflictCounts` 改为明确 DTO `ScheduleDailyConflictCounts`，移除 `resultType="HashMap"`。 |
| M-19 | 已修复 | `2e7c9f8`：新增 `StringSanitizer.trimToNull`，替换 5 个 Service 和 `ScheduleController` 内重复私有方法；新增 `StringSanitizerTest`。 |
| M-20 | 部分成立 | `frontend/src/api/index.ts` 已汇总大部分基础 API 与部分 v4 模块；仍有 `v4ScheduleAnalysisApi` 与 `v5SimulationApi` 两个命名冲突模块未能无脑 `export *`。 |
| M-21 | 已整理 | 新增 `frontend/src/utils/async.ts` 统一 `fallback<T>()`，5 个页面改为复用公共 helper。 |
| M-22 | 已整理 | `utils/status.ts` 补排课方案状态和 V5 修复任务状态映射，移除多处重复局部 `statusText`/`statusTagType`。 |
| M-23 | 已整理 | 新增 `frontend/src/utils/download.ts` 统一文件名解析和浏览器下载触发，`timetable.ts` 与 `v4ScheduleReportApi.ts` 复用。 |
| M-24 | 已修复 | 移除前端遗留 `token` store、`setToken`/`clearToken` 和 `TOKEN_KEY` 占位；当前认证完全依赖 `paike_token` httpOnly Cookie + `userInfo`。 |
| M-25 | 部分成立，已整理 | 项目同时存在 `create_time/update_time` 与 `created_at/updated_at` 两套时间字段，但未见单表内混用；本轮补齐 `auto_schedule_batch` 基线建表中的 `update_time`，保留 v9 幂等补列，并新增 `20260529_M25时间字段命名调查.md` 与 `M25TimestampNamingInvestigationTest`。 |
| M-26 | 不成立 | MyBatis-Plus 的约定映射允许字段不显式声明 `@TableField`；该项仅为风格偏好。 |
| M-27 | 部分成立 | `V4ScheduleAiAnalysisService` 已使用静态 `HTTP_CLIENT`；跨全部 AI 类的重复程度未由原条目精确定义。 |
| M-28 | 已修复 | 默认 JDBC URL 与 `.env.example` 改为 Java 编码名 `characterEncoding=UTF-8`；本轮补 `20260529_M28_M29数据库字符集调查.md` 与 `M28M29DatabaseCharsetInvestigationTest`。 |
| M-29 | 已修复 | 50 个 `CREATE TABLE` 语句补 `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`，新库初始化不再依赖实例默认字符集。 |
| M-30 | 已修复 | 移除未启用的 `flyway-mysql` 依赖和 `spring.flyway.enabled:false` 配置段；当前明确走 `spring.sql.init + SemesterSchemaInitializer`，本轮补 `20260529_M30_Flyway初始化策略调查.md` 与 `M30FlywayInitializationStrategyInvestigationTest`。 |
| M-31 | 部分成立，暂不修复 | 当前检出 15 个映射实体未包含 `deleted` 字段，但对应 SQL 表也没有 `deleted` 列；本轮补 `20260529_M31逻辑删除实体覆盖调查.md` 与 `M31LogicDeleteEntityCoverageInvestigationTest` 锁定现状。 |
| M-32 | 部分成立，暂不修复 | SQL 脚本未检出 `FOREIGN KEY` 或 `REFERENCES`，但当前关系通过业务层校验、软删除和索引/唯一约束表达；直接补外键会牵涉既有数据、迁移顺序和级联/软删除语义。本轮补 `20260529_M32外键约束调查.md` 与 `M32ForeignKeyConstraintInvestigationTest` 锁定现状。 |
| M-33 | 已修复 | `c604af0`：`.env.example` 去掉 `root/root` 示例，改为本地低权限账号和强密钥占位说明。 |
| M-34 | 部分成立 | `vite.config.ts:20` 为 `host: '0.0.0.0'`；是开发服务暴露范围配置，非生产漏洞本身。 |
| M-35 | 确认 | `v2_schema.sql:86` 以 `VARCHAR(255)` 保存 `related_schedule_ids`。 |
| M-36 | 确认 | `v3_score.sql` 的初始 INSERT 通过 `FROM semester s WHERE s.is_current = 1` 驱动，无当前学期时不会写入默认权重。 |
| M-37 | 已修复 | `424b1cb`：`SecurityHeadersFilter` 增加 `Content-Security-Policy`。 |
| M-38 | 确认 | `ScheduleRuleService.java:23` 使用 `ConcurrentHashMap` 缓存，读取逻辑未见过期机制。 |
| M-39 | 当前不存在 | 全部 Java 源码未检出 `.setDeleted(1)`，原定位已不适用于当前版本。 |
| M-40 | 已修复 | `fix/deleted-flag-comparisons`：清空主代码中 `getDeleted() == 1`/`!= 1` 风险写法，保留业务存在性判断语义。 |
| M-41 | 暂缓 | 复核显式 `deleted=0`：Java `LambdaQueryWrapper` 侧 66 处多为 `@TableLogic` 实体上的冗余条件，但 XML join 19 处和 DB 脚本 6 处属于必要/不同语义；批量删除收益低、回归面大，暂不机械清理。 |
| M-42 | 已修复 | `fix/autoschedule-batch-no-collision`：删除 `V3ScheduleGenerateService` 固定为 `true` 的评分开关和不可达 legacy 评分分支；新增 `V3ScheduleGenerateServiceTest` 防回归。 |
| M-43 | 暂缓 | 复核 `V5SimulationService.discard()`：`requireSimulationPlan()` 允许 `DISCARDED`，且清理明细前先生成返回快照；前端使用该快照展示结果。缺少可触发错误结果，暂不改代码。 |
| M-44 | 已修复 | `4c64595`：`V4ScheduleAdjustmentService` 预览路径给空原因设置默认值，应用路径保持非空校验。 |
| M-45 | 已修复 | `investigate/audit-log-failure-events`：`SystemAuditLogService` 新增独立事务 `recordFailure()`；`SchedulePlanService.applyPlanWithAudit()` 在应用/回滚失败时记录失败审计且不掩盖原异常；新增服务测试覆盖。 |
| M-46 | 已修复 | `2841a4e`：`TimeSlotController` 与同类 `AutoScheduleBatchController` 的详情不存在分支改抛 `BusinessException(404, ...)`，由全局异常处理返回真实 HTTP 404；新增 `ControllerNotFoundStatusTest`。 |
| M-47 | 已修复 | `4dd3ec7`：15 个分页列表 Controller 增加类级 `@Validated`，并为默认分页参数补齐 `@Min(1)` / `@Max(200)`；新增 `ControllerPaginationValidationTest` 锁定约束覆盖。 |
| M-48 | 已修复 | `f150760`：基础资料和教学任务请求表单按数据库 `VARCHAR` 长度补齐 `@Size`；新增 `ControllerRequestStringValidationTest` 锁定约束覆盖。 |
| M-49 | 复核暂缓 | `20260528_M49_M51契约调查.md`：当前前端 API 将 `weight` 声明为可选，后端 `updateWeight` 支持部分更新；强制 `@NotNull` 会改变接口契约。 |
| M-50 | 不成立（契约设计） | 绝大多数候选端点把 `request == null` 作为默认契约处理；本轮补 `20260528_M50_request_body_optionality调查.md` 与 `M50RequestBodyOptionalityInvestigationTest`。 |
| M-51 | 复核暂缓 | `20260528_M49_M51契约调查.md`：未配置 `ADMIN_DEFAULT_PASSWORD` 时，stdout 是当前随机初始密码交付方式；直接移除会导致首次 admin 凭据不可取得。 |
| M-52 | 确认 | `RateLimiterService` 使用进程内 `ConcurrentHashMap`，源码注释亦建议生产改为 Redis。 |
| M-53 | 不成立 | 检出 2 个 DELETE 端点使用 Query Param；这属于 API 风格选择，不自动构成 bug。 |

## 5. Minor 条目验证

| 编号 | 状态 | 当前证据与判定 |
|---|---|---|
| N-01 | 已修复 | `fix/deleted-flag-comparisons`：剩余 `getDeleted() == 1` 改为 `Integer.valueOf(1).equals(...)`，避免拆箱空值风险；新增 `DeletedFlagComparisonTest` 防回归。 |
| N-02 | 已修复 | `fix/autoschedule-batch-no-collision`：`TeacherUnavailableTimeService` 将 create/update 共享的教师、时间段、重复禁排校验收口到私有方法；新增 `TeacherUnavailableTimeServiceTest` 覆盖重复校验行为。 |
| N-03 | 已修复 | `fix/autoschedule-batch-no-collision`：`AutoScheduleService.categorizeReason()` 改为只读取 `ScheduleConflictService` 的结构化 `[TYPE]` 标签，不再按中文文案 contains 分类；新增 `AutoScheduleServiceTest` 防回归。 |
| N-04 | 已修复 | `cc9a472`：`ScheduleRuleWeightService.updateWeight()` 补 `@Transactional(rollbackFor = Exception.class)`；新增 `ServiceTransactionAnnotationTest`。 |
| N-05 | 已修复 | `cc9a472`：`TeacherUnavailableTimeService.delete()` 补 `@Transactional(rollbackFor = Exception.class)`；新增 `ServiceTransactionAnnotationTest`。 |
| N-06 | 当前不存在 | `ScheduleScoreReportService` 已定义 `DEDUCT_PER_*` 和 `FULL_SCORE` 常量，原“扣分魔法值”问题已处理。 |
| N-07 | 已修复 | `fix/autoschedule-batch-no-collision`：利用已有 `uk_batch_no` 唯一约束，批次插入遇到 `DuplicateKeyException` 时有限重试，耗尽后抛业务异常；新增 `AutoScheduleBatchServiceTest` 覆盖碰撞重试和耗尽失败。 |
| N-08 | 不成立 | `Collectors.toList()` 与 `List.of()` 用途不同，混用本身不是缺陷。 |
| N-09 | 已修复 | `fix/schedule-plan-status-enum`：本轮仅收口 `SchedulePlan.status`，新增 `SchedulePlanStatus` 枚举并替换后端方案状态比较/赋值；V5 repair task/suggestion 状态、`sourceType`、`planMode` 保持不变。 |
| N-10 | 已修复 | `fix/remove-v5-repair-keepobjects`：删除 `V5RepairExplanationService` 中仅用于避免 unused 警告的 `__keepObjects()` 占位方法，并移除 `Objects` import。 |
| N-11 | 已修复 | `investigate/v4-replan-keep-locked`：保留 `keepLocked=false` 输入拒绝契约，收口 `V4ScheduleReplanService` 内部恒 true 分支；局部重排无条件复制锁定项，结果固定 `keepLocked=true`；新增 `V4ScheduleReplanServiceTest` 覆盖 false 拒绝和默认复制锁定项。 |
| N-12 | 无法验证 | 日志是否应保留非 ERROR 内容属于产品/审计要求，原报告未给出期望行为。 |
| N-13 | 已修复 | `fix/N-13-enum-code-conversion`：补齐 `V5RepairTaskStatus`、`V5SuggestionStatus` 的 `fromCode()`/`is()`，为修复任务状态增加 `isTerminal()`；替换 V5 修复任务/建议服务中的任务状态字符串判断；新增 `V5StatusEnumConversionTest` 和任务状态更新回归测试。 |
| N-14 | 已修复 | `fix/N-14-v5-task-status-trailing-comma`：移除 `V5RepairTaskStatus` 枚举末项后的多余逗号。 |
| N-15 | 部分成立 | `TeacherUnavailableTime` 使用 Jakarta Validation 的事实可见；是否应扩展到其他 Entity 不是 bug 结论。 |
| N-16 | 已修复 | `investigate/N-16-report-semester-fields`：复核发现 `ScheduleScoreReport` 已由 v13 修复；本轮补齐 `ScheduleConflictReport`、`AutoScheduleBatch` 的 `semesterId` 字段、生成写入、列表过滤、前端 API 类型和 `v17_report_batch_semester.sql` 幂等迁移。 |
| N-17 | 已修复 | `investigate/N-17-schedule-report-semester-deleted`：补齐 `ScheduleReport.semesterId` 和 `@TableLogic deleted`，生成报告时继承方案学期；更新 VO/前端 API 类型、v14 建表、启动兜底初始化和 `v18_schedule_report_semester_deleted.sql` 幂等迁移。 |
| N-18 | 部分成立 | 枚举形态存在差异属于一致性维护项，原报告未给出会导致错误的接口依赖。 |
| N-19 | 已修复 | `investigate/N-19-schedule-score-detail-deleted`：补齐 `ScheduleScoreDetail.@TableLogic deleted`，更新 `v3_score.sql`/baseline 建表、启动兜底初始化和 `v19_score_detail_deleted.sql` 幂等迁移；`rescore()` 旧明细删除改由逻辑删除过滤。 |
| N-20 | 部分成立 | Form/DTO 源码同时使用 Lombok `@Getter` 与 `@Data`；属于编码约定项。 |
| N-21 | 确认 | 前端检出 20 个文件、37 处 `console.error`，部分加载失败没有用户反馈。 |
| N-22 | 当前不存在 | 当前前端未检出 `as any` 模式，原具体断言不再成立。 |
| N-23 | 确认 | 前端检出 formatter/参数相关 `any`，包括 `ScheduleCharts.vue`。 |
| N-24 | 确认 | `useCrudForm.ts` 仍存在 `tableData` 相关类型断言。 |
| N-25 | 不成立 | `ApiResponse<T>` 作为 TypeScript 静态类型本就不提供运行时校验；需要运行时 schema 时应另立需求。 |
| N-26 | 部分成立 | 命名一致性是维护建议，原条目没有可验证的功能错误。 |
| N-27 | 已修复 | `fix/frontend-error-feedback`：`ScheduleRuleWeightView.vue` 单项保存和批量保存失败时显示 `ElMessage.error`。 |
| N-28 | 已修复 | `fix/frontend-error-feedback`：`ScheduleStatisticsView.vue` 方案列表、概览、工作量、教室利用率、班级均衡度查询失败时显示错误提示。 |
| N-29 | 已修复 | `fix/frontend-error-feedback`：`AutoScheduleView.vue` 待排任务加载、自动排课、清空批次失败时显示错误提示。 |
| N-30 | 确认 | `useCrudForm.ts` 检出多处 `Record<string, unknown>` 搜索表单类型。 |
| N-31 | 确认 | ECharts 相关参数中存在 `any`，与 N-23 为重叠问题。 |
| N-32 | 部分成立 | 未检出空 `catch {}`，但检出大量仅控制台记录的 catch；与 N-21/N-28/N-29 重复计数。 |
| N-33 | 无法验证 | 原报告仅写“散布”，未提供文件或标识符；前端构建通过不等同于无未使用变量。 |
| N-34 | 已修复 | `fix/schedule-compare-duplicate-plan-ids`：`ScheduleCompareService.compare()` 在加载方案前拒绝重复方案 ID；新增 `ScheduleCompareServiceTest` 覆盖重复 ID、少于 2 个方案、跨学期方案拒绝。 |
| N-35 | 当前不存在 | `ScheduleStatisticsService` 已注入 `SchedulePlanItemMapper`，且处理 `SchedulePlanItem` 字段。 |
| N-36 | 部分成立 | SQL 脚本同时存在 `weekday`/`day_of_week` 定义；注释含义与实际时段映射需以业务模型确认。 |
| N-37 | 不成立 | 使用 `INT` 或 `TINYINT` 是存储优化选择，不构成功能 bug。 |
| N-38 | 不成立 | BCrypt 哈希长度当前可被 `VARCHAR(100)` 容纳，“未来可能不足”不是现存缺陷。 |
| N-39 | 不成立 | 独立引入 `spring-security-crypto` 属依赖选择，不构成问题。 |
| N-40 | 无法验证 | 本次确认存在 baseline 与初始化脚本，但“内容完全重复”未由逐表语义比对证明。 |
| N-41 | 部分成立 | `application.yml` 存在排课配置项；配置默认值硬编码是可配置性建议。 |
| N-42 | 不成立 | 默认学期为 `2025-2026`；在验证日期 2026-05-27 尚不能直接认定已过期。 |
| N-43 | 确认 | `application.yml` 未检出请求体大小限制配置。 |
| N-44 | 部分成立 | `GlobalExceptionHandler` 存在日志处理；是否缺少所需请求上下文需按日志合规要求定义。 |
| N-45 | 部分成立 | 两个 Controller 均存在路径映射；“路径重叠”是否冲突应按完整 method/path 映射判断。 |
| N-46 | 部分成立 | `ScheduleController` 以 POST 提供冲突检查；属于 REST 语义选择，不是运行故障。 |
| N-47 | 部分成立 | `TeachingTaskController` 涉及学期字段；是否遗漏跨实体学期校验需以创建用例复现。 |
| N-48 | 无法验证 | 原报告未提供该条明细。 |
| N-49 | 无法验证 | 原报告未提供该条明细。 |
| N-50 | 无法验证 | 原报告未提供该条明细。 |
| N-51 | 无法验证 | 原报告未提供该条明细。 |
| N-52 | 无法验证 | 原报告未提供该条明细。 |
| N-53 | 无法验证 | 原报告未提供该条明细。 |
| N-54 | 无法验证 | 原报告未提供该条明细。 |
| N-55 | 无法验证 | 原报告未提供该条明细。 |
| N-56 | 无法验证 | 原报告未提供该条明细。 |
| N-57 | 无法验证 | 原报告未提供该条明细。 |
| N-58 | 无法验证 | 原报告未提供该条明细。 |
| N-59 | 无法验证 | 原报告未提供该条明细。 |

## 6. 构建与测试验证

| 验证命令 | 结果 | 说明 |
|---|---|---|
| `cd D:\paike\frontend; npm run build` | 通过 | `vue-tsc -b && vite build` 成功，Vite 于 13.17 秒完成构建。 |
| `cd D:\paike\backend; mvn test -DskipTests=false` | 历史未通过 | 初始验证时共执行 28 项测试：0 failures、1 error；`SchedulerBackendApplicationTests.contextLoads` 因环境未提供 `JWT_SECRET` 导致 ApplicationContext 启动失败。 |
| `cd D:\paike\backend; mvn test` | 环境阻塞 | 2026-05-27 复测时已设置 `JWT_SECRET`，但 Spring context 测试因 `${DB_USERNAME}` 未解析导致 MySQL `Access denied`；不是本轮代码编译失败。 |
| `cd D:\paike\backend; mvn -Dtest=!SchedulerBackendApplicationTests test` | 通过 | 59 tests，0 failures，0 errors；覆盖当前可脱离 DB context 的后端测试。 |
| `cd D:\paike\backend; mvn -Dtest=V5ClassroomBatchLoadingTest test` | 通过 | 2 tests，0 failures，0 errors；验证 M-01/M-02 教室名称查询收敛到批量加载。 |
| `cd D:\paike\backend; mvn -Dtest=ScheduleConflictServiceTest clean test` | 通过 | 4 tests，0 failures，0 errors；验证 M-03 冲突检查入口改用任务详情合并查询。 |
| `cd D:\paike\backend; mvn -Dtest=UnscheduledTaskServiceTest clean test` | 通过 | 3 tests，0 failures，0 errors；验证 M-04 关联筛选改用数据库分页。 |
| `cd D:\paike\backend; mvn -Dtest=UnscheduledTaskServiceTest,V4ScheduleRiskServiceTest test` | 通过 | 4 tests，0 failures，0 errors；验证 M-04/M-07 定向回归。 |
| `cd D:\paike\backend; mvn -Dtest=DatabaseSchemaScriptTest test` | 通过 | 10 tests，0 failures，0 errors；验证 C-19 遗留补列脚本已改为幂等注册。 |
| `cd D:\paike\backend; mvn -Dtest=V5ConsistencyCheckServiceTest test` | 通过 | 3 tests，0 failures，0 errors；验证 M-06 当前算法行为与高重叠基线。 |
| `cd D:\paike\backend; mvn -Dtest=SchedulePlanServiceTest test` | 通过 | 4 tests，0 failures，0 errors；验证 M-05 冲突状态刷新只更新变化明细，大样本无变化时跳过明细更新。 |

本次未启动 Spring Boot 服务，也未执行需要数据库和浏览器登录态的接口/端到端复现。因此，事务超时、权限越权路径、SQL 在目标 MySQL 实例中的重复执行结果，以及前端错误参数的实际请求行为，均按静态证据标注为“确认模式”或“部分成立”，未伪装为运行时复现。

## 7. 建议修复顺序

| 顺序 | 待处理项 | 原因 |
|---:|---|---|
| 1 | M-08 至 M-11 中尚未修复项 | 继续收敛性能和事务稳定性风险；M-05、M-06 已验证暂不改生产代码。 |
| 2 | C-04、C-14 等仍确认项 | C-19 已修复；其余非本轮已完成范围，后续按风险和复现成本排期。 |
| 3 | 原报告计数与重复项修订 | 修正审查基线，避免把规范建议当成已复现缺陷管理。 |
