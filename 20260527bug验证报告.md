# 20260527 Bug 验证报告

## 1. 验证说明

- 验证对象：`D:\paike\CODE_REVIEW_REPORT.md`
- 验证日期：2026-05-27
- 验证方式：对当前工作区源码、SQL、配置和前端代码进行静态核验，并执行可运行的构建/单元测试命令。
- 状态定义：
  - **已修复**：当前分支已有对应修复提交，且已通过相应单测、静态验证或构建验证。
  - **确认**：当前源码中存在可直接支持原结论的代码或配置证据。
  - **部分成立**：代码现象存在，但原报告的严重级别、影响描述或可复现结果未被当前验证直接证明。
  - **不成立/当前不存在**：当前源码与原结论冲突，或该现象不足以构成报告所述问题。
  - **无法验证**：原报告缺少可对应的具体条目，或必须依赖未执行的运行场景。

## 2. 总体结论

原报告的明细数量与概览数量不一致：

截至 2026-05-28，`fix/code-review-critical-bugs` 已合并到 `main`，当前维护分支 `chore/review-maintenance-followups` 继续处理低优先维护项。

已完成或收敛的确认/部分成立条目：

- Critical：`C-01`、`C-02`、`C-03`、`C-05`、`C-06`、`C-10`、`C-11`、`C-12`、`C-15`、`C-16`、`C-17`、`C-21`、`C-22`。
- Major：`M-01`、`M-02`、`M-03`、`M-04`、`M-05`、`M-06`、`M-07`、`M-08`、`M-09`、`M-10`、`M-11`、`M-13`、`M-15`、`M-17`、`M-18`、`M-27`、`M-33`、`M-37`、`M-44`、`M-47`。
- Minor：`N-21`、`N-23`、`N-24`、`N-30`、`N-31`、`N-43`。
- 最新维护修复：`refactor/n23-echarts-any-types` 收口 `N-23`/`N-31` ECharts 和前端显式 `any` 类型。
- 剩余维护项不再进入严重问题修复计划，按收益和风险单独立项处理。

近期进展（按对应提交时间）：

- 2026-06-02 13:27（`7a821a0` / `d615988`）：收口 `M-15` 课表导出职责，新增 `TimetableService`，Controller 仅保留路由委派。
- 2026-06-02 13:52（`abc1437` / `13d19bf`）：收口 `M-13` Controller 服务边界，Controller 目录不再直接注入 Mapper。
- 2026-06-02 14:46（`980392f` / `dbd8c9c`）：收口 `N-21` 前端错误反馈，catch 内补 `ElMessage.error` 用户提示。
- 2026-06-02 15:00（`6945462` / `c2234c6`）：收口 `N-23`/`N-31` ECharts 与前端显式 `any` 类型。
- 2026-06-03 00:37（`24d8542`）：归档 `M-14`/`M-16`/`M-25` 三份风险评估报告与证据清单（`20260602_M14/M16/M25_*风险评估.md`、`minimaxm320260602.md`），含 gpt-5.5 评语逐条复核与勘误；以 squash 方式并入 `main`，**仅评估、不改生产代码**，故三项状态维持原判。
- 2026-06-03（分支 `fix/n43-request-body-size-limit`）：收口 `N-43`，新增 `RequestBodySizeLimitFilter` 应用层请求体大小兜底（按 `Content-Length` 拦截裸 JSON 大包，超限返回 413），补 `RequestBodySizeLimitFilterTest`，调查见 `20260603_N43_请求体大小限制调查.md`。
- 2026-06-03（分支 `refactor/n24-n30-usecrudform-types`）：收口 `N-24`/`N-30`，`useCrudForm.ts` 的 `tableData` 改 `as Ref<T[]>`、搜索表单引入泛型 `S` 收紧类型，前端 `npm run build` 通过，调查见 `20260603_N24_N30_useCrudForm类型收口.md`。
- 2026-06-03（分支 `refactor/m27-ai-client-consolidation`）：收敛 `M-27`，抽出 `AiChatProperties` + `RemoteAiChatClient` 统一远程 AI 的配置/HTTP envelope/JSON 提取/prompt 清洗，两业务服务去重，测试改写为 `M27AiClientConsolidationTest`，调查见 `20260603_M27_AI客户端收敛.md`。
- 2026-06-03（分支 `refactor/m14-mapstringobject-dto-ify`）：`M-14` 阶段1 收口，6 个低风险端点（health/logout/checkConflict/getScoreSummary/rescore/getUnassignedSummary）由 `Map<String,Object>` 机械替换为 6 个新 VO，JSON 契约逐字段不变、前端零改动；`Map<String,Object>` 计数 66→57 处。新增 `M14Phase1VoSerializationTest` 锁定 JSON 输出、更新现状计数测试，调查见 `20260603_M14_阶段1_低风险端点收口.md`。
- 2026-06-03（分支 `refactor/m14-phase2-apply-compare-vo`）：`M-14` 阶段2 收口，apply/adjust/rollback/compare 4 个端点 + 3 处跨模块内部调用方由 `Map<String,Object>` 改为 4 个新 VO（`AdjustPlanResultVo`/`ApplyPlanResultVo`/`ComparePlanVo`/`CompareResultVo`），apply 与 rollback 合用一个 VO（message 走 `@JsonInclude(NON_NULL)`），`compare` 内部聚合逻辑重写为 VO getter；JSON 契约不变、前端零改动；`Map<String,Object>` 计数 57→32 处。新增 `M14Phase2VoSerializationTest`、更新现状计数测试，调查见 `20260603_M14_阶段2_apply对比收口.md`。
- 2026-06-03（分支 `refactor/m14-phase3-statistics-vo`）：`M-14` 阶段3 收口（最后一阶段），统计聚合 5 个端点（teacher-workload/classroom-utilization/class-balance/plan-overview/dashboard）+ 2 个活 refresh 端点（schedule-analysis/schedule-risks）由 `Map<String,Object>` 改为 7 个新 VO（`TeacherWorkloadVo`/`ClassroomUtilizationVo`/`ClassBalanceVo`/`PlanOverviewVo`/`DashboardStatsVo`/`RefreshPlanSummaryVo`/`RefreshPlanRisksVo`）；`ScheduleStatisticsService` 嵌套 `Map<Long,Map<String,Object>>` 累加改强类型累加器、删私有 `longSet`/`integerLongMap` 强转（零 unchecked），`loadItems()` 多态保留 `instanceof`（不动 Entity）；调查发现并保留 2 处前端不读但历史输出的字段以维持 JSON 逐字节不变（`teacherWorkload.maxContinuousPeriods` 恒 0、`classBalance.dailyPeriods` 动态键），`PlanOverviewVo` 用普通 POJO 让 best/applied 块恒序列化 null（对齐前端 `number|null`）；JSON 契约不变、前端零改动；`Map<String,Object>` 计数 32→0 处、4→0 文件，**M-14 全量归零**。新增 `M14Phase3VoSerializationTest`（10 用例）、计数测试改写为归零守卫，调查见 `20260603_M14_阶段3_统计聚合VO收口.md`。
- 2026-06-03（仅调查、未动代码）：评估 `M-16` 是否为下一治理项，对 `20260602_M16_EntityViewField风险评估.md` 的高风险点逐条源码核实——确认评估勘误属实（`requiredSlots` 错配为现存 bug：`TeachingTask.java` 无此字段而前端 `teachingTask.ts:17` 必填声明，致前端 undefined），另发现 `TeachingTask` 前端仅声明 4/8 个 view 字段（courseType/teacherStatus/classStatus/studentCount 未声明但后端冲突检测/alias 依赖）、`Schedule` view 字段 getter 被 27 文件读（深耦合，应最后做）。产出 `20260603_M16_实施前置确认清单.md`，列出动手前须拍板的 6 个决策（推进范围/装配方案A·B/是否删 Entity 字段/TeachingTask 未声明字段/requiredSlots 既存 bug/契约兜底）。结论：M-16 需动 Entity+Mapper XML 两层、字段遗漏致前端白屏，比 M-14 风险高一级，建议单 Entity 单 PR 分批、从最简 `ScheduleConflictReport`(1 字段) 热身。`M-16` 状态维持「部分成立，暂不修复」，待决策后再启动。
- 2026-06-03（分支 `refactor/m16-batch1-conflict-report-vo`）：`M-16` 第1批收口（9 批热身批，最简）。`ScheduleConflictReport` 删唯一 `@TableField(exist = false)` view 字段 `timeSlotName`（连同仅此字段用的 `@TableField` import），新建 `ScheduleConflictReportVo`（普通 POJO、13 字段=12 持久化列+timeSlotName、保留 null 序列化）；`list()` 改建 VO 分页（保留 current/size/total + `toVo` 逐字段拷）、`fillRelationFields`/`extractDayOfWeekFromRelatedSchedules` 改吃 VO（保留 `normalizeReportNo` 去序号后缀 + timeSlotName 三分支：时段 label/「全周」/「-」），Controller `list` 返回 `Result<Page<ScheduleConflictReportVo>>`；`generate()`/`buildReport()` 持久化路径零改动。JSON 字段名/类型不变、前端零改动（`scheduleConflictReport.ts` 与 `ScheduleConflictReportView.vue:223` 仅读 `timeSlotName`）。view 字段命中数 43→42、承载 Entity 9→8。确立「删字段+VO+改下游+测试归零」五层范式，作后续 8 批模板。新增 `M16ConflictReportVoSerializationTest`（4 用例：13 字段集 + timeSlotName 三态 + null 保留）、改 `M16TableFieldViewFieldsInvestigationTest` 现状计数（43→42、9→8、删 ScheduleConflictReport 计数断言）。调查见 `20260603_M16_第1批_ScheduleConflictReport收口.md`。
- 2026-06-03（分支 `refactor/m16-batch2-unassigned-task-vo`）：`M-16` 第2批收口（按真实难度而非蓝图序号选型，跳过 `SchedulePlan` 选 `ScheduleUnassignedTask`：单端点、无 alias、三字段与第1批同构）。`ScheduleUnassignedTask` 删 3 个 `@TableField(exist=false)` view 字段 courseName/teacherName/className（`@TableField` import 保留——`created_at` 仍用，与第1批不同），新建 `ScheduleUnassignedTaskVo`（12 字段=9 持久化列〔含 `deleted`〕+3 view、普通 POJO 保留 null）；`SchedulePlanExplainService.listUnassignedTasks` 改 `selectList→map(toVo)→fillUnassignedRelations(List<Vo>)→返回 List<Vo>`，`summarizeUnassignedTasks`/`clearPlanArtifacts` 持久化路径不动。**本批扩展范式：view 字段存在内部 Java 消费方**——`listUnassignedTasks` 被 `V4ScheduleRiskService:70` 内部调用、`detectUnscheduledTasks` 读 `getCourseName/getTeacherName/getClassName` 等，故除 Controller `getUnassignedTasks→Result<List<ScheduleUnassignedTaskVo>>` 外，风险服务的变量/入参/循环变量也同步改 VO 类型（仅类型替换、读取逻辑零改动，两文件 entity import 换 VO import）。JSON 字段名/类型不变、前端零改动（`schedulePlan.ts:63-75` 三字段可选）。view 字段命中数累计 43→39、承载 Entity 9→7。新增 `M16UnassignedTaskVoSerializationTest`（11 字段集 + 三字段填充态/null 态，2 用例）、改 `M16TableFieldViewFieldsInvestigationTest` 计数（42→39、8→7、删 ScheduleUnassignedTask 计数断言）；`V4ScheduleRiskServiceTest`/`V4ScheduleReplanServiceTest` 零回归。另侦察发现 `SchedulePlan` 两 view 字段（semesterName/strategyName）全库无 setter、恒 null（疑似 `requiredSlots` 同类漏填 bug），留后续单独查。调查见 `20260603_M16_第2批_ScheduleUnassignedTask收口.md`。
- 2026-06-03（分支 `fix/m16-batch2-vo-deleted-field`）：修正第2批的契约偏差 + 确立 M-16 `deleted` 规则。第3批选型侦察时实测 `application.yml` 无 `spring.jackson` 全局配置、Entity 的 `@TableLogic deleted` 被 Jackson 序列化为 `0`（M14Phase3 测试断言 null 键存在亦佐证无全局 NON_NULL），即历史 `getUnassignedTasks` 响应**确含** `deleted:0`；而第2批 `ScheduleUnassignedTaskVo` 漏掉了它（报告原称「不下发」属误判）。拍板「**`deleted` 进 VO（严格逐字段）**」：VO 须镜像 Entity 当前序列化的全部字段（含 `deleted`/时间戳）以保 JSON 逐字节不变。本 fix 给 `ScheduleUnassignedTaskVo` 补 `deleted`（11→12 字段）、`toVo` 补 `setDeleted`、序列化测试加 `deleted=0` 断言，并订正第2批报告。规则对剩余各批同样适用（首批 `ScheduleConflictReport` 无 `@TableLogic`、不受影响）。
- 2026-06-03（分支 `refactor/m16-batch3-teacher-unavailable-vo`）：`M-16` 第3批收口（按真实难度选最干净的无 alias 实体：3 端点同 controller/service、view 字段零内部消费方、无嵌套-in-VO）。`TeacherUnavailableTime` 删 5 个 `@TableField(exist=false)` view 字段 teacherName/department/timeSlotName/dayOfWeek/periodNo（`@TableField` import 保留——create_time/update_time 用），新建 `TeacherUnavailableTimeVo`（14 字段=9 持久化〔含 `deleted` 恒 0/`createTime`/`updateTime`，首次落地「deleted 进 VO」规则〕+5 view、普通 POJO 保留 null）。**本批扩展范式：同一 Entity 多端点全填充 view 字段** → `TeacherUnavailableTimeService` 的 `list`/`create`/`update` 三方法都在返回前 `fillRelationFields` 填 5 字段，故三者统一转 VO：`list` 建 VO 分页（保留 teacherName 过滤的空分页早返回）、`create`/`update` 改 `toVo→fillRelationFields(singletonList)→返回 Vo`，`fillRelationFields` 改吃 `List<Vo>`、新增 `toVo` 拷 9 持久化列；Controller 三端点返回 `Result<...Vo>`、entity import 换 VO import。改面**全收敛在该 service+controller**，零 V4/V5/其它 VO 波及（`list/create/update` 无内部调用方、view 字段零内部读取方，其余注入方只调 `isUnavailable` boolean）；JSON 字段名/类型不变、前端零改动（`teacherUnavailableTime.ts` 不声明 `deleted`/`updateTime`、忽略多发字段）。view 字段命中数累计 39→34、承载 Entity 7→6。新增 `M16UnavailableTimeVoSerializationTest`（14 字段集 + view 填充/null 两态 + deleted=0，2 用例）、改 `M16TableFieldViewFieldsInvestigationTest` 计数（39→34、7→6、删 TeacherUnavailableTime 计数断言）；`TeacherUnavailableTimeServiceTest`/`ControllerPaginationValidationTest` 零回归。调查见 `20260603_M16_第3批_TeacherUnavailableTime收口.md`。

| 分类 | 报告宣称数量 | 可识别明细数量 | 结论 |
|---|---:|---:|---|
| Critical | 25 | 25 | 可逐条核验 |
| Major | 53 | 53 | 可逐条核验 |
| Minor | 59 | 47 | 缺少 `N-48` 至 `N-59` 共 12 条明细 |
| 合计 | 137 | 125 | 原报告多计 12 项 |

优先处理结论：

| 优先级 | 条目 | 验证结论 | 直接证据 |
|---|---|---|---|
| P0 | C-01 | 已修复 | `4c64595` 增加空值校验，空原因改为业务异常。 |
| P0 | C-10 | 已修复 | `07c0132` 修复候选位置容量校验，班级人数不再固定为 0。 |
| P0 | C-11 | 已修复 | `6fac914` 评分报告按学期隔离排课数据。 |
| P1 | C-15 | 已修复，已复核 | `10572d8` 对报告 HTML 输出字段做转义；本轮针对 Claude 复核意见确认 HTML 输出路径仍存在且 `safeHtml()` 覆盖方案名、质量等级、优化建议，补 `20260529_C15_HTML报告转义复核.md` 与优化建议断言。 |
| P1 | C-17 | 已修复 | `424b1cb` 增加管理员写保护；读接口保持登录态访问。 |
| P1 | C-22 | 已修复 | `7229421` 补齐缺失 V4/V5 表初始化脚本。 |
| P2 | C-20 | 不成立 | `v2_schema.sql` 与 `v6_bugfix_constraints.sql` 均使用 `ELSE NULL` |
| P2 | M-39 | 当前不存在 | 当前源码未检出 `.setDeleted(1)` |

## 3. Critical 条目验证

| 编号 | 状态 | 当前证据与判定 |
|---|---|---|
| C-01 | 已修复 | `4c64595`：`SchedulePlanService.adjustPlanItem()` 对 `adjustReason` 增加非空校验，避免 NPE。 |
| C-02 | 已修复 | `4c64595`：`ScheduleConflictService.checkConflict()` 改为安全比较，并对容量/人数缺失返回业务冲突原因。 |
| C-03 | 已修复 | `4c64595`：`ScheduleConflictService.checkConflict()` 对教师/班级/教室等比较改为 `Objects.equals(...)`。 |
| C-04 | 已修复 | `cc9a472`：`V4ScheduleSourceService.resolveSemester()` 对指定 `termId` 和当前学期缺失均抛业务异常；新增 `V4ScheduleSourceServiceTest`。 |
| C-05 | 已修复 | `4c64595`：`V5RepairTaskFlowService.createTask()` 对 `taskType` 增加空值校验。 |
| C-06 | 已修复 | `4c64595`：`V5Stage1DataService.createRepairTask()` 对 `taskCode`、`taskType` 增加空值校验。 |
| C-07 | 部分成立 | `AutoScheduleService.java:51` 存在入口级 `@Transactional`；事务过长或超时需用大批量数据实测。 |
| C-08 | 部分成立 | `V3ScheduleGenerateService.java:122-123` 的 `generateMultiple` 仍为事务方法；持续时间影响未实测。 |
| C-09 | 当前不存在，已验证 | 当前 `V4ScheduleLockService` 使用 `TransactionTemplate`，未检出报告所述 `synchronized` 锁释放顺序代码；并发重复锁定由 `schedule_locked_item` 活动锁唯一约束兜底，本轮补 `20260529_C09_锁事务顺序调查.md` 与 `C09LockTransactionOrderInvestigationTest`。 |
| C-10 | 已修复 | `07c0132`：候选位置容量校验改为读取真实班级人数，并补 `V5CandidatePositionServiceTest`。 |
| C-11 | 已修复 | `6fac914`：评分报告按学期过滤排课数据，避免跨学期污染。 |
| C-12 | 已修复 | `6fac914`：`V5RepairTaskStatus` 增加 `PENDING`，V5 修复任务初始状态对齐。 |
| C-13 | 已验证，暂不修复 | `V1__baseline.sql:153` 为旧写法 `ELSE id`，后续 `v2_schema.sql`/`v6_bugfix_constraints.sql` 已统一到 `active_key`；本轮补 `20260528_C13_baseline_unique_key_mismatch调查.md` 与 `DatabaseSchemaScriptTest` 回归。 |
| C-14 | 已修复 | `cc9a472`：正式课表锁定项将大节 `periodNo` 转换为小节范围，例如 `2 -> 3-4`；补充 `V4ScheduleLockServiceTest` 覆盖。 |
| C-15 | 已修复 | `10572d8`：报告 HTML 输出字段统一转义，阻断脚本注入展示。 |
| C-16 | 已修复 | `10572d8`：报告下载路径限制到报告目录，拒绝越界路径。 |
| C-17 | 已修复 | `424b1cb`：`AuthInterceptor` 增加管理员写保护，`POST/PUT/PATCH/DELETE` 默认要求 `ADMIN`。 |
| C-18 | 不成立 | `LoginResponse.token` 的 `@JsonIgnore` 事实存在，但项目采用 httpOnly cookie 时不向响应体返回 token 是接口设计，不构成 Critical 漏洞。 |
| C-19 | 已修复 | `4fd90a6`：`v3_semester_data_bind.sql` 与 `v2_alter_score_report.sql` 改为 `information_schema` 检查后幂等补列，补 `DatabaseSchemaScriptTest` 回归。 |
| C-20 | 不成立 | `v2_schema.sql:19` 与 `v6_bugfix_constraints.sql:16` 均为 `CASE WHEN deleted = 0 THEN 0 ELSE NULL END`；不一致实际位于 `V1__baseline.sql`。 |
| C-21 | 已修复 | `7229421`：`schedule` 唯一约束改用 `active_key`，允许软删除历史记录共存。 |
| C-22 | 已修复 | `7229421`：新增缺失表初始化脚本并注册到 `application.yml`。 |
| C-23 | 已修复 | `fix/frontend-plan-id-guard`：路由层统一校验 `id`、`taskId`、`planId` 为正整数，非法参数回到对应列表或父页面，避免详情页以 `NaN` 调接口。 |
| C-24 | 已修复 | 前端复制逻辑改为复用 `frontend/src/utils/clipboard.ts` 的 `copyText()`，移除 2 处 `document.execCommand('copy')` fallback；本轮补 `20260529_C24_Clipboard复制API调查.md`。 |
| C-25 | 已修复 | 9 处 `formRef = ref()` 已补齐 `FormInstance` 类型；本轮补 `20260529_C25_表单Ref类型调查.md` 并通过前端构建验证。 |

## 4. Major 条目验证

| 编号 | 状态 | 当前证据与判定 |
|---|---|---|
| M-01 | 已修复 | `8d6c063`：`V5RepairSuggestionService` 解析建议明细后统一 `selectBatchIds` 加载教室名称。 |
| M-02 | 已修复 | `8d6c063`：`V5SimulationService` 在 `buildCompare()` 中统一批量加载教室名称，变更明细和利用率共用名称 Map。 |
| M-03 | 已修复 | `ScheduleConflictService.checkConflict()` 改用 `TeachingTaskMapper.selectConflictCheckById()`，一次 JOIN 加载课程/教师/班级字段。 |
| M-04 | 已修复 | `UnscheduledTaskService.list()` 统一调用 `UnscheduledTaskMapper.selectFilteredPage()`，关联名称筛选和分页均下推数据库。 |
| M-05 | 已验证，暂不修复 | `SchedulePlanService.refreshPlanConflictState()` 已批量加载关联数据，且只更新冲突状态变化的明细；本轮补 `20260528_M05冲突状态刷新复杂度调查.md` 与大样本无变化回归测试。 |
| M-06 | 已验证，暂不修复 | `f51f206`：复核发现 `checkHardConflicts()` 已按星期分组、节次排序并使用 active 窗口剪枝；补 `V5ConsistencyCheckServiceTest` 覆盖无重叠、硬冲突和 200 条高重叠输入。 |
| M-07 | 已修复 | `V4ScheduleRiskService.buildContext()` 复用已加载的 `slots` 列表计算 `totalTimeSlots`，不再额外 `selectCount`。 |
| M-08 | 已收敛 | `5e7a78c`：保留课表包含搜索语义，新增 `idx_schedule_semester_deleted_created` 优化默认学期列表过滤/排序；若改为前缀/全文搜索仍需产品确认。 |
| M-09 | 已修复 | `bd847e3`：报告生成移除长事务边界，文件生成不再包在事务方法内。 |
| M-10 | 已修复 | `bd847e3`：V5 试算生成/局部重排只用事务包裹写入流程，成功后的详情构建移出事务。 |
| M-11 | 已修复 | `bd847e3`：报告目录支持 `paike.report.dir` 配置，不再硬编码 `data/reports`。 |
| M-12 | 不成立 | `ScheduleMapper.xml` 的 JOIN 被过滤条件和查询条件使用，并非“JOIN 结果未利用”；是否优化需以 SQL 执行计划判断。 |
| M-13 | 已修复 | 新增 `ScheduleService`、`TeachingTaskService`、`TimeSlotService`，`ScheduleController`、`TeachingTaskController`、`TimeSlotController` 改为服务委派；当前 Controller 目录不再检出 `private final *Mapper` 字段。本轮补 `20260602_M13_Controller服务边界收口.md` 并更新 `M13ControllerMapperInjectionInvestigationTest`。 |
| M-14 | 已修复 | 按 `20260602_M14_MapStringObject风险评估.md` §4.1 分阶段推进，三阶段全部收口，`Map<String,Object>` 计数 66→0 处、16→0 文件、公开端点契约 18→0，**M-14 全量归零**。**阶段1（2026-06-03，分支 `refactor/m14-mapstringobject-dto-ify`）**：6 个低风险端点（health/logout/checkConflict/getScoreSummary/rescore/getUnassignedSummary）从 `Map<String,Object>` 机械替换为 6 个新 VO（HealthInfoVo/LogoutResultVo/ConflictCheckResultVo/ScoreSummaryVo/RescoreResultVo/UnassignedSummaryVo），JSON 字段名/类型逐一不变、前端零改动；顺带消除 `getScoreSummary`/`rescore` 用 `Map.of` 在 null 值时抛 NPE 的隐患；`getScoreSummary` 的 conflictCount 契约不一致维持现状（5 字段、不补发，已确认）。计数 66→57 处、16→11 文件、公开端点契约 18→12。新增 `M14Phase1VoSerializationTest`（6 用例锁定 JSON 输出），更新 `M14MapStringObjectUsageInvestigationTest` 棘轮基线，详见 `20260603_M14_阶段1_低风险端点收口.md`。**阶段2（2026-06-03，分支 `refactor/m14-phase2-apply-compare-vo`）**：apply/adjust/rollback/compare 4 个端点改 VO（新增 `AdjustPlanResultVo`/`ApplyPlanResultVo`/`ComparePlanVo`/`CompareResultVo`），同步 `V4ScheduleAdjustmentService`/`V5SimulationService`/`applyPlanWithAudit` 三处内部调用方的 `result.get(...)`→getter；apply 与 rollback 合用一个 `ApplyPlanResultVo`（message 用 `@JsonInclude(NON_NULL)` 兼容 rollback 短路分支的第 5 字段）；`ScheduleCompareService.compare` 内部比较器/summary 重写为 VO getter（`nullSafeInt` 保留原 `getOrDefault(...,0)` 语义）。计数 57→32 处、11→4 文件、公开端点契约 12→7。新增 `M14Phase2VoSerializationTest`（5 用例），详见 `20260603_M14_阶段2_apply对比收口.md`。**阶段3（2026-06-03，分支 `refactor/m14-phase3-statistics-vo`）**：统计聚合 5 端点 + 2 个活 refresh 端点改 7 个新 VO（`TeacherWorkloadVo`/`ClassroomUtilizationVo`/`ClassBalanceVo`/`PlanOverviewVo`/`DashboardStatsVo`/`RefreshPlanSummaryVo`/`RefreshPlanRisksVo`），`ScheduleStatisticsService` 嵌套 `Map<Long,Map<String,Object>>` 累加改强类型累加器、删 `longSet`/`integerLongMap` 强转（零 unchecked），`loadItems()` 多态保留 `instanceof`（不动 Entity）；调查发现并保留 2 处前端不读但历史输出的字段以维持 JSON 逐字节不变（`teacherWorkload.maxContinuousPeriods` 恒 0、`classBalance.dailyPeriods` 动态键，故 VO 实为 10/12 字段而非蓝图记的 9/11），`PlanOverviewVo` 用普通 POJO 让 best/applied 块恒序列化 null（对齐前端 `number|null`、避免 NON_NULL 在子字段 null 时误省略）；计数 32→0 处、4→0 文件、公开端点契约 7→0。新增 `M14Phase3VoSerializationTest`（10 用例），计数测试 `M14MapStringObjectUsageInvestigationTest` 改写为归零守卫，详见 `20260603_M14_阶段3_统计聚合VO收口.md`。共新增 17 个 VO。另见 `20260529_M14_MapStringObject使用调查.md`、`20260602_M14_MapStringObject风险评估.md`（gpt-5.5 评语勘误：死代码仅 V3 `adjust`、两个 refresh 为活端点，`PlanOverview` 17 字段）。 |
| M-15 | 已修复 | 新增 `TimetableService` 承接课表查询、VO 装配和 Excel 导出；`TimetableController` 只保留 6 个路由入口并委派服务。本轮补 `20260602_M15_课表导出职责收口.md`、更新 `M15TimetableControllerExportComplexityInvestigationTest` 与 `M13ControllerMapperInjectionInvestigationTest`。 |
| M-16 | 部分修复（第1+2+3批已收口，余 6 批） | 当前共 9 个 Entity 检出真实 `@TableField(exist = false)` 视图字段。**第1批（2026-06-03，分支 `refactor/m16-batch1-conflict-report-vo`）**：`ScheduleConflictReport` 删唯一 view 字段 `timeSlotName`（连同 `@TableField` import），新建 `ScheduleConflictReportVo`（13 字段=12 持久化列+timeSlotName）承载下发，`list()` 转 VO、`fillRelationFields`/`extractDayOfWeekFromRelatedSchedules` 改吃 VO，Controller 返回 `Result<Page<ScheduleConflictReportVo>>`；`generate()` 持久化路径不动，JSON 字段名/类型不变、前端零改动。确立「删字段+VO+改下游+测试归零」五层范式，调查见 `20260603_M16_第1批_ScheduleConflictReport收口.md`。**第2批（2026-06-03，分支 `refactor/m16-batch2-unassigned-task-vo`）**：`ScheduleUnassignedTask` 删 3 个 view 字段 courseName/teacherName/className（`@TableField` import 保留，`created_at` 仍用），新建 `ScheduleUnassignedTaskVo`（12 字段，含 `deleted`），`SchedulePlanExplainService.listUnassignedTasks` 转 VO（`toVo`+`fillUnassignedRelations` 改吃 VO）；本批新增「**view 字段有内部 Java 消费方**」一层——除 Controller `getUnassignedTasks→Result<List<...Vo>>` 外，`V4ScheduleRiskService.detectUnscheduledTasks` 内部读三字段也同步改吃 VO（仅类型、逻辑不动）；前端零改动。view 字段命中数累计 43→39、承载 Entity 9→7。调查见 `20260603_M16_第2批_ScheduleUnassignedTask收口.md`。**第3批（2026-06-03，分支 `refactor/m16-batch3-teacher-unavailable-vo`）**：`TeacherUnavailableTime` 删 5 个 view 字段 teacherName/department/timeSlotName/dayOfWeek/periodNo，新建 `TeacherUnavailableTimeVo`（14 字段，含 `deleted`/`createTime`/`updateTime`）；本批新增「**同一 Entity 多端点全填充 → 三端点统一转 VO**」一层——`TeacherUnavailableTimeService` 的 `list`/`create`/`update` 都填 view 字段，故三者均改返回 `...Vo`（`fillRelationFields` 改吃 VO、新增 `toVo`），Controller 三端点返回 `Result<...Vo>`；改面全收敛在该 service+controller，零 V4/V5 波及、前端零改动。view 字段命中数累计 39→34、承载 Entity 7→6。调查见 `20260603_M16_第3批_TeacherUnavailableTime收口.md`。余 6 个 Entity（SchedulePlan/UnscheduledTask/TeachingTask/SchedulePlanItem/ScheduleAdjustLog/Schedule，含 Mapper alias 与深耦合；`SchedulePlan` 两 view 字段恒 null 疑漏填，`ScheduleAdjustLog` 含内部消费方+嵌套-in-VO，待按难度后续分批）。另见 `20260602_M16_EntityViewField风险评估.md`（含 gpt-5.5 评语勘误：`TeachingTask` 无 `requiredSlots`、22 端点均 `Result<...>` 包裹）。 |
| M-17 | 已修复 | `0b98907`：`selectFilteredTaskIds` 更名为 `selectFilteredTasks`，Mapper 接口、XML 和调用方契约一致。 |
| M-18 | 已修复 | `0b98907`：`selectDailyConflictCounts` 改为明确 DTO `ScheduleDailyConflictCounts`，移除 `resultType="HashMap"`。 |
| M-19 | 已修复 | `2e7c9f8`：新增 `StringSanitizer.trimToNull`，替换 5 个 Service 和 `ScheduleController` 内重复私有方法；新增 `StringSanitizerTest`。 |
| M-20 | 已整理 | `frontend/src/api/index.ts` 已汇总基础 API 与 v4/v5 API；`v4ScheduleAnalysisApi`、`v5SimulationApi` 因类型名冲突改用命名空间导出，本轮补 `20260529_M20_APIBarrel命名冲突收口.md`。 |
| M-21 | 已整理 | 新增 `frontend/src/utils/async.ts` 统一 `fallback<T>()`，5 个页面改为复用公共 helper。 |
| M-22 | 已整理 | `utils/status.ts` 补排课方案状态和 V5 修复任务状态映射，移除多处重复局部 `statusText`/`statusTagType`。 |
| M-23 | 已整理 | 新增 `frontend/src/utils/download.ts` 统一文件名解析和浏览器下载触发，`timetable.ts` 与 `v4ScheduleReportApi.ts` 复用。 |
| M-24 | 已修复 | 移除前端遗留 `token` store、`setToken`/`clearToken` 和 `TOKEN_KEY` 占位；当前认证完全依赖 `paike_token` httpOnly Cookie + `userInfo`。 |
| M-25 | 部分成立，已复核收口 | 项目同时存在 `create_time/update_time` 与 `created_at/updated_at` 两套时间字段，但未见单表内混用；已补齐 `auto_schedule_batch` 基线建表中的 `update_time`，保留 v9 幂等补列，并补强 `M25TimestampNamingInvestigationTest` 验证实体显式时间字段映射均能匹配建表脚本。另见 `20260602_M25_时间字段命名风险评估.md`（2026-06-03 归档于 `24d8542`，含实测计数对照表与 gpt-5.5 评语核验：3 个 Mapper 仍按 `create_time` 排序、legacy/at 两套声明各 18/19 处）。 |
| M-26 | 不成立 | MyBatis-Plus 的约定映射允许字段不显式声明 `@TableField`；该项仅为风格偏好。 |
| M-27 | 已收敛 | 远程 AI 调用仍仅两处；本轮按调查文档预设蓝图抽出 `AiChatProperties`（集中 `app.ai.*`）与 `RemoteAiChatClient`（HTTP envelope/鉴权/超时/content 提取/`extractJson`/`sanitizeForPrompt`），`V4ScheduleAiAnalysisService` 与 `V5RepairExplanationService` 去重后只保留各自 prompt 组装、业务 JSON schema 解析与本地兜底（行为等价，仅内部日志 message 统一）。分支 `refactor/m27-ai-client-consolidation`，详见 `20260529_M27_AI客户端复用调查.md` 与 `20260603_M27_AI客户端收敛.md`，测试由 `M27AiHttpClientDuplicationInvestigationTest` 改写为 `M27AiClientConsolidationTest`（3 tests 通过）。 |
| M-28 | 已修复 | 默认 JDBC URL 与 `.env.example` 改为 Java 编码名 `characterEncoding=UTF-8`；本轮补 `20260529_M28_M29数据库字符集调查.md` 与 `M28M29DatabaseCharsetInvestigationTest`。 |
| M-29 | 已修复 | 50 个 `CREATE TABLE` 语句补 `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`，新库初始化不再依赖实例默认字符集。 |
| M-30 | 已修复 | 移除未启用的 `flyway-mysql` 依赖和 `spring.flyway.enabled:false` 配置段；当前明确走 `spring.sql.init + SemesterSchemaInitializer`，本轮补 `20260529_M30_Flyway初始化策略调查.md` 与 `M30FlywayInitializationStrategyInvestigationTest`。 |
| M-31 | 部分成立，暂不修复 | 当前检出 15 个映射实体未包含 `deleted` 字段，但对应 SQL 表也没有 `deleted` 列；本轮补 `20260529_M31逻辑删除实体覆盖调查.md` 与 `M31LogicDeleteEntityCoverageInvestigationTest` 锁定现状。 |
| M-32 | 部分成立，暂不修复 | SQL 脚本未检出 `FOREIGN KEY` 或 `REFERENCES`，但当前关系通过业务层校验、软删除和索引/唯一约束表达；直接补外键会牵涉既有数据、迁移顺序和级联/软删除语义。本轮补 `20260529_M32外键约束调查.md` 与 `M32ForeignKeyConstraintInvestigationTest` 锁定现状。 |
| M-33 | 已修复 | `c604af0`：`.env.example` 去掉 `root/root` 示例，改为本地低权限账号和强密钥占位说明。 |
| M-34 | 已修复 | `vite.config.ts` 默认绑定 `127.0.0.1`，仅在本地设置 `DEV_SERVER_HOST=0.0.0.0` 时开放局域网调试；本轮补 `20260529_M34_Vite开发主机绑定调查.md`。 |
| M-35 | 已修复 | `schedule_conflict_report.related_schedule_ids` 从 `VARCHAR(255)` 放宽为 `TEXT`；本轮补 `v20_schedule_conflict_related_ids_text.sql` 迁移、同步 baseline/v2 文档，并新增 `20260529_M35关联排课ID字段长度调查.md` 与 `M35RelatedScheduleIdsColumnLengthTest`。 |
| M-36 | 已修复 | `v3_score.sql` 默认权重仍按当前学期初始化；本轮在 `v3_semester.sql` 与 `V1__baseline.sql` 增加“无当前学期则提升最新未删除学期”的兜底，补 `20260529_M36默认评分权重初始化调查.md` 与 `M36ScoreDefaultWeightInitializationTest`。 |
| M-37 | 已修复 | `424b1cb`：`SecurityHeadersFilter` 增加 `Content-Security-Policy`。 |
| M-38 | 不成立（已有写入失效） | `ScheduleRuleService` 确实使用 `ConcurrentHashMap` 缓存，但 `updateRules()` / `resetToDefault()` 均会 `ruleCache.clear()`；本轮补 `20260529_M38规则缓存失效调查.md` 与 `ScheduleRuleServiceTest` 锁定行为。 |
| M-39 | 已修复 | 未检出 `.setDeleted(1)` 字面调用，但 `SchedulePlanService` 存在 `LambdaUpdateWrapper.set(Schedule::getDeleted, 1)` 手动软删除；本轮改为 `scheduleMapper.delete(...)` 走 `@TableLogic`，并补 `20260529_M39手动软删除调查.md` 与 `M39ManualDeletedUpdateInvestigationTest`。 |
| M-40 | 已修复 | `fix/deleted-flag-comparisons`：清空主代码中 `getDeleted() == 1`/`!= 1` 风险写法，保留业务存在性判断语义。 |
| M-41 | 已整理 | 清理 Java `LambdaQueryWrapper` 侧 66 处冗余 `.eq(...::getDeleted, 0)`，保留 XML join 与 SQL 脚本中的必要 `deleted = 0`；本轮补 `20260529_M41显式Deleted条件整理.md` 与 `M41DeletedZeroConditionCleanupTest`。 |
| M-42 | 已修复 | `fix/autoschedule-batch-no-collision`：删除 `V3ScheduleGenerateService` 固定为 `true` 的评分开关和不可达 legacy 评分分支；新增 `V3ScheduleGenerateServiceTest` 防回归。 |
| M-43 | 不成立，已验证 | `discard()` 先置 `DISCARDED` 再 `detail()` 不会导致详情失败：`requireSimulationPlan()` 允许 `DISCARDED`，且详情快照在清理明细前生成；本轮补 `20260529_M43试算放弃顺序调查.md` 与 `M43SimulationDiscardOrderInvestigationTest`。 |
| M-44 | 已修复 | `4c64595`：`V4ScheduleAdjustmentService` 预览路径给空原因设置默认值，应用路径保持非空校验。 |
| M-45 | 已修复 | `investigate/audit-log-failure-events`：`SystemAuditLogService` 新增独立事务 `recordFailure()`；`SchedulePlanService.applyPlanWithAudit()` 在应用/回滚失败时记录失败审计且不掩盖原异常；新增服务测试覆盖。 |
| M-46 | 已修复 | `2841a4e`：`TimeSlotController` 与同类 `AutoScheduleBatchController` 的详情不存在分支改抛 `BusinessException(404, ...)`，由全局异常处理返回真实 HTTP 404；新增 `ControllerNotFoundStatusTest`。 |
| M-47 | 已修复 | `4dd3ec7`：15 个分页列表 Controller 增加类级 `@Validated`，并为默认分页参数补齐 `@Min(1)` / `@Max(200)`；新增 `ControllerPaginationValidationTest` 锁定约束覆盖。 |
| M-48 | 已修复 | `f150760`：基础资料和教学任务请求表单按数据库 `VARCHAR` 长度补齐 `@Size`；新增 `ControllerRequestStringValidationTest` 锁定约束覆盖。 |
| M-49 | 不成立，已验证 | `weight` 缺少 `@NotNull` 是部分更新契约：后端只更新非空字段，前端 `weight?: number`；本轮补 `20260529_M49_M51契约收口调查.md` 与 `M49M51ContractClosureTest`。 |
| M-50 | 不成立（契约设计） | 绝大多数候选端点把 `request == null` 作为默认契约处理；本轮补 `20260528_M50_request_body_optionality调查.md` 与 `M50RequestBodyOptionalityInvestigationTest`。 |
| M-51 | 不成立，已验证 | 未配置 `ADMIN_DEFAULT_PASSWORD` 时 stdout 是随机初始密码交付渠道；配置默认密码时不输出随机密码提示，本轮补 `20260529_M49_M51契约收口调查.md` 与 `M49M51ContractClosureTest`。 |
| M-52 | 部分成立，暂不修复 | `RateLimiterService` 确实使用进程内 `ConcurrentHashMap`，多实例部署下限流窗口不全局一致；但当前仅用于登录双键限流，已有过期空 key 清理，项目也未引入 Redis 基础设施。本轮补 `20260529_M52限流器生产可靠性调查.md` 与 `M52RateLimiterServiceInvestigationTest` 锁定现状。 |
| M-53 | 不成立 | 检出 2 个 DELETE 端点使用 Query Param；这属于 API 风格选择，不自动构成 bug。 |

## 5. Minor 条目验证

| 编号 | 状态 | 当前证据与判定 |
|---|---|---|
| N-01 | 已修复 | `fix/deleted-flag-comparisons`：剩余 `getDeleted() == 1` 改为 `Integer.valueOf(1).equals(...)`，避免拆箱空值风险；新增 `DeletedFlagComparisonTest` 防回归。 |
| N-02 | 已修复 | `fix/autoschedule-batch-no-collision`：`TeacherUnavailableTimeService` 将 create/update 共享的教师、时间段、重复禁排校验收口到私有方法；新增 `TeacherUnavailableTimeServiceTest` 覆盖重复校验行为。 |
| N-03 | 已修复 | `fix/autoschedule-batch-no-collision`：`AutoScheduleService.categorizeReason()` 改为只读取 `ScheduleConflictService` 的结构化 `[TYPE]` 标签，不再按中文文案 contains 分类；新增 `AutoScheduleServiceTest` 防回归。 |
| N-04 | 已修复 | `cc9a472`：`ScheduleRuleWeightService.updateWeight()` 补 `@Transactional(rollbackFor = Exception.class)`；新增 `ServiceTransactionAnnotationTest`。 |
| N-05 | 已修复 | `cc9a472`：`TeacherUnavailableTimeService.delete()` 补 `@Transactional(rollbackFor = Exception.class)`；新增 `ServiceTransactionAnnotationTest`。 |
| N-06 | 当前不存在 | `ScheduleScoreReportService` 已定义 `DEDUCT_PER_*` 和 `FULL_SCORE` 常量，原“扣分魔法值”问题已处理。 |
| N-07 | 已修复 | `fix/autoschedule-batch-no-collision`：利用已有 `uk_batch_no` 唯一约束，批次插入遇到 `DuplicateKeyException` 时有限重试，耗尽后抛业务异常；新增 `AutoScheduleBatchServiceTest` 覆盖碰撞重试和耗尽失败。 |
| N-08 | 不成立 | `Collectors.toList()` 与 `List.of()` 用途不同，混用本身不是缺陷。 |
| N-09 | 已修复 | `fix/schedule-plan-status-enum`：本轮仅收口 `SchedulePlan.status`，新增 `SchedulePlanStatus` 枚举并替换后端方案状态比较/赋值；V5 repair task/suggestion 状态、`sourceType`、`planMode` 保持不变。 |
| N-10 | 已修复 | `fix/remove-v5-repair-keepobjects`：删除 `V5RepairExplanationService` 中仅用于避免 unused 警告的 `__keepObjects()` 占位方法，并移除 `Objects` import。 |
| N-11 | 已修复 | `investigate/v4-replan-keep-locked`：保留 `keepLocked=false` 输入拒绝契约，收口 `V4ScheduleReplanService` 内部恒 true 分支；局部重排无条件复制锁定项，结果固定 `keepLocked=true`；新增 `V4ScheduleReplanServiceTest` 覆盖 false 拒绝和默认复制锁定项。 |
| N-12 | 无法验证 | 日志是否应保留非 ERROR 内容属于产品/审计要求，原报告未给出期望行为。 |
| N-13 | 已修复 | `fix/N-13-enum-code-conversion`：补齐 `V5RepairTaskStatus`、`V5SuggestionStatus` 的 `fromCode()`/`is()`，为修复任务状态增加 `isTerminal()`；替换 V5 修复任务/建议服务中的任务状态字符串判断；新增 `V5StatusEnumConversionTest` 和任务状态更新回归测试。 |
| N-14 | 已修复 | `fix/N-14-v5-task-status-trailing-comma`：移除 `V5RepairTaskStatus` 枚举末项后的多余逗号。 |
| N-15 | 部分成立 | `TeacherUnavailableTime` 使用 Jakarta Validation 的事实可见；是否应扩展到其他 Entity 不是 bug 结论。 |
| N-16 | 已修复 | `investigate/N-16-report-semester-fields`：复核发现 `ScheduleScoreReport` 已由 v13 修复；本轮补齐 `ScheduleConflictReport`、`AutoScheduleBatch` 的 `semesterId` 字段、生成写入、列表过滤、前端 API 类型和 `v17_report_batch_semester.sql` 幂等迁移。 |
| N-17 | 已修复 | `investigate/N-17-schedule-report-semester-deleted`：补齐 `ScheduleReport.semesterId` 和 `@TableLogic deleted`，生成报告时继承方案学期；更新 VO/前端 API 类型、v14 建表、启动兜底初始化和 `v18_schedule_report_semester_deleted.sql` 幂等迁移。 |
| N-18 | 部分成立 | 枚举形态存在差异属于一致性维护项，原报告未给出会导致错误的接口依赖。 |
| N-19 | 已修复 | `investigate/N-19-schedule-score-detail-deleted`：补齐 `ScheduleScoreDetail.@TableLogic deleted`，更新 `v3_score.sql`/baseline 建表、启动兜底初始化和 `v19_score_detail_deleted.sql` 幂等迁移；`rescore()` 旧明细删除改由逻辑删除过滤。 |
| N-20 | 部分成立 | Form/DTO 源码同时使用 Lombok `@Getter` 与 `@Data`；属于编码约定项。 |
| N-21 | 已修复 | 当前前端检出 18 个文件、29 处 `console.error`；除 `ErrorBoundary.vue` 这类页面级可见错误边界外，所有 catch 内 `console.error` 均已补 `ElMessage.error` 用户反馈。本轮补 `20260602_N21_前端错误反馈收口.md`。 |
| N-22 | 当前不存在 | 当前前端未检出显式 `any` 或 `as any` 模式，原具体断言不再成立。 |
| N-23 | 已修复 | `ScheduleCharts.vue` 的 tooltip formatter 和 ECharts 点击参数改用 `unknown` 守卫与 `ECElementEvent` 类型；V5 两处表格内联 `any` 改为具名 typed handler。本轮补 `20260602_N23_N31_ECharts类型收口.md`。 |
| N-24 | 已修复 | `useCrudForm.ts` 的 `tableData` 由 `as { value: T[] }` 改为规范的 `as Ref<T[]>`，保留 `Ref` 语义；分支 `refactor/n24-n30-usecrudform-types`，详见 `20260603_N24_N30_useCrudForm类型收口.md`，前端 `npm run build` 通过。 |
| N-25 | 不成立 | `ApiResponse<T>` 作为 TypeScript 静态类型本就不提供运行时校验；需要运行时 schema 时应另立需求。 |
| N-26 | 部分成立 | 命名一致性是维护建议，原条目没有可验证的功能错误。 |
| N-27 | 已修复 | `fix/frontend-error-feedback`：`ScheduleRuleWeightView.vue` 单项保存和批量保存失败时显示 `ElMessage.error`。 |
| N-28 | 已修复 | `fix/frontend-error-feedback`：`ScheduleStatisticsView.vue` 方案列表、概览、工作量、教室利用率、班级均衡度查询失败时显示错误提示。 |
| N-29 | 已修复 | `fix/frontend-error-feedback`：`AutoScheduleView.vue` 待排任务加载、自动排课、清空批次失败时显示错误提示。 |
| N-30 | 已修复 | `useCrudForm.ts` 增加第三个泛型 `S extends Record<string, unknown> = Record<string, unknown>` 表示搜索表单类型，`searchDefaults` / `searchForm` / `fetchList` 入参随之收紧；默认值保证现有调用法向后兼容（该 composable 当前无 View 调用）。分支 `refactor/n24-n30-usecrudform-types`，详见 `20260603_N24_N30_useCrudForm类型收口.md`。 |
| N-31 | 已修复 | 与 N-23 重叠处理；`EChartPanel.vue` 的 `chartClick` emit 改为 `ECElementEvent`，父组件点击处理函数不再使用 `any`。 |
| N-32 | 部分成立 | 未检出空 `catch {}`，但检出大量仅控制台记录的 catch；与 N-21/N-28/N-29 重复计数。 |
| N-33 | 无法验证 | 原报告仅写“散布”，未提供文件或标识符；前端构建通过不等同于无未使用变量。 |
| N-34 | 已修复 | `fix/schedule-compare-duplicate-plan-ids`：`ScheduleCompareService.compare()` 在加载方案前拒绝重复方案 ID；新增 `ScheduleCompareServiceTest` 覆盖重复 ID、少于 2 个方案、跨学期方案拒绝。 |
| N-35 | 当前不存在 | `ScheduleStatisticsService` 已注入 `SchedulePlanItemMapper`，且处理 `SchedulePlanItem` 字段。 |
| N-36 | 部分成立 | SQL 脚本同时存在 `weekday`/`day_of_week` 定义；注释含义与实际时段映射需以业务模型确认。 |
| N-37 | 不成立 | 使用 `INT` 或 `TINYINT` 是存储优化选择，不构成功能 bug。 |
| N-38 | 不成立 | BCrypt 哈希长度当前可被 `VARCHAR(100)` 容纳，“未来可能不足”不是现存缺陷。 |
| N-39 | 不成立 | 独立引入 `spring-security-crypto` 属依赖选择，不构成问题。 |
| N-40 | 无法验证 | 本次确认存在 baseline 与初始化脚本，但“内容完全重复”未由逐表语义比对证明。 |
| N-41 | 部分成立 | `application.yml` 存在排课配置项；配置默认值硬编码是可配置性建议。 |
| N-42 | 不成立 | 默认学期为 `2025-2026`；在验证日期 2026-05-27 尚不能直接认定已过期。 |
| N-43 | 已修复 | 新增 `RequestBodySizeLimitFilter` 按 `Content-Length` 拦截超大 JSON 请求体（过滤器在 DispatcherServlet 前执行，超限直接写统一信封 413）；阈值 `app.security.max-request-body-size`（默认 2MB）可配，`application.yml` 另补 `spring.servlet.multipart.*` 预置护栏。分支 `fix/n43-request-body-size-limit`，调查见 `20260603_N43_请求体大小限制调查.md`，`RequestBodySizeLimitFilterTest`（3 tests）通过。 |
| N-44 | 部分成立 | `GlobalExceptionHandler` 存在日志处理；是否缺少所需请求上下文需按日志合规要求定义。 |
| N-45 | 部分成立 | 两个 Controller 均存在路径映射；“路径重叠”是否冲突应按完整 method/path 映射判断。 |
| N-46 | 部分成立 | `ScheduleController` 以 POST 提供冲突检查；属于 REST 语义选择，不是运行故障。 |
| N-47 | 部分成立 | `TeachingTaskController` 涉及学期字段；是否遗漏跨实体学期校验需以创建用例复现。 |
| N-48 | 无法验证 | 原报告未提供该条明细。 |
| N-49 | 无法验证 | 原报告未提供该条明细。 |
| N-50 | 无法验证 | 原报告未提供该条明细。 |
| N-51 | 无法验证 | 原报告未提供该条明细。 |
| N-52 | 无法验证 | 原报告未提供该条明细。 |
| N-53 | 无法验证 | 原报告未提供该条明细。 |
| N-54 | 无法验证 | 原报告未提供该条明细。 |
| N-55 | 无法验证 | 原报告未提供该条明细。 |
| N-56 | 无法验证 | 原报告未提供该条明细。 |
| N-57 | 无法验证 | 原报告未提供该条明细。 |
| N-58 | 无法验证 | 原报告未提供该条明细。 |
| N-59 | 无法验证 | 原报告未提供该条明细。 |

## 6. 构建与测试验证

| 验证命令 | 结果 | 说明 |
|---|---|---|
| `cd D:\paike\frontend; npm run build` | 通过 | `vue-tsc -b && vite build` 成功；本轮 N-23/N-31 复测 Vite 于 7.70 秒完成构建。N-24/N-30 useCrudForm 泛型收口后复测，vue-tsc 0 error、Vite 20.40 秒完成。 |
| `cd D:\paike\backend; mvn test -DskipTests=false` | 历史未通过 | 初始验证时共执行 28 项测试：0 failures、1 error；`SchedulerBackendApplicationTests.contextLoads` 因环境未提供 `JWT_SECRET` 导致 ApplicationContext 启动失败。 |
| `cd D:\paike\backend; mvn test` | 环境阻塞 | 2026-05-27 复测时已设置 `JWT_SECRET`，但 Spring context 测试因 `${DB_USERNAME}` 未解析导致 MySQL `Access denied`；不是本轮代码编译失败。 |
| `cd D:\paike\backend; mvn -Dtest=!SchedulerBackendApplicationTests test` | 通过 | 151 tests，0 failures，0 errors；覆盖当前可脱离 DB context 的后端测试。 |
| `cd D:\paike\backend; mvn -Dtest=V5ClassroomBatchLoadingTest test` | 通过 | 2 tests，0 failures，0 errors；验证 M-01/M-02 教室名称查询收敛到批量加载。 |
| `cd D:\paike\backend; mvn -Dtest=ScheduleConflictServiceTest clean test` | 通过 | 4 tests，0 failures，0 errors；验证 M-03 冲突检查入口改用任务详情合并查询。 |
| `cd D:\paike\backend; mvn -Dtest=UnscheduledTaskServiceTest clean test` | 通过 | 3 tests，0 failures，0 errors；验证 M-04 关联筛选改用数据库分页。 |
| `cd D:\paike\backend; mvn -Dtest=UnscheduledTaskServiceTest,V4ScheduleRiskServiceTest test` | 通过 | 4 tests，0 failures，0 errors；验证 M-04/M-07 定向回归。 |
| `cd D:\paike\backend; mvn -Dtest=DatabaseSchemaScriptTest test` | 通过 | 10 tests，0 failures，0 errors；验证 C-19 遗留补列脚本已改为幂等注册。 |
| `cd D:\paike\backend; mvn -Dtest=V5ConsistencyCheckServiceTest test` | 通过 | 3 tests，0 failures，0 errors；验证 M-06 当前算法行为与高重叠基线。 |
| `cd D:\paike\backend; mvn -Dtest=SchedulePlanServiceTest test` | 通过 | 4 tests，0 failures，0 errors；验证 M-05 冲突状态刷新只更新变化明细，大样本无变化时跳过明细更新。 |
| `cd D:\paike\backend; mvn -Dtest=M15TimetableControllerExportComplexityInvestigationTest test` | 通过 | 1 test，0 failures，0 errors；验证 M-15 控制器职责已迁移到服务层。 |
| `cd D:\paike\backend; mvn -Dtest=M13ControllerMapperInjectionInvestigationTest test` | 通过 | 1 test，0 failures，0 errors；验证 M-13 Controller 层不再直接注入 Mapper。 |
| `cd D:\paike\backend; mvn -Dtest=RequestBodySizeLimitFilterTest test` | 通过 | 3 tests，0 failures，0 errors；验证 N-43 超限 413、阈值内放行、无 Content-Length 放行。 |
| `cd D:\paike\backend; mvn -Dtest=M27AiClientConsolidationTest,M50RequestBodyOptionalityInvestigationTest test` | 通过 | 4 tests，0 failures，0 errors；验证 M-27 远程 AI 客户端已收敛到 `RemoteAiChatClient`+`AiChatProperties`，且 M-50 V4 请求可选性契约未受影响。 |
| `cd D:\paike\backend; mvn -Dtest=M14Phase1VoSerializationTest,M14MapStringObjectUsageInvestigationTest test` | 通过 | 8 tests，0 failures，0 errors；验证 M-14 阶段1 的 6 个 VO 序列化 JSON 与历史 Map 输出逐字段一致，并锁定 `Map<String,Object>` 计数降至 57 处/11 文件（含全量 main 重新编译 263 文件）。 |
| `cd D:\paike\backend; mvn -Dtest=M14Phase2VoSerializationTest,M14MapStringObjectUsageInvestigationTest,M14Phase1VoSerializationTest,SchedulePlanServiceTest,ScheduleCompareServiceTest test` | 通过 | 20 tests，0 failures，0 errors；验证 M-14 阶段2 的 4 个 VO 序列化一致（含 ApplyPlanResultVo 的 message 省略/存在两态、CompareResultVo 嵌套），SchedulePlanService/ScheduleCompareService 现有测试零回归，并锁定 `Map<String,Object>` 计数降至 32 处/4 文件。 |
| `cd D:\paike\backend; mvn -Dtest=M14Phase3VoSerializationTest,M14MapStringObjectUsageInvestigationTest,M14Phase1VoSerializationTest,M14Phase2VoSerializationTest test` | 通过 | 23 tests，0 failures，0 errors；验证 M-14 阶段3 的 7 个统计/refresh VO 序列化与历史 Map 输出逐字段一致（含 dailyPeriods 动态键对象、courseCount 为 size 非 Set、PlanOverviewVo 17 字段有/无方案两态、DashboardStatsVo 嵌套），阶段1/2 VO 零回归，并将 `Map<String,Object>` 计数归零守卫锁定为 0 处/0 文件/公开端点契约 0（含全量 main 重新编译）。 |

| `cd D:\paike\backend; mvn "-Dtest=M16ConflictReportVoSerializationTest,M16TableFieldViewFieldsInvestigationTest" test` | 通过 | 6 tests，0 failures，0 errors（含全量 main 重新编译）；验证 M-16 第1批 `ScheduleConflictReportVo` 序列化的 13 字段集与 timeSlotName 三态（时段 label/「全周」/「-」）与历史输出一致、保留 null 序列化，并锁定 view 字段命中数降至 42 处、承载 Entity 降至 8 个（删 ScheduleConflictReport 计数断言）。 |
| `cd D:\paike\backend; mvn "-Dtest=ScheduleConflictReportServiceTest" test` | 通过 | 1 test，0 failures，0 errors；验证 M-16 第1批改造后 `generate()` 持久化路径零回归（语义/学期落库不变）。 |
| `cd D:\paike\backend; mvn "-Dtest=M16UnassignedTaskVoSerializationTest,M16TableFieldViewFieldsInvestigationTest,V4ScheduleReplanServiceTest" test` | 通过 | 6 tests，0 failures，0 errors（含全量 main 重新编译）；验证 M-16 第2批 `ScheduleUnassignedTaskVo` 序列化 12 字段集（含 `deleted`，见下方 fix 行）与三字段填充/null 两态、view 字段命中累计降至 39 处/承载 Entity 降至 7 个（删 ScheduleUnassignedTask 计数断言），并确认未排任务持久化路径零回归。 |
| `cd D:\paike\backend; mvn "-Dtest=V4ScheduleRiskServiceTest" test` | 通过 | 1 test，0 failures，0 errors；验证 M-16 第2批内部消费方 `V4ScheduleRiskService.detectUnscheduledTasks` 改吃 `ScheduleUnassignedTaskVo` 后零回归（仅类型替换、风险项组装逻辑不变）。 |
| `cd D:\paike\backend; mvn "-Dtest=M16UnassignedTaskVoSerializationTest" test` | 通过 | 2 tests，0 failures，0 errors；验证 `fix/m16-batch2-vo-deleted-field` 给 `ScheduleUnassignedTaskVo` 补 `deleted`（12 字段集 + `deleted=0` 断言）后 JSON 与历史逐字段一致，确立 M-16「deleted 进 VO」规则。 |
| `cd D:\paike\backend; mvn "-Dtest=M16UnavailableTimeVoSerializationTest,M16TableFieldViewFieldsInvestigationTest,TeacherUnavailableTimeServiceTest" test` | 通过 | 6 tests，0 failures，0 errors（含全量 main 重新编译）；验证 M-16 第3批 `TeacherUnavailableTimeVo` 序列化 14 字段集（含 `deleted=0`/createTime/updateTime）与 view 填充/null 两态、view 字段命中累计降至 34 处/承载 Entity 降至 6 个（删 TeacherUnavailableTime 计数断言），并确认 list/create/update 三端点转 VO 后服务校验路径零回归。 |
| `cd D:\paike\backend; mvn "-Dtest=ControllerPaginationValidationTest" test` | 通过 | 1 test，0 failures，0 errors；验证 M-16 第3批 `TeacherUnavailableTimeController` 三端点改返回 `Result<...Vo>` 后分页参数校验契约未受影响。 |

本次未启动 Spring Boot 服务，也未执行需要数据库和浏览器登录态的接口/端到端复现。因此，事务超时、权限越权路径、SQL 在目标 MySQL 实例中的重复执行结果，以及前端错误参数的实际请求行为，均按静态证据标注为“确认模式”或“部分成立”，未伪装为运行时复现。

## 7. 建议修复顺序

| 顺序 | 待处理项 | 原因 |
|---:|---|---|
| 1 | M-08 至 M-11 中尚未修复项 | 继续收敛性能和事务稳定性风险；M-05、M-06 已验证暂不改生产代码。 |
| 2 | C-04、C-14 等仍确认项 | C-19 已修复；其余非本轮已完成范围，后续按风险和复现成本排期。 |
| 3 | 原报告计数与重复项修订 | 修正审查基线，避免把规范建议当成已复现缺陷管理。 |
