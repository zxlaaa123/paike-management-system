# M-28/M-29 数据库字符集调查

## 结论

已修复。

原状态：

- `application.yml` 默认 JDBC URL 使用 `characterEncoding=utf8`。
- `.env.example` 也使用 `characterEncoding=utf8`。
- SQL 建表语句未声明 `CHARSET` / `CHARACTER SET` / `COLLATE`，新库初始化会依赖数据库实例默认字符集。

本轮处理：

- 默认 JDBC URL 改为 `characterEncoding=UTF-8`。
- `.env.example` 同步改为 `characterEncoding=UTF-8`。
- 50 个 `CREATE TABLE` 语句补充 `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`。

## 判断

MySQL Connector/J 的 `characterEncoding` 使用 Java 编码名。官方文档说明 `UTF-8` 会映射到 MySQL `utf8mb4`，因此不应把 JDBC URL 改成 `characterEncoding=utf8mb4`。

SQL 侧补表级 `utf8mb4` 选项后，新建数据库时不再依赖实例默认字符集。这个改动不转换既有库表字符集；已有部署若需要一致化，应单独执行数据备份、表转换和回归验证。

参考：

- https://dev.mysql.com/doc/connector-j/en/connector-j-reference-charsets.html

## 本轮验证

新增 `M28M29DatabaseCharsetInvestigationTest` 锁定当前事实：

- `application.yml` / `.env.example` 使用 `characterEncoding=UTF-8`。
- 建表 SQL 均声明 `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`。
