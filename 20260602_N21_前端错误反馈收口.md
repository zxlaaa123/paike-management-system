# 20260602 N21 前端错误反馈收口

## 结论

`N-21` 已修复。当前前端仍保留 `console.error` 作为调试日志，但 catch 内失败路径已补用户可见的 `ElMessage.error`。

## 调查

- 当前检出 18 个文件、29 处 `console.error`。
- `ErrorBoundary.vue` 会渲染页面级错误信息，属于可见错误边界。
- 其余 catch 内 `console.error` 已补齐错误提示，覆盖保存、加载、生成、清空和导出失败路径。

## 修改范围

- 基础资料：班级、教室、课程、教师、学期、教学任务保存失败提示。
- 排课相关：手动排课、方案生成、规则保存/恢复、冲突报告加载、未排任务批次加载和清空失败提示。
- 教师禁排：基础选项加载和保存失败提示。
- 课表导出：班级、教师、教室课表导出失败提示。
- 布局和登录：当前学期切换失败、登录失败提示。

## 验证

- 脚本扫描：
  - `console.error=29`
  - `files=18`
  - `catchMissingElMessageError=0`
- `cd D:\paike\frontend; npm run build`
  - `vue-tsc -b && vite build` 通过。
  - Vite 构建完成时间：7.71 秒。

## 备注

首次构建把日志写入 `frontend/dist/n21-build.log`，Vite 清空 `dist` 时因日志文件被占用触发 `EPERM`。改用临时目录日志后构建通过，该失败不是代码问题。
