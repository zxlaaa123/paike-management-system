# D3 议题清单

> 状态：候选议题池，未排期、未立项
> 来源：D2 评分体系收口（commit 887ee07/2511b9e/f5142b9）完成后留下的尾巴
> 日期：2026-05-25
> 仓库：D:\paike，分支 main（D1.2+D2 已 fast-forward 合并）

D2 的目标是『让 90.13 可解释』，已通过 ScoringDimensions 双轨制文档 +
ScoringFunctions 集中 + detailMessage 量纲化 + 回归基线单测达成。下面
是 D2 完成后留下的事实，按价值×成本排序，**不是 backlog 也不是承诺**，
是给未来重新进入该模块时的入口列表。

## A. 命中现状但未在 D2 范围内修的事实

### A.1 MORNING_THEORY_PRIORITY 在 COMPREHENSIVE 策略下隐式失效

- **事实**：COMPREHENSIVE 默认权重表（`ScheduleRuleWeightService.getDefaultRules`
  switch default 分支）共 11 条规则，**没有** MORNING_THEORY_PRIORITY。
  但 V3 在线 `scoreCandidate` 仍然调
  `weight(refData, "MORNING_THEORY_PRIORITY") * ScoringFunctions.candidateMorningPriority(...)`。
- **行为**：weightMap.getOrDefault(..., ZERO) 返回 0，整项贡献 0，相当于该维度
  对贪心选谁完全无影响。
- **现象等级**：不是 bug（结果正常），但是"代码写了实际不生效"的字段，未来读者
  会困惑。
- **决策门槛**：是产品意图（"COMPREHENSIVE 故意不要"），还是漏配（应加一条 weight=10）？
- **成本估算**：~30min（加 1 条 default rule + 重跑 V3 BALANCED 看 90.13 是不是漂）
- **冒烟影响**：补了之后 90.13 一定会变，因为引入了新维度的扣分。要更新所有 baseline。

### A.2 normalizeStrategyType 函数搜不到

- **事实**：D2 计划书摸现状时引用了 `normalizeStrategyType`（把未知 strategy 映射成
  COMPREHENSIVE）。codegraph_search 搜不到，grep 也搜不到。
- **可能性**：被某次重构 inline 掉了 / 改名了 / 从未真的存在过（计划书写错了）。
- **影响**：如果未来想理解『为什么 BALANCED 走到 COMPREHENSIVE 规则』，需要
  重新找入口。
- **成本估算**：~10min（git log -S "normalize" / 搜 generate 入口 strategyType
  的处理）

### A.3 schedule_score_detail.violation_count 字段命名 misleading

- **事实**：`ScheduleScoreService.buildSoftMetric` 把 `level`（clamp 后乘 100 取
  整的偏差百分比，∈[0,100]）写到 `ScheduleScoreDetail.violationCount` 字段，跟
  buildHardMetric 真正的"违规次数"含义不一致。
- **影响**：前端、报表、统计接口任何一个把 violationCount 当成"实际违规几次"用
  的地方都会错。已知 `ScheduleScoreController.getScoreSummary` 用 `score < 0`
  作为软硬违规计数判断条件，刚好绕过了这个字段，没踩坑。
- **成本估算**：要么改字段语义（要前端配合），要么拆字段（DB 改动）。前者
  ~1-2h，后者 ~3-4h。
- **优先级**：低（当前没有 caller 误用），但未来加新展示前要先看一眼。

## B. GPT-5.5 review 出来的结构性反馈

针对 D2 commit 2511b9e（ScoringFunctions 集中）后的双轨制保留决策，
GPT-5.5 给了三个 angle，#2 和 #3 真正有结构价值。

### B.1 用边际罚分 -ΔPenalty 作为在线评分（合并双轨的方向）

- **思路**：抛弃在线 candidateXxx 的 6 个独立公式，改成『把候选 item 试加进去，
  重算离线 penaltyXxx，取负的边际变化』。同名规则码就一套公式。
- **优势**：彻底消除双轨制，未来加规则只在一个地方加。
- **难点（GPT-5.5 已指出）**：方差、分布类目标具有全局依赖——每个候选都要
  重算整张表的方差，性能 N×M 倍恶化（N=候选数，M=已排数）。
  - 缓解方向：增量公式（方差有 Welford 在线公式），但 6 个 penalty 不一定都
    能写成增量形式。
- **冒烟门槛**：替换后必须保证 V3 BALANCED totalScore=90.13 严格不漂、
  V4 PARTIAL 253/254 严格不漂。这是个数值锁定的硬约束。
- **成本估算**：探索 + POC ~1 天，全量替换 + 性能优化 + 回归测试 ~2-3 天。
  收益是消除一个长期混淆源。

### B.2 类型层面分轨道（ScoringDimensions 拆 OnlineSoft / OfflineSoft）

- **思路**：当前 `ScoringDimensions.Dimension` record 一条同时含 onlineFormula
  和 offlineFormula 两个字段，仅靠 javadoc 防误用。改成两个独立 record
  `OnlineSoft` / `OfflineSoft`，让 ScoringFunctions 的签名也能在编译期拒绝
  误传（比如不让在线公式拿到 BigDecimal 入参）。
- **优势**：把"防御性文档"升级成"防御性类型"。成本低、不改算法。
- **难点**：会让 ScoringDimensions 表从『一条规则一行』变成『一条规则两行』，
  常量表读起来反而更碎。或者保留一条聚合行但额外加两个 record list 维护索引
  ——后者更复杂。
- **成本估算**：~2-3h，纯类型层面调整 + 单测。
- **价值评估**：值不值得做取决于未来读 ScoringDimensions 的人是否真的会误用。
  目前 ScoringFunctions 的 candidateXxx 返 double + penaltyXxx 返 BigDecimal
  已经在签名层面区分了；多一层类型隔离收益边际。

## C. D3 不该做的事

- **不在 D3 直接修 D2 的代码**，除非发现 D2 引入了真 bug。本清单的每一条都
  应该先开一个新分支独立评估，跟 D1.2/D2 一样有计划书+冒烟基线+commit message
  写 why。
- **不一次性扫完所有议题**。挑 1-2 条价值最高的做，其余继续留档。
- **不为了"补完整"而做 A.3**。前端没踩坑就先放着。

## D. 推荐推进顺序（如果未来重新进入该模块）

| # | 议题 | 价值 | 成本 | 推荐 |
|---|---|---|---|---|
| A.1 | MORNING 隐式失效 | 中（要先确认是不是产品意图） | 30min + 冒烟更新 | 先问，再动 |
| A.2 | normalizeStrategyType 找代码 | 低 | 10min | 顺手做 |
| B.2 | 类型层面分轨道 | 中 | 2-3h | 想做就做 |
| B.1 | -ΔPenalty 合并双轨 | 高 | 3-4 天 | 立独立 ticket |
| A.3 | violation_count 字段重命名 | 低 | 1-4h | 触发再说 |
