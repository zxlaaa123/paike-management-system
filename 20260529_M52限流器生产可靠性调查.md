# M-52 限流器生产可靠性调查

## 结论

部分成立，暂不修复生产代码。

原问题指出 `RateLimiterService` 使用进程内内存限流，生产环境不可靠。复查后确认：

- `RateLimiterService` 使用 `ConcurrentHashMap<String, Deque<Long>> attempts` 保存限流窗口。
- `AuthService.login()` 只在登录入口使用该服务，并采用用户名维度 `login:user:` 与 IP 维度 `login:ip:` 双键限流。
- 当前项目未引入 Redis、Lettuce、Redisson 或 `spring-boot-starter-data-redis`。

因此，在单实例本地部署下当前实现可以工作；但在多实例部署下，各实例会维护独立计数，登录请求如果被负载均衡分散到不同节点，限流窗口不会全局一致。服务重启也会清空限流状态。

## 不是本轮要修的点

旧报告中提到过“key 永不清理导致 OOM”。当前代码已不是这个状态：

- `cleanupExpiredKeysIfDue()` 会按窗口触发清理。
- `cleanupExpiredKeys()` 会移除已过期且为空的 deque：`attempts.remove(attemptKey, timestamps)`。

所以 M-52 的核心问题不是“当前没有任何清理”，而是“限流状态是单进程本地状态”。

## 判断

本轮不直接切 Redis，原因：

- 项目当前没有 Redis 依赖、配置、连接参数或部署约定。
- Redis 限流需要明确窗口算法、key 命名、TTL、Lua 原子性、Redis 不可用时 fail-open/fail-closed 策略。
- 登录限流属于认证安全路径，不能只通过加依赖和替换存储做机械修改。

后续如果要支持多实例生产部署，建议单独立项：

- 抽象 `RateLimiterStore` 或等价接口。
- 保留当前内存实现用于本地开发和单实例部署。
- 增加 Redis 实现，用 Lua 或 sorted set 保证滑动窗口原子性。
- 用配置项显式选择限流后端，并补 Redis 不可用时的安全策略。

## 本轮验证

新增 `M52RateLimiterServiceInvestigationTest` 锁定当前事实：

- `RateLimiterService` 仍是进程内 `ConcurrentHashMap` 状态。
- 当前代码不依赖 Redis 组件。
- 登录入口同时使用用户名和 IP 双键限流。
- 当前限流器已有过期空 key 清理逻辑。

