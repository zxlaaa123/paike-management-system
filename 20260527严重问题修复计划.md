# 20260527 严重问题修复计划

## 1. 目标

基于当前分支 `fix/code-review-critical-bugs`，先修复已验证报告中会影响排课正确性、接口稳定性、部署可用性和安全边界的严重问题。

不处理范围：

- 纯代码风格项。
- 原报告中已经判定为“不成立/当前不存在”的项。
- 需要产品重新定义权限模型的大型 RBAC 重构。当前只做最小可用权限防线设计。

## 2. 修复顺序

| 阶段 | 条目 | 状态 | 目标 | 原因 |
|---|---|---|---|---|
| P0 | C-10 | 已完成 | 修复候选位置容量校验 | 当前会把班级人数算成 0，直接影响排课结果正确性。 |
| P0 | C-01、C-05、C-06、M-44、C-02、C-03 | 已完成 | 修复核心链路 NPE | 前端漏字段、历史脏数据或空关联会触发 500。 |
| P1 | C-11、C-12 | 已完成 | 修复评分报告学期隔离和任务状态枚举 | 跨学期统计和状态不匹配会污染业务数据。 |
| P1 | C-21、C-22 | 已完成 | 修复 SQL 索引/缺表问题 | 新环境部署和软删除重建存在硬失败风险。 |
| P1 | C-15、C-16、C-17、M-37 | 已完成 | 修复安全边界 | XSS、下载路径、权限粗放、缺 CSP。 |
| P2 | M-01 至 M-11 中已确认项 | 进行中 | 修复性能与事务稳定性问题 | 数据量扩大后影响接口响应和事务稳定性。 |
| P2 | 原报告计数与重复项修订 | 待开始 | 修正审查基线 | 避免把规范建议当成已复现缺陷管理。 |

## 2.1 当前进度

截至 2026-05-27 17:45：

- 当前分支：`fix/code-review-critical-bugs`
- 已完成：`C-10` 候选位置容量校验修复。
- 已完成：NPE 组 `C-01`、`C-05`、`C-06`、`M-44`、`C-02`、`C-03`。
- 已完成：`C-11`、`C-12` 评分报告学期隔离和 V5 修复任务 `PENDING` 状态补齐。
- 已完成：`C-21`、`C-22` SQL 索引与缺表修复。
- 已完成：`C-15`、`C-16`、`C-17`、`M-37` 安全边界修复。
- 已完成：`M-01`、`M-02` V5 教室名称批量加载。
- 已完成：`M-03` 冲突检查任务关联资源合并查询。
- 已完成：`M-04` 未排课任务关联过滤数据库分页。
- 已完成：`M-05` 方案冲突刷新预分组同时间冲突，并跳过未变化明细更新。
- 已完成：`M-07` 风险分析时间段统计重复查询收敛。
- 已完成：`M-06` V5 一致性硬冲突检查按星期区间扫描，避免全量两两比较。
- 已完成：`M-09`、`M-11` 报告生成移除长事务边界，报告目录支持 `paike.report.dir` 配置。
- 已完成：`M-10` V5 试算生成/局部重排显式事务包裹写入流程，成功后的详情构建移出事务。
- 已完成：复核补丁：保护 `v14` 唯一约束替换前置条件，报告测试使用临时目录，并补充报告目录配置说明。
- 已复核：`M-08` 保留包含匹配语义，仅对课表筛选关键字做后端 trim/空白转 null；是否改为前缀/全文搜索需产品确认和 SQL 执行计划。
- 已新增测试：
  - `backend/src/test/java/com/paike/scheduler/service/V5CandidatePositionServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/SchedulePlanServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/V4ScheduleAdjustmentServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/V5RepairTaskFlowServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/V5Stage1DataServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/ScheduleConflictServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/ScheduleScoreReportServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/db/DatabaseSchemaScriptTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/V4ScheduleReportServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/auth/AuthInterceptorTest.java`
  - `backend/src/test/java/com/paike/scheduler/config/SecurityHeadersFilterTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/V5ClassroomBatchLoadingTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/UnscheduledTaskServiceTest.java`
  - `backend/src/test/java/com/paike/scheduler/service/V4ScheduleRiskServiceTest.java`
- 已更新文档：
  - `README.md` 后端启动变量说明。
  - `backend/src/main/resources/db/README.md` 数据库迁移说明。
  - 本计划文档当前进度、验证结果和剩余任务说明。
- 验证结果：
  - `GET http://127.0.0.1:8090/api/health` 返回 `200`，服务状态 `UP`。
  - `mvn test -Dtest=V5CandidatePositionServiceTest` 通过：2 tests，0 failures，0 errors。
  - NPE 组定向测试通过：10 tests，0 failures，0 errors。
  - C-11/C-12 定向测试通过：13 tests，0 failures，0 errors。
  - 后端完整 `mvn test` 通过：53 tests，0 failures，0 errors。
  - 前端 `npm run build` 通过。
  - `POST /api/v5/candidate-positions/generate` 未登录返回 `401`，认证拦截正常；业务级接口验证还需要登录态。
- 下一步：修复性能与事务稳定性问题，优先处理 `M-01` 至 `M-11` 中已确认项。

## 3. 分组修复方案

### 3.1 C-10 候选位置容量校验

状态：已完成。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/V5CandidatePositionService.java`
- `backend/src/test/java/com/paike/scheduler/service/V5CandidatePositionServiceTest.java`

影响面：

- `V5CandidatePositionService.generate()`
- `V5CandidatePositionController.generate()`
- V5 修复建议、试算、局部重排前的候选位置推荐结果。

修复方案：

- 已在候选生成前批量加载 `classId -> studentCount` 缓存，避免候选循环内重复查库。
- `fastHardCheck()` 使用真实班级人数判断 `studentCount > room.capacity`。
- 班级信息缺失时返回不可用原因 `班级信息不存在`，不再默认为 0 人。
- 教室容量为空时保持原业务语义：不触发容量拦截，避免本次修复扩大规则面。

回归风险：

- 以前被错误放行的超容量候选会减少，前端可选项数量可能下降。
- 生成候选位置时多一次班级数据依赖，需避免 N+1。

验证：

- 已通过单测：班级人数 60、教室容量 50，候选不可用，原因包含容量不足。
- 已通过单测：班级人数 40、教室容量 50，容量校验通过。
- 已通过后端完整测试：30 tests，0 failures，0 errors。
- 运行：`cd D:\paike\backend; mvn test -Dtest=V5CandidatePositionServiceTest`

### 3.2 C-01/C-05/C-06/M-44/C-02/C-03 NPE 修复

状态：已完成。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/SchedulePlanService.java`
- `backend/src/main/java/com/paike/scheduler/service/V5RepairTaskFlowService.java`
- `backend/src/main/java/com/paike/scheduler/service/V5Stage1DataService.java`
- `backend/src/main/java/com/paike/scheduler/service/V4ScheduleAdjustmentService.java`
- `backend/src/main/java/com/paike/scheduler/service/ScheduleConflictService.java`
- `backend/src/test/java/com/paike/scheduler/service/SchedulePlanServiceTest.java`
- `backend/src/test/java/com/paike/scheduler/service/V4ScheduleAdjustmentServiceTest.java`
- `backend/src/test/java/com/paike/scheduler/service/V5RepairTaskFlowServiceTest.java`
- `backend/src/test/java/com/paike/scheduler/service/V5Stage1DataServiceTest.java`
- `backend/src/test/java/com/paike/scheduler/service/ScheduleConflictServiceTest.java`

影响面：

- 方案手动调整。
- V4 调整应用。
- V5 修复任务创建/阶段数据初始化。
- 手动排课冲突检查与自动排课冲突检查。
- 前端 `schedule.ts` 的 `checkConflict()` 调用。

修复方案：

- `SchedulePlanService.adjustPlanItem()`：入口先校验 `adjustReason`，为空返回 `BusinessException("调整原因不能为空")`，避免更新方案后写日志时才 NPE。
- `V5RepairTaskFlowService.createTask()`：入口先校验 `taskType`，为空返回 `BusinessException("任务类型不能为空")`，避免进入绑定查询后再 `.trim()`。
- `V5Stage1DataService.createRepairTask()`：入口先校验 `taskCode`、`taskType`，为空分别返回明确业务异常。
- `V4ScheduleAdjustmentService.toPlanAdjustRequest()`：预检路径允许 `adjustReason` 为空，内部转换为默认原因 `调整预检`；应用路径仍由 `applyAdjustmentInternal()` 要求调整原因不能为空。
- `ScheduleConflictService.checkConflict()`：
  - `deleted/status` 等 `Integer` 比较改为防空比较。
  - `classroom.capacity`、`classInfo.studentCount` 缺失时返回带标签的冲突原因，不再拆箱 NPE。
  - `existingTask.teacherId/classId`、`schedule.classroomId` 比较改为 `Objects.equals(...)`。

回归风险：

- 原先隐式 500 的请求会变为业务错误，前端错误提示路径可能变化。
- 如果历史数据里存在空状态，冲突判断结果会更保守。

验证：

- 已通过定向测试：
  - `SchedulePlanServiceTest`
  - `V4ScheduleAdjustmentServiceTest`
  - `V5RepairTaskFlowServiceTest`
  - `V5Stage1DataServiceTest`
  - `ScheduleConflictServiceTest`
  - `V5CandidatePositionServiceTest`
- 定向结果：10 tests，0 failures，0 errors。
- 后端完整 `mvn test`：38 tests，0 failures，0 errors。

### 3.3 C-11/C-12 学期隔离与状态枚举

状态：已完成。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/ScheduleScoreReportService.java`
- `backend/src/main/java/com/paike/scheduler/controller/ScheduleScoreReportController.java`
- `backend/src/main/java/com/paike/scheduler/entity/ScheduleScoreReport.java`
- `backend/src/main/java/com/paike/scheduler/common/enums/V5RepairTaskStatus.java`
- `frontend/src/api/v5RepairTaskApi.ts`
- `frontend/src/api/scheduleScoreReport.ts`
- `frontend/src/views/v5/RepairTaskListView.vue`
- `frontend/src/views/v5/RepairTaskDetailView.vue`
- `backend/src/main/resources/db/v13_score_report_semester.sql`
- `backend/src/test/java/com/paike/scheduler/service/ScheduleScoreReportServiceTest.java`

影响面：

- 评分报告生成。
- 评分报告列表/最新报告读取。
- V5 修复任务创建、列表、状态展示。

修复方案：

- `ScheduleScoreReportService.generate(Long semesterId)`、`getLatest(Long semesterId)`、`list(Long semesterId, ...)` 支持显式学期；未传时使用当前学期。
- 生成评分时，`schedule` 和 `teaching_task` 查询按同一 `semesterId` 过滤，避免跨学期统计。
- `ScheduleScoreReport` 增加 `semesterId` 字段；新增 `v13_score_report_semester.sql` 幂等补列和索引。
- `ScheduleScoreReportController` 三个接口支持可选 `semesterId` 参数，旧调用默认当前学期。
- `V5RepairTaskStatus`、前端 `V5RepairTaskStatus` 类型和状态标签补齐 `PENDING`。

回归风险：

- 历史评分报告若没有学期字段，列表筛选和最新报告逻辑需兼容空值。
- 状态枚举新增后，前端状态标签需要显示兜底。

验证：

- 已通过单测：生成评分报告时写入请求学期 ID。
- 已通过单测：未传学期时读取当前学期后查询最新报告。
- 已通过单测：`V5RepairTaskStatus.PENDING` 与数据库默认值一致。
- C-11/C-12 定向测试：13 tests，0 failures，0 errors。
- 后端完整 `mvn test`：41 tests，0 failures，0 errors。
- 前端 `npm run build`：通过。

### 3.4 C-21/C-22 SQL 索引与缺表

涉及文件：

- `backend/src/main/resources/db/v6_schedule_index.sql`
- 新增 SQL：`backend/src/main/resources/db/v14_missing_v4_v5_tables_and_schedule_keys.sql`
- `backend/src/main/resources/application.yml`
- `backend/src/main/java/com/paike/scheduler/config/SemesterSchemaInitializer.java`
- `backend/src/test/java/com/paike/scheduler/db/DatabaseSchemaScriptTest.java`
- 涉及实体：
  - `ScheduleAdjustLog`
  - `ScheduleGenerateLog`
  - `ScheduleLockedItem`
  - `ScheduleReport`
  - `ScheduleUnassignedTask`

影响面：

- 新环境启动初始化。
- 课程锁定、审计、报告生成下载、未排任务管理。
- schedule 表软删除后的重建能力。

修复方案：

- 不直接破坏旧库。新增幂等 SQL 文件，追加到 `schema-locations` 末尾。状态：已完成。
- 为缺失的 5 张表补 `CREATE TABLE IF NOT EXISTS`，字段以 Entity 为准。状态：已完成。
- `schedule` 唯一约束修正。状态：已完成。
  - 避免 `(time_slot_id, teacher_id, deleted)` 这种软删除后只能保留一条历史的唯一键。
  - 采用 `active_key` 生成列，先删除包含 `deleted` 的错误唯一键后加正确唯一键。
  - 所有 DROP/ADD KEY 都要查 `INFORMATION_SCHEMA.STATISTICS` 后执行，保持幂等。
- 保留 `continue-on-error: true` 的现状，但新脚本自身应尽量幂等，不依赖失败跳过。
- 更新 `SemesterSchemaInitializer` 的兜底建表 DDL，避免运行时兜底创建的表缺 `deleted` 列。状态：已完成。

回归风险：

- 修改唯一索引会影响排课插入冲突约束。必须确认 Service 层冲突检测和数据库约束一致。
- 老库已有重复数据时，新增唯一键可能失败，需要先清理或设计迁移策略。
- 表字段与 Entity 不一致会导致 MyBatis 插入失败。

验证：

- 用空库启动后确认所有 `@TableName` 对应表存在。
- 对 `schedule` 执行：插入一条有效记录、软删除、再插入同位置有效记录，应成功。
- 对 `schedule` 执行：两条同位置有效记录，应失败或被业务层提前拦截。
- 运行脚本二次执行，不应因重复列/重复索引失败。
- 静态验证：`DatabaseSchemaScriptTest` 覆盖缺表 DDL、`active_key` 唯一键和 `application.yml` 注册。
- 可用脚本：`scripts/verify-soft-delete-plan.js`，必要时新增 `scripts/verify-db-schema.js`。

### 3.5 C-15/C-16/C-17/M-37 安全边界

实际涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/V4ScheduleReportService.java`
- `backend/src/main/java/com/paike/scheduler/config/SecurityHeadersFilter.java`
- `backend/src/main/java/com/paike/scheduler/auth/AuthInterceptor.java`
- `backend/src/main/java/com/paike/scheduler/auth/AuthService.java`
- `backend/src/main/java/com/paike/scheduler/auth/vo/UserInfoVo.java`
- `backend/src/main/java/com/paike/scheduler/config/AdminUserInitializer.java`
- `backend/src/main/java/com/paike/scheduler/entity/SysUser.java`
- `backend/src/main/resources/db/v15_sys_user_role.sql`
- `backend/src/main/resources/application.yml`
- `frontend/src/api/auth.ts`
- `backend/src/test/java/com/paike/scheduler/service/V4ScheduleReportServiceTest.java`
- `backend/src/test/java/com/paike/scheduler/auth/AuthInterceptorTest.java`
- `backend/src/test/java/com/paike/scheduler/config/SecurityHeadersFilterTest.java`
- `backend/src/test/java/com/paike/scheduler/db/DatabaseSchemaScriptTest.java`

影响面：

- HTML 报告生成和下载。
- 所有已登录接口的访问边界。
- 浏览器安全策略。

修复方案：

- HTML 转义：
  - `safe()` 改为 HTML escape，至少处理 `& < > " ' /`。状态：已完成，已拆为 `safeText()` 与 `safeHtml()`。
  - Excel 输出不复用 HTML 转义。状态：已完成。
- 下载路径：
  - `ensureReportDir()` 返回规范化绝对路径。状态：已完成。
  - `resolveDownloadFile()` 对数据库 `filePath` 做 `normalize().toAbsolutePath()`。状态：已完成。
  - 校验目标路径必须 `startsWith(reportDir)`。状态：已完成。
  - 文件不存在仍返回业务 404。
- CSP：
  - 在 `SecurityHeadersFilter` 增加保守 CSP。状态：已完成。
  - 当前策略：`default-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'`。
  - 若前端静态资源需要内联样式，需要评估是否影响 Element Plus；后端 API 响应加 CSP 通常影响较小。
- 权限控制：
  - 最小方案：在 `AuthInterceptor` 中区分只读接口与写接口。状态：已完成。
  - `/api/**` 的 `POST/PUT/PATCH/DELETE` 要求 `ADMIN`，`/api/auth/logout` 例外。状态：已完成。
  - 新增 `sys_user.role`，默认 `USER`；内置 `admin` 通过迁移和初始化器标记为 `ADMIN`。状态：已完成。

回归风险：

- HTML 转义后报告显示会变化，但这是预期。
- 下载路径收紧后，历史数据库中相对路径或旧目录报告可能无法下载，需要迁移或兼容一次。
- CSP 过严可能影响未来同源页面嵌入或下载预览。
- 权限控制可能影响现有测试账号和前端操作流程。

验证：

- 单测：报告字段含 `<script>` 时，HTML 文件中只出现转义文本。
- 单测：报告 `filePath=..\..\x` 或绝对路径越界时，下载返回业务错误。
- 单测/接口测试：响应头包含 CSP。
- 权限验证：
  - 未登录访问写接口仍被拦截。
  - 普通用户访问高危写接口被拒绝。
  - 管理员访问高危写接口通过。
- 前端构建：`npm run build`。

实际验证：

- 安全组定向测试通过：12 tests，0 failures，0 errors。
- 后端完整 `mvn test` 通过：53 tests，0 failures，0 errors。
- 前端 `npm run build` 通过。

### 3.6 M-01/M-02 V5 教室名称 N+1 查询

状态：已完成并提交。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/V5RepairSuggestionService.java`
- `backend/src/main/java/com/paike/scheduler/service/V5SimulationService.java`
- `backend/src/test/java/com/paike/scheduler/service/V5ClassroomBatchLoadingTest.java`

影响面：

- V5 修复建议列表/详情/进入试算时的源教室、目标教室名称展示。
- V5 试算对比中课程变更明细和教室利用率变化的教室名称展示。

修复内容：

- `V5RepairSuggestionService`：先解析建议明细中的源/目标教室 ID，统一 `selectBatchIds` 加载教室名称，再传入 `toVo`，避免列表映射时每条建议两次 `selectById`。
- `V5SimulationService`：在 `buildCompare()` 中一次性收集基准方案、试算方案和接受建议变更项的教室 ID，统一 `selectBatchIds` 加载教室名称；课程变更和教室利用率变化共用同一个名称 Map。
- 移除两个服务中 `classroomMapper.selectById(...)` 的教室名称查询路径，仅保留各服务 1 个 `classroomMapper.selectBatchIds(...)` 批量入口。

回归风险：

- 教室被删除或 ID 不存在时名称仍返回 `null`，与原逻辑保持一致。
- 批量加载范围扩大到对比所需教室 ID，单次查询参数量随方案教室数增长，但查询次数从 N 次收敛为 1 次。

验证：

- 新增 `V5ClassroomBatchLoadingTest` 静态验证：
  - 两个服务不再包含 `classroomMapper.selectById(`。
  - 每个服务只保留 1 个 `classroomMapper.selectBatchIds(`。
  - 修复建议和试算对比均走批量名称 Map。
- 定向测试通过：`mvn -Dtest=V5ClassroomBatchLoadingTest test`，2 tests，0 failures，0 errors。

### 3.7 M-03 冲突检查关联表连续查询

状态：已完成并提交。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/entity/TeachingTask.java`
- `backend/src/main/java/com/paike/scheduler/mapper/TeachingTaskMapper.java`
- `backend/src/main/resources/mapper/TeachingTaskMapper.xml`
- `backend/src/main/java/com/paike/scheduler/service/ScheduleConflictService.java`
- `backend/src/test/java/com/paike/scheduler/service/ScheduleConflictServiceTest.java`

影响面：

- 手动排课和自动排课共用的冲突检测入口 `ScheduleConflictService.checkConflict()`。
- 前端 `POST /api/schedules/check-conflict` 间接受影响。

修复内容：

- 新增 `TeachingTaskMapper.selectConflictCheckById()`，一次 JOIN 加载教学任务、课程类型、教师姓名/状态、班级名称/状态/人数。
- `ScheduleConflictService.checkConflict()` 改用任务详情查询，移除入口处 `courseMapper.selectById()`、`teacherMapper.selectById()`、`classInfoMapper.selectById()` 三次关联查询。
- 保留时间段、教室按本次排课参数独立查询；已有时间段内排课任务仍使用 `selectBatchIds` 批量加载。
- `TeachingTask` 增加仅用于查询结果映射的非持久字段：`courseType`、`teacherStatus`、`classStatus`、`studentCount`。

回归风险：

- 任务详情查询使用 `LEFT JOIN`，关联课程/教师/班级缺失时保持原有兜底行为。
- 冲突提示中的教师名、班级名来自 JOIN 映射字段，需保证 SQL alias 与实体字段一致。

验证：

- `mvn -Dtest=ScheduleConflictServiceTest clean test` 通过：4 tests，0 failures，0 errors。
- 新增测试确认 `checkConflict()` 调用 `selectConflictCheckById()`，不再回退到 `teachingTaskMapper.selectById()`。
- 后端可脱离 DB context 的测试通过：`mvn -Dtest=!SchedulerBackendApplicationTests test`，55 tests，0 failures，0 errors。

### 3.8 M-04 未排课任务关联过滤内存分页

状态：已完成并提交。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/UnscheduledTaskService.java`
- `backend/src/main/java/com/paike/scheduler/mapper/UnscheduledTaskMapper.java`
- `backend/src/main/resources/mapper/UnscheduledTaskMapper.xml`
- `backend/src/test/java/com/paike/scheduler/service/UnscheduledTaskServiceTest.java`

影响面：

- 未排课任务列表及按批次列表接口。
- 课程名、教师名、班级名、失败原因类型筛选和分页总数。

修复内容：

- 新增 `UnscheduledTaskMapper.selectFilteredPage()`，在数据库层通过关联表筛选并分页。
- 查询使用 `LEFT JOIN` 返回 `courseName`、`teacherName`、`className`、`batchNo`，保留关联数据缺失时仍展示未排课记录的既有行为。
- `UnscheduledTaskService.list()` 统一走分页查询，删除全量 `selectList`、内存筛选和 `subList` 分页，以及分页后的多批关联补查。
- 空白筛选参数在服务层归一为 `null`，保持“不启用筛选”的接口语义。

回归风险：

- 分页查询引入 JOIN，需依赖关联列索引和 MyBatis-Plus 分页拦截器正确生成总数查询。
- 名称筛选针对已被逻辑删除的关联记录不会匹配；未使用名称筛选时记录仍会保留，名称为空，与旧逻辑一致。

验证：

- `mvn -Dtest=UnscheduledTaskServiceTest clean test` 通过：3 tests，0 failures，0 errors。
- 单测确认名称筛选调用数据库分页、空白筛选不参与 SQL 条件，并静态锁定服务中不再存在 `selectList` / `subList` 内存分页路径。
- 后端可脱离 DB context 的测试通过：`mvn -Dtest=!SchedulerBackendApplicationTests test`，58 tests，0 failures，0 errors。

### 3.9 M-07 风险分析时间段重复查询

状态：已完成，待提交。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/V4ScheduleRiskService.java`
- `backend/src/test/java/com/paike/scheduler/service/V4ScheduleRiskServiceTest.java`

影响面：

- V4/V5 风险分析中 `V4ScheduleRiskService.getPlanRisks()` 的上下文构建。
- 教室利用率风险的分母 `totalTimeSlots`。

修复内容：

- `buildContext()` 原本对 `time_slot` 先 `selectList` 构建 `slotMap`，再 `selectCount` 计算总数。
- 改为复用已加载的 `slots` 列表，通过 `slots.size()` 计算 `totalTimeSlots`，把同表两次查询收敛为一次。

回归风险：

- `selectList` 和 `selectCount` 原本都基于无条件查询；复用列表大小语义一致。
- 空时间段表仍通过 `Math.max(..., 1)` 保底，避免利用率分母为 0。

验证：

- `mvn -Dtest=UnscheduledTaskServiceTest,V4ScheduleRiskServiceTest test` 通过：4 tests，0 failures，0 errors。
- 单测静态锁定 `V4ScheduleRiskService` 不再调用 `timeSlotMapper.selectCount`，并复用 `slots.size()`。
- 后端可脱离 DB context 的测试通过：`mvn -Dtest=!SchedulerBackendApplicationTests test`，59 tests，0 failures，0 errors。

## 4. 已完成提交

当前分支已按小提交推进，最近修复提交如下：

1. `07c0132 fix(v5): 修复候选位置容量校验`
2. `4c64595 fix: 修复核心服务空值异常`
3. `6fac914 fix: 隔离评分报告学期数据`
4. `7229421 fix: 修复 SQL 索引与缺表初始化`
5. `10572d8 fix(security): 转义报告并限制下载路径`
6. `424b1cb fix(security): 增加 CSP 和管理员写保护`
7. `fix(perf): 批量加载 V5 教室名称`
8. `fix(perf): 合并冲突检查任务关联查询`
9. `fix(perf): 下推未排课任务分页筛选`
10. `fix(perf): 复用风险分析时间段查询`

每个提交都可单独解释、单独回滚。

## 4.1 剩余任务

下一阶段不再继续扩展安全权限模型，先处理已确认的性能与事务稳定性问题：

1. 原报告计数与重复项修订：更新审查基线，区分已复现缺陷、部分成立项和规范建议。

## 5. 验证命令

基础验证：

```powershell
cd D:\paike\backend
$env:JWT_SECRET='test_secret_32_bytes_minimum_value_123'
mvn test
```

前端验证：

```powershell
cd D:\paike\frontend
npm run build
```

数据库验证：

```powershell
cd D:\paike
node scripts/verify-soft-delete-plan.js
```

如果需要接口级验证，由用户按项目约定手动启动后端：

```powershell
cd D:\paike\backend
mvn spring-boot:run
```

启动成功后再执行 API smoke 或 Playwright 测试。

## 6. 开工建议

`M-01` / `M-02` / `M-03` / `M-04` / `M-07` 已完成。下一步继续处理性能与事务稳定性问题：

1. 复核原报告计数与重复项，避免把已修复项、误报和规范建议混在同一缺陷池。
2. 跑对应服务测试和后端完整 `mvn test`。
3. 若涉及前端展示字段，再跑 `npm run build`。

这些部分不涉及数据库表结构和权限模型，风险较低，能继续降低数据量扩大后的接口延迟。
