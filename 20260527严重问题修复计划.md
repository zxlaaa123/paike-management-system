# 20260527 严重问题修复计划

## 1. 目标

基于当前分支 `fix/code-review-critical-bugs`，先修复已验证报告中会影响排课正确性、接口稳定性、部署可用性和安全边界的严重问题。

不处理范围：

- 纯代码风格项。
- 原报告中已经判定为“不成立/当前不存在”的项。
- 需要产品重新定义权限模型的大型 RBAC 重构。当前只做最小可用权限防线设计。

## 2. 修复顺序

| 阶段 | 条目 | 状态 | 目标 | 原因 |
|---|---|---|---|---|
| P0 | C-10 | 已完成 | 修复候选位置容量校验 | 当前会把班级人数算成 0，直接影响排课结果正确性。 |
| P0 | C-01、C-05、C-06、M-44、C-02、C-03 | 待开始 | 修复核心链路 NPE | 前端漏字段、历史脏数据或空关联会触发 500。 |
| P1 | C-11、C-12 | 待开始 | 修复评分报告学期隔离和任务状态枚举 | 跨学期统计和状态不匹配会污染业务数据。 |
| P1 | C-21、C-22 | 待开始 | 修复 SQL 索引/缺表问题 | 新环境部署和软删除重建存在硬失败风险。 |
| P1 | C-15、C-16、C-17、M-37 | 待开始 | 修复安全边界 | XSS、下载路径、权限粗放、缺 CSP。 |

## 2.1 当前进度

截至 2026-05-27 12:42：

- 当前分支：`fix/code-review-critical-bugs`
- 已完成：`C-10` 候选位置容量校验修复。
- 已新增测试：`backend/src/test/java/com/paike/scheduler/service/V5CandidatePositionServiceTest.java`
- 已更新文档：`README.md` 后端启动变量说明。
- 验证结果：
  - `GET http://127.0.0.1:8090/api/health` 返回 `200`，服务状态 `UP`。
  - `mvn test -Dtest=V5CandidatePositionServiceTest` 通过：2 tests，0 failures，0 errors。
  - 后端完整 `mvn test` 通过：30 tests，0 failures，0 errors。
  - `POST /api/v5/candidate-positions/generate` 未登录返回 `401`，认证拦截正常；业务级接口验证还需要登录态。
- 下一步：修复 NPE 组 `C-01`、`C-05`、`C-06`、`M-44`、`C-02`、`C-03`。

## 3. 分组修复方案

### 3.1 C-10 候选位置容量校验

状态：已完成。

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/V5CandidatePositionService.java`
- `backend/src/test/java/com/paike/scheduler/service/V5CandidatePositionServiceTest.java`

影响面：

- `V5CandidatePositionService.generate()`
- `V5CandidatePositionController.generate()`
- V5 修复建议、试算、局部重排前的候选位置推荐结果。

修复方案：

- 已在候选生成前批量加载 `classId -> studentCount` 缓存，避免候选循环内重复查库。
- `fastHardCheck()` 使用真实班级人数判断 `studentCount > room.capacity`。
- 班级信息缺失时返回不可用原因 `班级信息不存在`，不再默认为 0 人。
- 教室容量为空时保持原业务语义：不触发容量拦截，避免本次修复扩大规则面。

回归风险：

- 以前被错误放行的超容量候选会减少，前端可选项数量可能下降。
- 生成候选位置时多一次班级数据依赖，需避免 N+1。

验证：

- 已通过单测：班级人数 60、教室容量 50，候选不可用，原因包含容量不足。
- 已通过单测：班级人数 40、教室容量 50，容量校验通过。
- 已通过后端完整测试：30 tests，0 failures，0 errors。
- 运行：`cd D:\paike\backend; mvn test -Dtest=V5CandidatePositionServiceTest`

### 3.2 C-01/C-05/C-06/M-44/C-02/C-03 NPE 修复

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/SchedulePlanService.java`
- `backend/src/main/java/com/paike/scheduler/service/V5RepairTaskFlowService.java`
- `backend/src/main/java/com/paike/scheduler/service/V5Stage1DataService.java`
- `backend/src/main/java/com/paike/scheduler/service/V4ScheduleAdjustmentService.java`
- `backend/src/main/java/com/paike/scheduler/service/ScheduleConflictService.java`

影响面：

- 方案手动调整。
- V4 调整应用。
- V5 修复任务创建/阶段数据初始化。
- 手动排课冲突检查与自动排课冲突检查。
- 前端 `schedule.ts` 的 `checkConflict()` 调用。

修复方案：

- 增加统一的小型私有方法：`trimToNull()` 或 `trimToDefault()`，就近放在 Service 内，避免扩大改动面。
- `adjustReason`：
  - 空值允许，写入默认原因，例如 `手动调整` / `V4智能调整`。
  - 不允许把空值传入 `.trim()`。
- `taskCode`、`taskType`：
  - `taskCode` 为空时生成后端兜底编码，或返回明确 `BusinessException`。建议：创建类接口中必填，后端校验并返回 400 业务错误。
  - `taskType` 为空时返回明确错误，不默认猜测类型。
- `ScheduleConflictService`：
  - 所有 `Integer` 状态比较改为 `Objects.equals(value, 1)` 或安全判断。
  - `existingTask.getTeacherId().equals(teacherId)` 改为 `Objects.equals(existingTask.getTeacherId(), teacherId)`。
  - 关联对象为空时返回业务原因，不抛系统异常。

回归风险：

- 原先隐式 500 的请求会变为业务错误，前端错误提示路径可能变化。
- 如果历史数据里存在空状态，冲突判断结果会更保守。

验证：

- 单测：`adjustReason=null` 调整课程不抛 NPE。
- 单测：V4 调整 `adjustReason=null` 不抛 NPE。
- 单测：V5 创建请求缺 `taskType` 返回业务异常。
- 单测：冲突检查遇到空 `teacherId`、空 `status`、空 `capacity` 不抛 NPE。
- 运行：`cd D:\paike\backend; mvn test -Dtest=ScheduleConflictServiceTest,SchedulePlanServiceTest,V5RepairTaskFlowServiceTest,V5Stage1DataServiceTest,V4ScheduleAdjustmentServiceTest`

### 3.3 C-11/C-12 学期隔离与状态枚举

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/ScheduleScoreReportService.java`
- `backend/src/main/java/com/paike/scheduler/controller/ScheduleScoreReportController.java`
- `backend/src/main/java/com/paike/scheduler/common/enums/V5RepairTaskStatus.java`
- `frontend/src/api/v5RepairTaskApi.ts`
- 可能涉及 SQL 默认值：`backend/src/main/resources/db/v5_stage1.sql`

影响面：

- 评分报告生成。
- 评分报告列表/最新报告读取。
- V5 修复任务创建、列表、状态展示。

修复方案：

- `ScheduleScoreReportService.generate()` 接收 `semesterId`，无入参时使用当前学期。
- 查询 `schedule`、`teaching_task`、禁排等数据时全部按同一 `semesterId` 收敛。
- 报告实体若当前缺 `semesterId`，需同步加字段；如果已有 SQL 字段但 Entity 缺失，补 Entity。
- `V5RepairTaskStatus` 增加 `PENDING`，并和前端 `V5RepairTaskStatus` 类型保持一致。
- 检查默认创建状态：数据库默认值、Service 创建值、前端筛选值三者统一。

回归风险：

- 历史评分报告若没有学期字段，列表筛选和最新报告逻辑需兼容空值。
- 状态枚举新增后，前端状态标签需要显示兜底。

验证：

- 单测：两个学期各有排课数据，生成 A 学期评分时不统计 B 学期。
- 单测：无当前学期时返回明确业务错误，不静默生成全局报告。
- 单测：读取 `PENDING` 状态任务不异常。
- 前端构建：`cd D:\paike\frontend; npm run build`
- 后端测试需先提供测试密钥：`$env:JWT_SECRET='test_secret_32_bytes_minimum_value_123'; mvn test`

### 3.4 C-21/C-22 SQL 索引与缺表

涉及文件：

- `backend/src/main/resources/db/v6_schedule_index.sql`
- 新增 SQL：建议 `backend/src/main/resources/db/v13_missing_v4_v5_tables.sql`
- `backend/src/main/resources/application.yml`
- 涉及实体：
  - `ScheduleAdjustLog`
  - `ScheduleGenerateLog`
  - `ScheduleLockedItem`
  - `ScheduleReport`
  - `UnscheduledTask` / `ScheduleUnassignedTask` 命名需核对实体表名。

影响面：

- 新环境启动初始化。
- 课程锁定、审计、报告生成下载、未排任务管理。
- schedule 表软删除后的重建能力。

修复方案：

- 不直接破坏旧库。新增幂等 SQL 文件，追加到 `schema-locations` 末尾。
- 为缺失的 5 张表补 `CREATE TABLE IF NOT EXISTS`，字段以 Entity 为准。
- `schedule` 唯一约束修正：
  - 避免 `(time_slot_id, teacher_id, deleted)` 这种软删除后只能保留一条历史的唯一键。
  - 采用 `active_key` 生成列，或先删除错误唯一键后加正确唯一键。
  - 所有 DROP/ADD KEY 都要查 `INFORMATION_SCHEMA.STATISTICS` 后执行，保持幂等。
- 保留 `continue-on-error: true` 的现状，但新脚本自身应尽量幂等，不依赖失败跳过。

回归风险：

- 修改唯一索引会影响排课插入冲突约束。必须确认 Service 层冲突检测和数据库约束一致。
- 老库已有重复数据时，新增唯一键可能失败，需要先清理或设计迁移策略。
- 表字段与 Entity 不一致会导致 MyBatis 插入失败。

验证：

- 用空库启动后确认所有 `@TableName` 对应表存在。
- 对 `schedule` 执行：插入一条有效记录、软删除、再插入同位置有效记录，应成功。
- 对 `schedule` 执行：两条同位置有效记录，应失败或被业务层提前拦截。
- 运行脚本二次执行，不应因重复列/重复索引失败。
- 可用脚本：`scripts/verify-soft-delete-plan.js`，必要时新增 `scripts/verify-db-schema.js`。

### 3.5 C-15/C-16/C-17/M-37 安全边界

涉及文件：

- `backend/src/main/java/com/paike/scheduler/service/V4ScheduleReportService.java`
- `backend/src/main/java/com/paike/scheduler/controller/ScheduleReportController.java`
- `backend/src/main/java/com/paike/scheduler/config/SecurityHeadersFilter.java`
- `backend/src/main/java/com/paike/scheduler/config/WebMvcConfig.java`
- `backend/src/main/java/com/paike/scheduler/auth/AuthInterceptor.java`

影响面：

- HTML 报告生成和下载。
- 所有已登录接口的访问边界。
- 浏览器安全策略。

修复方案：

- HTML 转义：
  - `safe()` 改为 HTML escape，至少处理 `& < > " ' /`。
  - Excel 输出不应复用 HTML 转义；必要时拆成 `safeText()` 与 `safeHtml()`。
- 下载路径：
  - `ensureReportDir()` 返回规范化绝对路径。
  - `resolveDownloadFile()` 对数据库 `filePath` 做 `normalize().toAbsolutePath()`。
  - 校验目标路径必须 `startsWith(reportDir)`。
  - 文件不存在仍返回业务 404。
- CSP：
  - 在 `SecurityHeadersFilter` 增加保守 CSP。
  - 初始建议：`default-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'`。
  - 若前端静态资源需要内联样式，需要评估是否影响 Element Plus；后端 API 响应加 CSP 通常影响较小。
- 权限控制：
  - 最小方案：在 `AuthInterceptor` 或新增权限拦截器中区分只读接口与写接口。
  - 写接口、删除接口、生成/应用/回滚/锁定接口要求管理员。
  - 如果当前用户模型没有角色字段，则先只完成安全计划和保护高危接口，避免硬编码破坏现有登录。

回归风险：

- HTML 转义后报告显示会变化，但这是预期。
- 下载路径收紧后，历史数据库中相对路径或旧目录报告可能无法下载，需要迁移或兼容一次。
- CSP 过严可能影响未来同源页面嵌入或下载预览。
- 权限控制可能影响现有测试账号和前端操作流程。

验证：

- 单测：报告字段含 `<script>` 时，HTML 文件中只出现转义文本。
- 单测：报告 `filePath=..\..\x` 或绝对路径越界时，下载返回业务错误。
- 单测/接口测试：响应头包含 CSP。
- 权限验证：
  - 未登录访问写接口仍被拦截。
  - 普通用户访问高危写接口被拒绝。
  - 管理员访问高危写接口通过。
- 前端构建：`npm run build`。

## 4. 执行策略

建议按小提交推进：

1. `fix(v5): correct candidate capacity hard check`
2. `fix(core): harden null handling in scheduling flows`
3. `fix(score): scope score reports by semester`
4. `fix(v5): align repair task pending status`
5. `fix(db): add missing tables and repair soft-delete indexes`
6. `fix(security): escape reports and constrain downloads`
7. `fix(security): add CSP and minimal write protection`

每个提交都要能单独解释、单独回滚。

## 5. 验证命令

基础验证：

```powershell
cd D:\paike\backend
$env:JWT_SECRET='test_secret_32_bytes_minimum_value_123'
mvn test
```

前端验证：

```powershell
cd D:\paike\frontend
npm run build
```

数据库验证：

```powershell
cd D:\paike
node scripts/verify-soft-delete-plan.js
```

如果需要接口级验证，由用户按项目约定手动启动后端：

```powershell
cd D:\paike\backend
mvn spring-boot:run
```

启动成功后再执行 API smoke 或 Playwright 测试。

## 6. 开工建议

先从 P0 的纯后端逻辑修复开始：

1. 修 C-10。
2. 修 NPE 组。
3. 补对应单测。
4. 跑定向单测。

这部分不涉及 SQL 和权限模型，风险最低，收益最大。
