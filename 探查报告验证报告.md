# 探查报告验证报告

> 生成时间：2026-05-23
> 被验证报告：`D:\paike\深度代码探查报告.md`（共 64 条）
> 验证方式：逐条回到源码核对（codegraph + 直接 Read + 多 Agent 并行交叉验证）
>
> **裁定口径**
> - **属实**：源码与报告描述一致，问题真实存在；
> - **部分属实**：事实层面成立，但报告的影响判断或后果夸大 / 已有缓解；
> - **不属实**：源码与报告描述明显不一致，或报告依据错误的假设；
> - **路径偏差**：报告中文件路径/行号有出入但问题客观存在。

---

## 总评

| 优先级 | 属实 | 部分属实 | 不属实 | 合计 |
|--------|------|----------|--------|------|
| **P0**  | 4 | 3 | 2 | 9  |
| **P1**  | 14 | 3 | 2 | 19 |
| **P2**  | 24 | 3 | 1 | 28 |
| **P3**  | 7 | 1 | 0 | 8  |
| **合计** | **49** | **10** | **5** | **64** |

整体而言，报告 ~77% 完全属实，~15% 部分属实，~8% 不属实。**报告对 P0 安全/数据问题的判断有几处明显错误**（特别是 1.1、1.7、1.9），需注意纠正。其余多数低中等优先级问题客观存在，可作为整改输入。

---

## 一、P0 严重问题（9 项）逐条核对

### 1.1 [安全] /v3/** 接口完全绕过认证 —— **不属实** ❌

- **报告说**：`addPathPatterns("/api/**")` 仅拦截 `/api/**`，存在裸 `/v3/` 前缀绕过认证。
- **实际**：`WebMvcConfig.java:27` 确实写的是 `addPathPatterns("/api/**")`，但 grep 全部 8 个 v3 Controller 都是 `@RequestMapping("/api/v3/xxx")`（如 `SchedulePlanController:23`、`ScheduleScoreController:16`、`SemesterController:15` 等），**不存在裸 `/v3/` 前缀**。所有 v3 接口均被 `/api/**` 拦截器覆盖。
- **结论**：误报。

### 1.2 [安全] 全部控制器缺少细粒度授权 —— **属实** ✅

- 验证：grep 整个 backend 模块，`@PreAuthorize` / `@Secured` 0 命中；`controller/` 目录下共 38 个 Controller，没有任何方法级别授权注解。
- 仅 `AuthInterceptor` 做"已登录"粗粒度校验，但无角色 / 资源粒度。

### 1.3 [安全] LoginResponse token 被 @JsonIgnore —— **部分属实** ⚠️

- **事实层**：`LoginResponse.java:10` 确实有 `@JsonIgnore`。
- **影响层**：报告称"登录功能形同虚设" —— 错误判断。`AuthController.java:44-51` 通过 `ResponseCookie.from("paike_token", token).httpOnly(true)...` 以 HttpOnly Cookie 形式下发 JWT，是有意为之的防 XSS 设计（store 注释也明确说明）。`@JsonIgnore` 是配套设计，避免 token 同时落在响应体（XSS 可读）。
- **结论**：事实正确，但"形同虚设"是误读，应改为"by design"。

### 1.4 [数据] schema.sql 仅定义 8/30+ 张表 —— **属实** ✅

- 验证：`schema.sql` grep `CREATE TABLE` 仅 8 处（sys_user / teacher / class_info / classroom / course / teaching_task / time_slot / schedule）。
- `application.yml:21` `schema-locations` 列出 14 个 .sql 文件按顺序执行；另有 `migration/V1__baseline.sql`（Flyway 占位，已禁用）。
- "极易引入偏差"判断合理，分散管理是事实。

### 1.5 [数据] 多个迁移脚本使用 DELIMITER 语法 —— **部分属实** ⚠️

- 验证：grep `DELIMITER` 命中 `v2_alter_schedule.sql`、`v6_bugfix_constraints.sql`、`v6_schedule_index.sql`、`v6_semester_current_unique.sql`、`v7_soft_delete_plan_semester.sql`，确实使用存储过程模式。
- **缓解措施**：`application.yml:24` 设置 `continue-on-error: true`；`SemesterSchemaInitializer` 兜底；`README.md` 注释"v2/v6 早期文件用 DROP PROCEDURE 风格，对老库重跑可能局部报错但语义已满足"。
- **结论**：脚本写法确属高风险（Spring `ScriptUtils` 默认按 `;` 切分 SQL，DELIMITER 块会被错切），但当前依赖 `continue-on-error + Java 兜底` 软化为可运行状态。不是"永远无法添加"。

### 1.6 [数据] 6 个 Entity 字段在 schema.sql 中无对应列 —— **属实** ✅

- 验证：
  - `Schedule.java:21` `semesterId`、`:40` `sourceType`、`:42` `batchId`、`:44` `planId` —— `schema.sql:117-128` schedule 表均无对应列；
  - `TeachingTask.java:21` `semesterId` —— `schema.sql:71-83` teaching_task 表无 `semester_id`。
- 这些列由 `v2_alter_schedule.sql` / `v3_semester_data_bind.sql` 等迁移脚本添加。若新环境跳过迁移直接初始化 schema.sql，Entity insert 必然 SQLException。
- 注：`ScheduleScoreReport.java:41` 的 `gradeName` 实为展示字段，不该列入此条，但其它 5 处确实成立。

### 1.7 [逻辑] 自动排课事务隔离导致日上限失效 —— **不属实** ❌

- **报告说**：`checkTeacherDailyLimit`/`checkClassDailyLimit` 在 `@Transactional` 内"先插再查"，REPEATABLE READ 下看不到本轮插入。
- **实际**：MySQL InnoDB 在 REPEATABLE READ 下**支持 read-own-writes**，同事务/同连接可见自己已写入但未提交的行。`AutoScheduleService.java:365-368` 的注释明确说明这一点："同一连接读得到（MySQL 的 read-own-writes），无需额外 batchId 过滤"。代码逻辑正确。
- **结论**：报告对 MySQL 隔离级别的理解有误。

### 1.8 [逻辑] `AutoScheduleService.clearAll()` 跨学期误删 —— **属实** ✅

- 验证：`UnscheduledTaskService.java:112-114`：
  ```java
  public void clearAll() {
      unscheduledTaskMapper.delete(new LambdaQueryWrapper<>());
  }
  ```
  无任何条件，物理/逻辑删全部 `unscheduled_task` 记录。
- `AutoScheduleService.java:62` 和 `:72` 在清空当前学期 schedule 后无差别调用 `clearAll()`，确实会清空其他学期的未排课记录。

### 1.9 [逻辑] `SchedulePlanService.adjustPlanItem` NPE —— **不属实** ❌

- **报告说**：`request.getAdjustReason()` 可能为 null，line 180 调用 `.trim()` 抛 NPE。
- **实际**：
  - DTO `SchedulePlanItemAdjustRequest.java:26` 标注 `@NotBlank(message = "调整原因不能为空")`；
  - Controller `SchedulePlanItemController.java:26` 入参带 `@Valid`；
  - `GlobalExceptionHandler` 会先把 `MethodArgumentNotValidException` 转成 400。
- adjustReason 不可能进到 service 层时为 null/空。
- **结论**：NPE 链路不成立。

---

## 二、P1 高优先级（19 项）逐条核对

### 2.1 JwtService 未校验密钥最小长度 —— **部分属实** ⚠️

- `JwtService.java:25-28` 只检查 `isBlank` / 是否为默认占位值，没有显式 `length() >= 32` 校验。
- **缓解**：line 30 `Keys.hmacShaKeyFor(secret.getBytes(UTF_8))` 是 jjwt 库的标准 API，会在密钥长度 < 256 bit (32 字节) 时**自己抛 `WeakKeyException`**，启动期失败。
- 报告事实正确，但兜底机制存在，影响层属"启动期抛错，运行期不会过弱"。

### 2.2 明文管理员密码输出到 stdout —— **属实** ✅

- `AdminUserInitializer.java:74` `System.out.println("随机初始密码：" + password)`。注释（line 70）声称"避免明文密码持久化到 logs/ 目录"，但容器环境 stdout 仍会被采集到日志聚合系统。

### 2.3 12 个端点 @RequestBody 缺少 @Valid —— **属实** ✅

- 验证 grep `@RequestBody` 共 41 处。无 `@Valid` 的端点（>= 12 处实测）：
  - `AutoScheduleBatchController.java:44`、`ScheduleAiAnalysisController.java:24`
  - `ScheduleRuleWeightController.java:54,60`、`ScheduleRuleController.java:26`
  - `ScheduleLockController.java:19,25`、`ScheduleGenerateController.java:21,26`
  - `ScheduleReplanController.java:24`、`ScheduleReportController.java:31`
  - `V5CandidatePositionController.java:18`、`V5RepairSuggestionController.java:25`
  - `V5SimulationController.java:31`、`V5RepairTaskController.java:51`
- 报告数字保守可信。

### 2.4 裸实体直接作为 @RequestBody —— **属实** ✅

- `TeacherUnavailableTimeController.java:34,40,52` 直接以 `TeacherUnavailableTime`（MyBatis-Plus Entity，带 `id/deleted/createTime`）作为入参。
- `ScheduleRuleWeightController.java:60` 用 `List<ScheduleRuleWeight>`（Entity）。
- 攻击者可覆盖 `id/deleted/createTime` 等内部字段。

### 2.5 RateLimiterService 内存限流器无容量上限 —— **部分属实** ⚠️

- `RateLimiterService.java:17` `ConcurrentHashMap<String, Deque<Long>>` 无硬上限。
- **缓解**：`cleanupExpiredKeys`（line 53-64）会清空已过期的 Deque 并移除空 entry，配合 `cleanupIntervalMs` 周期触发。
- 但攻击者用每秒轮换不同 username/IP 可在窗口内塞入大量 key，仍存在 OOM 路径。报告方向正确。

### 2.6 排课循环内 N+1 查询（4 处）—— **属实**（3/4 处）✅

| 报告处 | 验证结果 |
|--------|----------|
| `AutoScheduleService.java:397-401` getTimeSlotIdsByDay 在双层循环内重复查 | **属实** |
| `V3ScheduleGenerateService.java:568-575` getCourseType/getClassStudentCount | **属实**（line 293-294 任务循环 + sortTasks 比较器内反复 selectById） |
| `UnscheduledTaskService.java:130-143` fillRelationFields | **属实**（line 133/135/137 逐条 selectById） |
| `ScheduleStatisticsService.java:77-78` teacherWorkload | **部分属实**：使用 `teacherCache.computeIfAbsent`，每个 teacherId 仅查一次，严格说不是"N+1"，但仍是逐条而非批量 |

### 2.7 TeacherUnavailableTimeService 缺事务边界 —— **属实** ✅

- `TeacherUnavailableTimeService.java:103` `create`、`:145` `update`、`updateStatus` 类方法均无 `@Transactional`。重复性检查 + insert 存在并发竞态。

### 2.8 ScheduleRuleService.updateRules 无事务保护 —— **属实** ✅

- `ScheduleRuleService.java:29-52` `updateRules` 方法无 `@Transactional`，循环内逐条 `updateById`。

### 2.9 AutoScheduleBatchService.updateBatchResult 无事务 —— **属实** ✅

- `AutoScheduleBatchService.java:54-65` 先 `selectById` 再 `updateById`，无 `@Transactional`。

### 2.10 前端 auth store token 残留状态 —— **部分属实** ⚠️

- `stores/auth.ts:14` `const token = ref<string>('')`、`:29` 登录成功后调 `clearToken()`。事实成立。
- **影响层**：store 顶部注释明确说明 token 实际通过 HttpOnly Cookie 传递，前端 `token` ref 是历史遗留，并不参与实际鉴权。所以"任何代码依赖 token.value"不成立，这只是死代码。
- 建议保留报告作为"清理死代码"。

### 2.11 响应拦截器对 blob 下载请求误处理 —— **不属实** ❌

- `utils/request.ts:69-91` 拦截器：line 71 `if (isJsonBlob(response.data))` 优先识别 `application/json` 类型 Blob，line 72-79 解析后正确进入错误分支。
- 普通文件 Blob（非 JSON content-type）直接 `return response`，不会误用 `payload.code`。
- 代码已正确处理 blob 错误响应路径。

### 2.12 ScheduleScoreController.rescore 缺少 null 检查 —— **不属实** ❌

- `ScheduleScoreController.java:54` 调用 `planService.getById(planId)`，而 `SchedulePlanService.java:64-70` `getById` 在 plan 为 null 时直接 `throw new BusinessException("排课方案不存在")`，由全局异常处理转 4xx 返回，根本走不到后续 `scoreService.rescore(plan)`。
- 报告把它当作裸 `selectById` 看待，事实错误。

### 2.13 ScheduleController.create 中 task 未做 null 检查 —— **属实** ✅

- `ScheduleController.java:99-102`：
  ```java
  TeachingTask task = teachingTaskMapper.selectById(form.getTeachingTaskId());
  Schedule schedule = new Schedule();
  schedule.setSemesterId(task.getSemesterId());  // <-- task 可能 null
  ```
- 报告位置略有偏差（实际是 99-106 范围），但 NPE 风险成立。

### 2.14 错误处理不一致 —— **属实** ✅

- 三种模式并存：
  - `throw BusinessException`：主流；
  - `Result.fail(...)`：`Result.java:24/28` 静态方法存在；
  - `catch + 静默返回空`：`ScheduleAdjustLogController.java:35-36`、`ScheduleRuleWeightController.java:33-34` 在 `BusinessException` 捕获后返回空 Page/空 List，掩盖了"获取当前学期失败"的根因。

### 2.15 多个页面 fetchData 缺少 catch —— **属实** ✅

- `ScheduleRuleWeightView.vue:41-51`、`ScheduleConflictReportView.vue:77-95`、`ScheduleView.vue:85-98`、`SchedulePlanView.vue:42-55` 均为 `try { ... } finally { loading.value = false }`，无 catch。错误依赖拦截器全局 toast，组件层无补救逻辑。

### 2.16 handleSubmit 未 await fetchData —— **属实** ✅

- `composables/useCrudForm.ts:121` `fetchData()` 调用未 `await`。报告路径正确。

### 2.17 全局超时 10 秒对长操作不合理 —— **属实** ✅

- `utils/request.ts:24` `timeout: 10000`。自动排课、报告生成等操作显然 > 10s。

### 2.18 路由守卫 fetchCurrentUser 竞态条件 —— **属实** ✅

- `router/index.ts:222-229`：在 `!userInfo` 时直接 `await authStore.fetchCurrentUser()`，无 in-flight 标记。短时间内 2 个并发导航会发 2 个 `/auth/me`。

### 2.19 useCrudForm handleDelete 重复显示错误消息 —— **属实** ✅

- `useCrudForm.ts:138-139` 在 `catch` 内 `ElMessage.error(extractMessage(...))`，而 `request.ts:87/103` 拦截器路径也调用 `ElMessage.error(...)`，删除接口出错时确实会双弹。

---

## 三、P2 中等问题（28 项）逐条核对

### 3.1-3.6 安全与配置

| # | 结论 | 备注 |
|---|------|------|
| 3.1 | **属实** ✅ | `CorsConfig.java:32` `setAllowedHeaders(List.of("*"))` |
| 3.2 | **属实** ✅ | `application.yml:47` `cookie-secure: ${COOKIE_SECURE:false}`，生产需手动开 |
| 3.3 | **属实** ✅ | `LoginRequest.java` 仅 `@NotBlank`，无 `@Size`，超长输入不挡 |
| 3.4 | **属实** ✅ | `SecurityConfig.java` 只声明 `BCryptPasswordEncoder` Bean，无 `SecurityFilterChain`，缺安全响应头 |
| 3.5 | **属实** ✅ | `GlobalExceptionHandler.java:27` `HttpStatus.resolve(ex.getCode())`，业务码 = HTTP 码 |
| 3.6 | **属实** ✅ | `Result.java:12` `Integer code` 可 null，未加 `@JsonInclude(NON_NULL)` |

### 3.7-3.10 硬编码魔法数字

| # | 结论 | 备注 |
|---|------|------|
| 3.7 | **属实** ✅ | `V4ScheduleRiskService.java:49-54` 18/22/8/10 全部硬编码 |
| 3.8 | **属实** ✅ | `V4ScheduleChartService.java:42` `TOTAL_AVAILABLE_PERIODS = 20` |
| 3.9 | **属实** ✅ | `ScheduleScoreService.java:315` `>= 5` 作下午分界 |
| 3.10 | **属实** ✅ | `V4ScheduleAnalysisService.java:90,96,106,108` 阈值 18/8/85/30 硬编码 |

### 3.11-3.14 空指针风险

| # | 结论 | 备注 |
|---|------|------|
| 3.11 | **属实** ✅ | `ScheduleConflictReportService.java:658-659` `getDayOfWeek` slot==null 时返回 null，分组 key 风险 |
| 3.12 | **部分属实** ⚠️ | `V4ScheduleAdjustmentService.java:361` 有 `currentSlot != null` 守卫，但 `getPeriodNo()` 返回 Integer 可能为 null，`*2-1` 拆箱仍 NPE。报告定位偏差但风险点存在 |
| 3.13 | **不属实** ❌ | `SchedulePlanService.java:570-575` 后续访问 `slot.getId()` 处实际上做了 `task != null && slot != null` 守卫（line 572），不会 NPE |
| 3.14 | **属实** ✅ | `CourseType.java:25`、`RoomType.java:25`、`ScheduleSourceType.java:24` 三个 `fromCode` 均 `return null` |

### 3.15-3.18 数据库与 Entity

| # | 结论 | 备注 |
|---|------|------|
| 3.15 | **属实** ✅ | V1: `Schedule/Teacher/Course/TeachingTask` 用 `create_time/update_time`；V3+: `SchedulePlan/SchedulePlanItem/Semester` 用 `created_at/updated_at`。两套命名并行 |
| 3.16 | **部分属实** ⚠️ | 报告说 "14 张表" 偏低，实测 entity/ 30 个文件中只有 11 个带 `@TableLogic`，另 ~19 个无软删除（包括 `ScheduleLockedItem`、`ScheduleAdjustLog` 等），方向正确数字偏少 |
| 3.17 | **属实** ✅ | `AutoScheduleBatch.java:39` `createTime` 无 `@TableField`，且无 `updateTime` 字段 |
| 3.18 | **属实** ✅ | `schema.sql:71-83` teaching_task 表无任何二级索引；`v6_schedule_index.sql` 只为 schedule 表加索引，teaching_task 仍裸表 |

### 3.19-3.22 代码重复

| # | 结论 | 备注 |
|---|------|------|
| 3.19 | **属实** ✅ | `SchedulePlanService.applyPlan`(267-356) 与 `applyPlanInternal`(373-446) 几乎逐行重复，仅入口校验不同 |
| 3.20 | **属实** ✅ | `AutoScheduleService` 与 `V3ScheduleGenerateService` 共享 `sortTasks/checkTeacherDailyLimit/checkClassDailyLimit/hasSameCourseSameDay/isRoomTypeMatched` 等同名同结构方法 |
| 3.21 | **属实** ✅ | `ScheduleScoreReportService`（基于 schedule 表的扣分制）与 `ScheduleScoreService`（基于 plan_item + rule_weight 的加权制）两套独立评分体系并行 |
| 3.22 | **属实** ✅ | `fallback<T>(...)` 在 `TeachingTaskView.vue:63`、`ScheduleView.vue:78`、`ScheduleStatisticsView.vue:35`、`ScheduleRuleWeightView.vue:13`、`SchedulePlanView.vue:35` 共 5 处重复定义 |

### 3.23-3.28 前端

| # | 结论 | 备注 |
|---|------|------|
| 3.23 | **部分属实** ⚠️ | `BaseLayout.vue:62` `:default-active="router.currentRoute.value.path"`：`currentRoute` 是 shallowRef，模板内会追踪；且 el-menu 在 router 模式下另有内部高亮逻辑，实际未必失效，但写法语义模糊（`default-active` ≠ `active`） |
| 3.24 | **属实** ✅ | `useCrudForm.ts:113` `if (editingId.value)` 当 id=0 误判。实际数据库自增很少为 0，但代码缺陷成立 |
| 3.25 | **属实** ✅ | `request.ts:5-9` 与 `api/types.ts:8-12` 各自定义 `ApiResponse`，结构相同 |
| 3.26 | **不属实** ❌ | `ScheduleRuleView.vue` 全文只有一个 `const rules = ref<ScheduleRuleConfig[]>([])`（line 8），没有第二个 `rules` 声明覆盖。报告引用的 line 11/31 在实际文件中不对应 |
| 3.27 | **属实** ✅ | `api/index.ts` 未 re-export 至少 9 个模块：`v4ScheduleAnalysisApi / v4ScheduleAdjustmentApi / v4ScheduleLockApi / v4ScheduleReplanApi / v4ScheduleReportApi / v4ScheduleAiApi / v5SimulationApi / scheduleConflictReport / scheduleScoreReport` |
| 3.28 | **不属实** ❌ | `utils/errors.ts` 的 `isCancel` 被 `ScheduleLockManage.vue`、`TeachingTaskView.vue`、`SchedulePlanView.vue`、`ScheduleAdjustDialog.vue` 至少 4 处引用，不是死代码 |

---

## 四、P3 低优先级（8 项）逐条核对

| # | 结论 | 备注 |
|---|------|------|
| 4.1 | **属实** ✅ | `SemesterSchemaInitializer.java` 多处 `log.warn("...: {}", e.getMessage())`，未把 `e` 作为最后参数，堆栈丢失（line 52/65/82/98/111/149/176/207/230/263/300/329/347） |
| 4.2 | **属实** ✅ | `V5RepairExplanationService.java:443-447` `__keepObjects()` 仅做 `Objects.hash(this)`，`@SuppressWarnings("unused")`，全仓库无调用 |
| 4.3 | **属实** ✅ | 全部 .vue 文件 grep `aria-` / `role=` 命中 0，依赖 Element Plus 内置 a11y |
| 4.4 | **属实** ✅ | views/ 下 27 个 Vue 文件含 128 处 `style="..."`（ScheduleView.vue 10 处、AutoScheduleView.vue 17 处、ScheduleCompareView.vue 11 处） |
| 4.5 | **属实** ✅ | `components/v4/EChartPanel.vue:75-77` `new ResizeObserver(() => chart?.resize())` 无 debounce |
| 4.6 | **部分属实** ⚠️ | `views/v4/ScheduleAiAnalysis.vue:97` 用 `document.execCommand('copy')`，但仅作为 `navigator.clipboard.writeText` 失败的兜底（try/catch 内），非唯一手段 |
| 4.7 | **不属实** ❌ | `views/v5/SimulationPlanDetailView.vue` 实测 662 行（含 `<style>`），未超过 700。路径也少了 `v5/` 子目录 |
| 4.8 | **属实** ✅ | `App.vue` 仅 3 行 `<template><RouterView /></template>`，无 `<script>` / `onErrorCaptured`，整个前端无任何错误边界 |

---

## 五、原报告"修复优先级建议"复核

报告末尾给出的 20 条修复建议，按本验证结果重新打分：

### 应立即修复（验证后仍成立）

- ✅ **add @PreAuthorize / RBAC**（对应 1.2）—— 完全成立，整改优先级最高；
- ✅ **AutoScheduleService.clearAll() 添加学期过滤**（对应 1.8）—— 真实数据风险；
- ✅ **schema.sql + 迁移脚本整合或上 Flyway**（对应 1.4 / 1.5 / 1.6）—— 维护风险真实存在；
- ✅ **12 个 Controller 补 @Valid**（对应 2.3）—— 校验绕过真实存在；
- ✅ **裸实体 @RequestBody 改 DTO**（对应 2.4）—— 字段覆盖风险真实存在；
- ✅ **排课循环 N+1 改批量**（对应 2.6）—— 3/4 处确认；
- ✅ **TeacherUnavailableTimeService / ScheduleRuleService / AutoScheduleBatchService 加 @Transactional**（2.7-2.9）；
- ✅ **统一错误处理模式**（2.14）；
- ✅ **teaching_task 加外键索引**（3.18）；
- ✅ **JwtService 显式长度校验**（2.1，jjwt 已兜底，建议添加更明确的报错）；

### 验证后判断为优先级降级或无需改

- ❌ **WebMvcConfig 加 /v3/**`** —— 误报，不需要改（1.1）；
- ❌ **LoginResponse 移除 @JsonIgnore** —— 误报，HttpOnly Cookie 是 by design（1.3）；
- ❌ **AutoScheduleService 事务隔离改造** —— MySQL read-own-writes 已支持，不需要改（1.7）；
- ❌ **SchedulePlanService.adjustPlanItem null 检查** —— DTO + @Valid 已覆盖，不需要改（1.9）；
- ❌ **ScheduleScoreController.rescore null 检查** —— getById 已抛业务异常（2.12）；
- ❌ **request.ts blob 处理修复** —— 现有实现已正确（2.11）；
- ❌ **errors.ts isCancel 死代码** —— 实际有 4 处使用（3.28）。

### 整体可继续推进

- ✅ 前端错误边界（4.8）、a11y（4.3）、内联样式整治（4.4）、防抖（4.5）、execCommand 兜底降级保留（4.6）等改造方向都成立。

---

## 六、给原报告的反馈

1. **MySQL 隔离级别认知错误**（1.7）是最严重的一处，应在后续审计中重点强化数据库基础理论。
2. **混淆"接口设计"与"安全漏洞"**（1.1、1.3）：报告未注意 `/api/v3/` 实际前缀，也忽视 HttpOnly Cookie 是已知安全实践，把 `@JsonIgnore` 单独读出来下了过重结论。
3. **未交叉验证 DTO + @Valid 链路**（1.9、2.12）：建议在 NPE 类报错前先确认是否存在前置校验/抛出。
4. **行号引用偶有错位**（3.26、4.7、3.16 数字偏低、3.12 字段定位偏差）：建议下次列入"具体行号 + 关键代码片段（≤ 3 行）" 双证。
5. **总体把握仍可信**：49/64 完全属实、10/64 方向正确，作为整改输入价值很高，但请按本验证报告剔除/降级误报项。

---

## 七、修复建议

按照"修复成本 / 风险收益"维度，把所有验证通过的问题拆分成 4 个批次。每条标注**改动范围、估计工时、具体动作**。

> 规则：
> - **批次 A（本周）** = 真实安全/数据风险，1 天内可完成；
> - **批次 B（本迭代）** = 功能正确性问题，每项 2-8 小时；
> - **批次 C（下一迭代）** = 维护性 / 一致性，可抽时间集中处理；
> - **批次 D（待规划）** = 涉及架构层调整，需要单独立项。

---

### 批次 A — 立即修复（建议 1-2 天内全部清掉）

#### A1. `clearAll()` 加学期过滤（对应 1.8）

- **文件**：`backend/.../service/UnscheduledTaskService.java:112-114`、`AutoScheduleService.java:62,72`
- **动作**：
  1. `UnscheduledTaskService.clearAll()` 改名为 `clearBySemester(Long semesterId)`，内部 `LambdaQueryWrapper` 加 `.eq(UnscheduledTask::getSemesterId, semesterId)`；
  2. `AutoScheduleService:62,72` 改成传入当前 `semesterId`；
  3. 若 `UnscheduledTask` 实体没有 `semesterId` 字段，先通过 `batchId` 关联 `auto_schedule_batch` 表过滤。
- **估计**：1.5h（含 mapper 单测）。

#### A2. 12 个 Controller 补 `@Valid`（对应 2.3）

- **文件**：见 2.3 节列出的 12 处 `@RequestBody`
- **动作**：直接在每个 `@RequestBody` 前加 `@Valid`，同时审核对应 DTO 是否已有 `@NotNull / @NotBlank / @Min / @Max / @Size` 注解；缺则补齐。
- **估计**：1h（机械改动）。

#### A3. 裸实体 `@RequestBody` 换 Form DTO（对应 2.4）

- **文件**：
  - `TeacherUnavailableTimeController.java:34,40,52`
  - `ScheduleRuleWeightController.java:60`
- **动作**：
  1. 新增 `TeacherUnavailableTimeForm`、`ScheduleRuleWeightBatchForm` DTO，只暴露必要业务字段（不含 `id/deleted/createTime/updateTime`）；
  2. Service 层接收 Form 后再装配 Entity；
  3. 用 `@Valid` 校验。
- **估计**：2h。

#### A4. 排课关键 Service 加 `@Transactional`（对应 2.7-2.9）

- **文件**：
  - `TeacherUnavailableTimeService.java:103/145` `create/update/updateStatus`
  - `ScheduleRuleService.java:29` `updateRules`
  - `AutoScheduleBatchService.java:54` `updateBatchResult`
- **动作**：方法签名加 `@Transactional(rollbackFor = Exception.class)`；其中 `TeacherUnavailableTimeService.create` 的"重复性校验 + insert"如果对并发要求高，再补 `try { insert } catch (DuplicateKeyException)` 用 DB 唯一索引兜底。
- **估计**：0.5h。

#### A5. JwtService 显式校验密钥长度（对应 2.1）

- **文件**：`backend/.../auth/JwtService.java:25`
- **动作**：在 `secret.isBlank()` 之后追加：
  ```java
  if (secret.getBytes(StandardCharsets.UTF_8).length < 32) {
      throw new IllegalStateException("JWT_SECRET 长度必须 >= 32 字节");
  }
  ```
  比 jjwt 抛 `WeakKeyException` 更早、报错更直观。
- **估计**：0.2h。

---

### 批次 B — 本迭代修复（建议 1 周内完成）

#### B1. 排课循环 N+1 改批量查询（对应 2.6）

- **文件**：
  - `AutoScheduleService.java:397-401` (`getTimeSlotIdsByDay`)
  - `V3ScheduleGenerateService.java:568-575` (`getCourseType / getClassStudentCount`)
  - `UnscheduledTaskService.java:130-143` (`fillRelationFields`)
- **动作**：
  1. 进入主循环前一次性 `selectList` 全部 timeSlot/course/teacher/classInfo，转 `Map<Long, Entity>`；
  2. 循环内只走 `map.get(id)`；
  3. `AutoScheduleService` 的 `timeSlots` 已经在外层加载（line 102），把按 `dayOfWeek` 分组缓存成 `Map<Integer, List<Long>>`，把 `getTimeSlotIdsByDay` 改成 `slotIdsByDay.get(dayOfWeek)`。
- **估计**：4h。

#### B2. 统一错误处理模式（对应 2.14）

- **文件**：
  - `ScheduleAdjustLogController.java:35-36`
  - `ScheduleRuleWeightController.java:33-34`
  - 全工程 grep `catch (BusinessException` 看是否还有别处
- **动作**：把"catch + 返回空列表"改成直接抛出，由 `GlobalExceptionHandler` 统一处理。若是"当前学期未配置"这种业务可允许的场景，应返回明确的 4xx 错误码 + 提示文案，而不是静默返回 200 空 list。
- **估计**：2h。

#### B3. `ScheduleController.create` 加 task null 检查（对应 2.13）

- **文件**：`ScheduleController.java:99`
- **动作**：
  ```java
  TeachingTask task = teachingTaskMapper.selectById(form.getTeachingTaskId());
  if (task == null || task.getDeleted() == 1) {
      throw new BusinessException(400, "教学任务不存在");
  }
  ```
- **估计**：0.1h。

#### B4. 前端列表 fetchData 补 catch + handleSubmit await（对应 2.15 / 2.16）

- **文件**：
  - `views/schedule/ScheduleRuleWeightView.vue:41`、`ScheduleConflictReportView.vue:77`、`ScheduleView.vue:85`、`SchedulePlanView.vue:42`
  - `composables/useCrudForm.ts:121`
- **动作**：
  1. 4 个 fetchData 加 `catch (e) { ElMessage.error(extractMessage(e, '加载失败')) }`；
  2. `useCrudForm.ts:121` 改成 `await fetchData()`。
- **估计**：1h。

#### B5. 拦截器 + handleDelete 错误提示去重（对应 2.19）

- **文件**：`composables/useCrudForm.ts:138-139`
- **动作**：删除 catch 块中的 `ElMessage.error(...)`，让拦截器统一弹一次。
- **估计**：0.1h。

#### B6. 路由守卫 fetchCurrentUser 加 in-flight 锁（对应 2.18）

- **文件**：`router/index.ts:222`、`stores/auth.ts`
- **动作**：在 `auth` store 里加一个 `fetchingUser: Promise<UserInfo> | null` 字段，路由守卫调用前先复用 in-flight Promise，结束后清空。
- **估计**：0.5h。

#### B7. 全局 timeout 分级（对应 2.17）

- **文件**：`utils/request.ts:24`
- **动作**：保留默认 10s；为自动排课、报告生成等长接口在调用处显式传 `{ timeout: 60_000 }` 或更长。建议封装 `requestLong = request.create({ timeout: 120_000 })`。
- **估计**：1h（含调用方梳理）。

---

### 批次 C — 下一迭代修复（按主题集中处理）

#### C1. 数据库 schema 治理（对应 1.4 / 1.5 / 1.6 / 3.17 / 3.18）

- 推荐方案：**开启 Flyway**（项目里已经有 `migration/V1__baseline.sql` 占位）
- **动作**：
  1. 把 `schema.sql` + 所有 `v2/v3/v5/v6/v7` 脚本整合进 `V1__baseline.sql`（DELIMITER 块改为 `SET @ddl=IF(...); PREPARE/EXECUTE` 风格）；
  2. `application.yml` 关掉 `spring.sql.init`、打开 `spring.flyway.enabled=true`；
  3. `AutoScheduleBatch.java` 补 `@TableField("create_time")` 与 `updateTime` 字段；
  4. `teaching_task` 表补三个外键索引：`idx_tt_course / idx_tt_teacher / idx_tt_class`，作为新 `V2__teaching_task_index.sql`。
- **估计**：1-2 天（含本地/测试环境验证）。

#### C2. 安全加固（对应 3.1-3.5 / 1.2 / 3.6）

- **动作**：
  1. `CorsConfig.java:32` `allowedHeaders` 收紧到 `Content-Type / X-CSRF-Token / Authorization`；
  2. 增加 `SecurityConfig` 的 `SecurityFilterChain`，启用标准安全响应头（`X-Content-Type-Options`、`X-Frame-Options`、`Strict-Transport-Security` 等）；
  3. `LoginRequest` 补 `@Size(max = 64)` 限制用户名/密码长度；
  4. 引入 RBAC：先定义 3-4 个角色（admin / scheduler / readonly），在关键 DELETE/PUT/排课触发端点加 `@PreAuthorize("hasRole('xxx')")`；
  5. `Result.java` 补 `@JsonInclude(JsonInclude.Include.NON_NULL)`；
  6. `GlobalExceptionHandler` 的 `BusinessException` 业务码与 HTTP 状态码解耦：保留 `code` 为业务码，统一返回 200/400/401/403/500。
- **估计**：3-5 天（RBAC 是大头）。

#### C3. 空指针 / 死代码清理（对应 3.11 / 3.12 / 3.14 / 4.2）

- **动作**：
  1. `ScheduleConflictReportService.java:382/413/658` `getDayOfWeek` 加 null 兜底，slot 为 null 时跳过该条；
  2. `V4ScheduleAdjustmentService.java:363` 给 `periodNo` 加 null 守卫；
  3. 三个 enum `fromCode` 改成抛 `BusinessException("未知 code: " + code)`；
  4. 删除 `V5RepairExplanationService.__keepObjects()`、`stores/auth.ts` 的 `token` ref。
- **估计**：2h。

#### C4. 前端死代码 / 一致性清理（对应 3.22 / 3.25 / 3.27 / 4.4）

- **动作**：
  1. 把 5 处 `fallback<T>` 提到 `utils/asyncHelpers.ts`，统一 import；
  2. 删除 `request.ts:5-9` 的 `ApiResponse` 重复定义，统一从 `api/types.ts` 引；
  3. 补全 `api/index.ts` 的 9 个 v4/v5 模块 re-export，并把现有相对导入改成 barrel 导入；
  4. ScheduleView/AutoScheduleView/ScheduleCompareView 等内联 style 大户改为 scoped CSS class。
- **估计**：1 天。

#### C5. 硬编码常量 → 配置化（对应 3.7-3.10）

- **动作**：把 `V4ScheduleRiskService`、`V4ScheduleAnalysisService`、`V4ScheduleChartService`、`ScheduleScoreService` 的阈值（18/22/8/10/85/30/20/5 等）抽到 `schedule_rule_config` 表或 `application.yml` 的 `app.schedule.thresholds.*` 节点。
- **估计**：1 天。

---

### 批次 D — 待规划立项

#### D1. 排课引擎统一（对应 3.19 / 3.20）

- 现状：`SchedulePlanService.applyPlan / applyPlanInternal` 100+ 行重复；`AutoScheduleService / V3ScheduleGenerateService` 排课算法重复。
- **动作**：
  1. 抽 `applyPlanInternal` 为 protected，`applyPlan` 内部直接调用；
  2. 抽 `core/scheduling/` 包，提取共享 `Scheduler` 引擎 + 不同 `OutputStrategy`（直接写 schedule / 写 plan_item）；
  3. 关键路径必须有冲突检测、规则校验、N+1 防护的单测。
- **估计**：1 周 + QA。

#### D2. 评分体系收口（对应 3.21）

- 现状：`ScheduleScoreReportService`（按 schedule 表扣分）与 `ScheduleScoreService`（按 plan_item + rule_weight 加权）并行，可能给同一份排课不同的分数。
- **动作**：
  1. 与产品/教务对齐"哪种评分对外可见"；
  2. 保留主链路，另一条改为只读统计或废弃；
  3. 写迁移工具回填旧数据。
- **估计**：3-5 天。

#### D3. 前端组件拆分 + 错误边界（对应 4.7 / 4.8 / 3.23）

- **动作**：
  1. `SimulationPlanDetailView.vue`（662 行）按业务拆为 `SimulationOverview / SimulationConflictTable / SimulationActionPanel` 等子组件；
  2. `App.vue` 包一层 `<ErrorBoundary>`，全局捕获 `onErrorCaptured`，渲染统一兜底页面 + 上报；
  3. `BaseLayout.vue:62` `el-menu :default-active` 改为 `:active`，使用 `computed(() => route.path)`。
- **估计**：2-3 天。

#### D4. 软删除补齐 + ScheduleLockedItem 物理删除审查（对应 3.16）

- 19 个无 `@TableLogic` 的 Entity 中，`ScheduleLockedItem`、`ScheduleAdjustLog` 等业务表存在被误物理删除的风险，需要：
  1. 评估每张表是否真的"删了就没了"也可接受；
  2. 关键表加 `deleted` 字段 + `@TableLogic`，对应 migration 加列；
  3. 已有 `deleteById` 调用点回归测试。
- **估计**：2 天（含数据评估）。

---

### 不要修复 / 已驳回项

以下是验证报告中已判定为**误报**的条目，明确**不应**进入修复列表：

- 1.1 WebMvcConfig `/v3/**` 拦截路径（实际路径都是 `/api/v3/`）
- 1.3 `LoginResponse` 移除 `@JsonIgnore`（HttpOnly Cookie 是 by design）
- 1.7 AutoScheduleService 事务隔离改造（MySQL 已支持 read-own-writes）
- 1.9 SchedulePlanService.adjustPlanItem 加 null 检查（@Valid 已挡）
- 2.11 request.ts blob 处理（已正确）
- 2.12 ScheduleScoreController.rescore null 检查（getById 已抛）
- 3.13 SchedulePlanService.java:570 slot.getId() NPE（已有 null 守卫）
- 3.26 ScheduleRuleView.vue `rules` 变量名冲突（不存在第二个 rules 声明）
- 3.28 errors.ts `isCancel` 死代码（实际 4 处引用）
- 4.7 SimulationPlanDetailView 700+ 行（实测 662 行）

提交修复 PR 时，请在 PR 描述里明确引用本验证报告的小节编号（如 `A2 / 报告 2.3`），方便 reviewer 对照。

