# 修复计划 2026-05-22

目标：按风险从高到低修复排课系统的正确性、安全性、并发一致性和维护性问题。

## 总原则

1. 先修会影响数据正确性和安全边界的问题。
2. 先统一学期口径，再处理派生统计、报表和试算链路。
3. 所有修复都要配套最小化验证，不只看代码是否能编译。
4. 不做大重构，优先局部修复和补测试。

## 第一阶段：P0

### 1. 修复正式排课和自动排课的核心正确性

- `AutoScheduleService.saveSchedule` 补写 `semesterId`。
- `SchedulePlanService.applyPlan` 增加应用前冲突复检。
- `SchedulePlanService.applyPlan`、`SchedulePlanService.rollbackPlan` 明确回滚语义，避免自我覆盖。
- `ScheduleConflictService.checkConflict` 和自动排课的冲突口径统一到同一学期。

验收：
- 自动排课生成的 `schedule` 都带正确 `semester_id`。
- 冲突校验、应用、回滚在同一学期口径下输出一致结果。
- 状态：已完成首轮修复。`applyPlan` 增加应用前冲突复检；`rollbackPlan` 避免重复应用时自我覆盖；锁定课程在调整/应用同步链路中会被拦截。
- 收尾状态：完工。本轮补齐编辑场景 `excludeScheduleId` 到每日上限/同课同日统计链路，避免被编辑记录参与自身冲突计数。

### 2. 修复安全边界

- 前端 token 存储从 `localStorage` 迁移到服务端 cookie 主导方案，避免双存储混用。
- `AuthService.login` 改为先检查状态再做密码校验，减少账号枚举信息泄露。
- 登录限流保留，但避免全量扫描成为热点。

验收：
- 浏览器不再依赖 `localStorage` 作为主认证态。
- 停用账号和不存在账号的返回行为一致。
- 状态：已完成首轮修复。登录返回不再向前端暴露 token；前端改为 cookie 主导，`withCredentials` 已开启；停用账号与不存在账号统一走 401；登录状态由 `/auth/me` 维持；登录限流过期 key 清理已改为节流触发，避免每次请求全量扫描。

### 3. 修复锁定机制

- `V4ScheduleLockService.lock` 加唯一约束或等效并发保护。
- 主要写入链路在落库前检查锁定状态。
- 明确哪些入口允许绕过锁，哪些入口绝不允许。

验收：
- 并发锁定不会插入重复 active 记录。
- 已锁定课程不能被普通排课/调整链路覆盖。
- 状态：已完成首轮修复。锁定/解锁入口已改为串行事务执行，写入前复查 active 锁；`SchedulePlanService` 的调整、正式应用同步都补了锁拦截。
- 收尾状态：完工。本轮补齐 `schedule_locked_item` 的 active generated column 和 active 唯一约束，并将重复锁定插入异常转换为业务错误。

## 第二阶段：P1

### 4. 统一学期与统计口径

- `AutoScheduleService` 的每日上限、同日同课规则补上学期过滤。
- `ScheduleConflictReportService.generate` 按学期生成报告。
- `ScheduleController.create`、`checkConflict`、批量应用、报表生成统一使用同一学期来源。

验收：
- 跨学期数据不会被合并到同一统计结果。
- 同一个教学任务在不同入口得到一致判断。
- 状态：已完成本轮修复。`AutoScheduleService` 已按当前/指定学期清理旧课表、读取任务、检查教师/班级日上限、检查同日同课，并在保存 `schedule` 时写入任务学期；`ScheduleConflictReportService.generate` 默认按当前学期生成，接口支持传入 `semesterId`；`ScheduleController.create` 保存时使用教学任务学期，避免和冲突检测口径不一致。

### 5. 修复应用、调整、试算链路的事务一致性

- `V4ScheduleAdjustmentService.checkAdjustment` / `applyAdjustment` 增加并发保护。
- `V5SimulationService.localReplan`、`apply`、`discard` 明确状态转换和清理规则。
- `copyLocks` 的复制上限和重复复制策略要有明确边界。

验收：
- 试算方案不会无限膨胀。
- 调整和应用不会出现“先检查后被别人改掉”的竞态。
- 状态：已完成本轮修复。`V4ScheduleAdjustmentService.applyAdjustment` 已串行化检查+写入，减少预检和保存之间的并发窗口；`V5SimulationService.apply` 应用后回写试算方案 `APPLIED` 状态，`discard` 拦截重复放弃；`copyLocks` 对同一新方案明细去重并跳过已有 active 锁，避免反复复制锁记录。
- 收尾状态：完工。本轮移除 `applyAdjustment` 外层 `@Transactional`，保留 `TransactionTemplate` + `synchronized`，避免 synchronized 释放早于事务提交。

## 第三阶段：P2

### 6. 降低维护成本

- `useCrudForm.openAdd` 改为深拷贝默认值。
- `ScheduleScoreService` 的冲突类型识别改为结构化字段，不再依赖字符串包含。
- `ScheduleConflictReportService.clear` 改成精确删除，不用前缀 `LIKE`。
- `ScheduleRuleService.ruleCache` 评估多实例缓存失效策略。

验收：
- 默认表单不会被上一次编辑污染。
- 报告清理不会误删其他编号。
- 状态：已完成本轮修复。`useCrudForm.openAdd` 已改为 `structuredClone` 深拷贝默认值；`ScheduleScoreService` 已把禁排/容量/类型计数收敛到内部冲突类型归一化判断，避免直接 `contains`；`ScheduleConflictReportService.clear` 已改为删除精确 base reportNo 或 `base-数字序号`，避免前缀误删；schema/初始化入口已核对，并补齐遗漏的 `v6_bugfix_constraints.sql` 到 `schema-locations`，不做大范围迁移重构。

### 7. 收敛 schema 和初始化方式

- 统一 `schema.sql`、初始化器和迁移方式。
- 若继续保留多个初始化入口，必须明确执行顺序和职责边界。

验收：
- 新环境和已有环境的初始化结果一致。
- 状态：已完成小范围收敛。当前保留 `schema.sql` + 增量 SQL + `SemesterSchemaInitializer` 兼容旧库的模式，仅修正已存在但未加载的约束脚本入口；多入口重构留到独立迁移方案处理。

## 建议执行顺序

1. `P0-3`、`P0-4`、`P0-6`、`P0-7`
2. `P0-1`、`P1-1`、`P1-2`、`P1-5`、`P1-8`
3. `P1-10`、`P1-11`、`P1-12`、`P1-13`、`P1-15`
4. `P2` 全部清理项

## 验证要求

每个阶段结束后都要做三类检查：

1. 单元或接口验证。
2. 关键路径人工核对。
3. 报表或列表页面回归检查。

## 风险提示

- 不要在未确认学期口径前先修统计逻辑，否则容易出现“修一个点，坏一片”。
- 不要只改前端提示，不改后端校验。
- 不要把锁定机制只做在展示层，必须落到写入链路。

## 回归验证记录 2026-05-22

### 修复提交（按时间序）

| Commit | 说明 |
|---|---|
| 436c60e | P0 verification batch 1（前端 cookie 鉴权 / applyPlan 冲突复检 / 锁定拦截首轮 / rollback 自我覆盖 / 限流节流 / 账号枚举） |
| 261c047 | 学期口径统一（saveSchedule semesterId / ScheduleController.create / AutoSchedule daily-limit / ConflictReport 按学期） |
| 5903417 | 事务一致性收紧（V4 调整串行化 / V5 apply 回写 APPLIED / discard 拒重复 / copyLocks 去重） |
| 5a512d6 | P2 维护项（clear 精确匹配 / 评分结构化匹配 / openAdd 深拷贝 / 激活 v6_bugfix_constraints） |
| 31f8beb | 收尾批次（V4 调整删 @Transactional / excludeScheduleId 透传 / schedule_locked_item 唯一索引） |
| cba8098 | V4 调整路径补锁定预检 |

### 端到端回归结果

| 场景 | 结果 | 备注 |
|---|---|---|
| 1 认证链路 | ✅ | 登录/刷新/登出全链路；localStorage 无 token，HttpOnly cookie 正常 |
| 2 自动排课 semester_id | ✅ | AUTO schedule 的 semester_id 全部为当前学期，无 NULL，无 DuplicateKey |
| 3 应用方案前冲突复检 | ✅ | 冲突方案 400 拦截，无冲突方案应用成功 |
| 4 编辑排课同日移动节次 | ✅ | V4 调整链路同日内移节次保存成功，无每日上限误报 |
| 5 锁定拦截 | ✅ | V4 调整接口对已锁定明细返回 400「该课程已锁定，不能调整」 |
| 6 锁定唯一索引 | ✅ | uk_locked_plan_item 阻止重复 active 锁记录 |
| 7 冲突报告按学期 | ✅ | 报告仅包含指定学期的 schedule |

### 阶段进度

- P0（1/2/3）：✅ 全部完成
- P1-4 学期口径：✅ 完成
- P1-5 事务一致性：✅ 完成
- P2-6 部分清理：✅ 完成
- P2-7 schema 收敛：⬜ 未开始

### 剩余 backlog（按性价比）

1. P0-5 — SchedulePlan / SchedulePlanItem / Semester 加 @TableLogic ✅ 完成（090501a）
2. P0-6 剩余路径 — ScheduleController.delete、AutoScheduleService.clearAllSchedule 补锁拦截 ✅ 完成（3cf0f94）
3. P1-5 软规则范围 — ScheduleConflictService 软规则只看 plan_id IS NULL ✅ 完成（3cf0f94）
4. P1-11 — 前端 confirm 文案与后端 force 参数对齐 ✅ 完成（8d09019）
5. P1-13 — V5 findBestLocalPlacement 性能（百万 SQL 风险）⬜ 未开始
6. P2-15 — discard 不清理 simulation plan 的孤儿数据 ✅ 完成（2026-05-23）
7. P2-7 — 三套 schema 演进收敛 ⬜ 未开始
8. V4ScheduleLockService 拆 JVM synchronized（DB 唯一索引已上）✅ 完成（da45c09）
9. ScheduleAdjustmentController message 覆盖问题 ✅ 完成（3cf0f94）

### 待跟进事项 2026-05-22

- **v6_bugfix_constraints.sql 真实生效状态** ✅ 2026-05-23 人工核查通过。
  当前开发库 5 项 DDL 全部生效：
  - `teacher_unavailable_time.active_key` + `uk_teacher_timeslot(teacher_id, time_slot_id, active_key)`
  - `schedule_plan_item.uk_plan_task_slot(plan_id, teaching_task_id, weekday, start_period, end_period)`
  - `schedule_locked_item.active_key` + `uk_locked_plan_item(plan_item_id, active_key)` + `uk_locked_schedule(schedule_id, active_key)`

  说明：`DELIMITER + CREATE PROCEDURE` 在 Spring `ScriptUtils` 不是正式支持语法，本机生效带运气成分。
  本项目是自用开发，DDL 已持久化在 DB，不再改写为 v7 风格。
  如未来需要部署到新环境，部署前手动跑一次 SHOW INDEX / SHOW COLUMNS 核查即可。

### 增量修复记录 2026-05-22（晚）

- 已完成：`ScheduleController.delete` 补锁定拦截；`AutoScheduleService.clearAllSchedule` / `clearOldAutoSchedule` 清空前补锁定预检。
- 已完成：`ScheduleConflictService` 调整为“硬冲突/周课时统计看同学期全部正式课表与已应用方案；软规则每日上限/同课同日仍只看 `plan_id IS NULL`”。
- 已完成：`ScheduleAdjustmentController.apply` 优先透传 service 返回 `message`，不再统一覆盖成“检测完成”。
- 已完成：`scripts/api-smoke-test.js` 适配 HttpOnly cookie + `XSRF-TOKEN` / `X-CSRF-Token` 鉴权链路，不再把 JSON token 作为登录成功唯一判据。
- 实现说明：新增 `ScheduleLockGuardService` 统一复用正式课表锁 / 关联方案明细锁判断，替换 `SchedulePlanService`、`V4ScheduleAdjustmentService` 内部重复逻辑。

### 本轮验证

- `backend`: `mvn -DskipTests compile` ✅
- `scripts/api-smoke-test.js`: `node --check` ✅
- 端到端回归：`npm run smoke:api` ✅；已验证删除已锁定 schedule 返回 400「该课程已锁定，不能删除」、`clearAllSchedule` 返回 400「存在已锁定课程，不能清空当前排课结果」、V4 调整接口对锁定项返回 400「该课程已锁定，不能调整」。

### 增量修复记录 2026-05-22（深夜）

| Commit | 说明 |
|---|---|
| 3cf0f94 | 抽取 ScheduleLockGuardService，补 ScheduleController.delete / AutoScheduleService.clear*Schedule 锁拦截；ScheduleConflictService 调整为硬冲突/周课时统计看全部，软规则仍 plan_id IS NULL；ScheduleAdjustmentController 不再覆盖 service 返回 message；scripts/api-smoke-test.js 适配 cookie + XSRF |
| 8d09019 | P1-11 前端 apply confirm 与后端 strict reject 对齐（SchedulePlanDetailView / ScheduleCompareView），有冲突直接 alert 硬挡不发请求 |
| da45c09 | V4ScheduleLockService 移除 JVM synchronized，依赖 DB 唯一索引兜底；unlock 改为 WHERE active_flag=1 条件更新 + rows==0 抛 BusinessException |
| 46bf36d | 新增 scripts/verify-lock-refactor.js 一次性脚本，串行 5 步覆盖 lock-dup-lock / unlock-dup-unlock / lock-unlock-lock 循环，全绿 |
| 090501a | P0-5 SchedulePlan / SchedulePlanItem / Semester 三张表加 deleted 列与 @TableLogic；新增 v7_soft_delete_plan_semester.sql 幂等迁移；新增 scripts/verify-soft-delete-plan.js |

验证：
- 全部 commit：mvn -DskipTests compile ✅
- 前端 commit：vue-tsc -b ✅
- verify-lock-refactor.js：5 步全绿，cleanup 已清理测试锁
- verify-soft-delete-plan.js：5 步全绿（plan id=70 + 291 items + semester id=5 实测 deleted=1）
- npm run smoke:api：16/16 通过

### 增量修复记录 2026-05-23

| 改动 | 说明 |
|---|---|
| V5SimulationService.discard | P2-15：放弃试算方案时清理孤儿数据。先生成详情快照再清理，避开 V4ScheduleRiskService 对空 items 的 selectBatchIds 空 IN 报错。软删除 schedule_plan_item（依赖 @TableLogic）、`active_flag=0` 翻 schedule_locked_item、物删 schedule_score_detail；保留 schedule_optimization_compare 与 schedule_adjust_log 作为审计快照 |
| ScheduleRepairTask.resultPlanId | 加 `@TableField(updateStrategy = FieldStrategy.IGNORED)`，让 discard 时 `setResultPlanId(null)` 能真正写入 NULL（修复 MyBatis-Plus 默认 NOT_NULL 策略导致 null 字段被忽略的副作用） |
| V4ScheduleRiskService.buildContext | 抽取 `loadByIds` helper，空 id 列表直接返回 `Map.of()`，修复 plan 0 items 时 `selectBatchIds([])` 触发 `WHERE id IN ()` SQL 语法错的潜在 bug |
| v6_bugfix_constraints.sql | 人工核查通过：5 项 DDL（active_key 列 + 4 个唯一索引）全部生效，不改写为 v7 风格 |

端到端验证（后端重启后实测 plan 64 + task 5）：
- POST /api/v5/repair-tasks/5/simulations/64/discard → 200，返回 275 items 快照
- DB 复查：plan 64 status=DISCARDED；275 plan_items 软删；7 score_details 物删；optimization_compare/adjust_log 保留；task 5 status=SUGGESTED 且 result_plan_id=NULL
- GET /api/v5/repair-tasks/5/simulations/64（0 active items 状态）→ 200，risks.riskCount=0，验证空 IN bug 已修

历史孤儿清理（本机）：
- DISCARDED plan id=74：275 条 schedule_plan_item 软删除、7 条 schedule_score_detail 物删、optimization_compare id=3 作为审计保留
- 复查：discarded_plans=1，orphan_items_active=0，orphan_locks_active=0，orphan_scores=0
