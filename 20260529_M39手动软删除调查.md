# M-39 手动软删除调查

## 结论

M-39 部分成立，已修复。

原报告写的是 `SchedulePlanService` 手动 `setDeleted(1)` 绕过 `@TableLogic`。当前代码里已经没有 `.setDeleted(1)` 字面调用，但在应用新方案时，旧正式课表仍通过 `LambdaUpdateWrapper.set(Schedule::getDeleted, 1)` 批量置删除标记。

该写法语义上仍属于手动写 `deleted=1`，不如 MyBatis-Plus 逻辑删除 API 明确。

## 本轮修改

将 `SchedulePlanService.applyPlanInternal()` 中覆盖旧正式课表的逻辑从：

- `scheduleMapper.update(null, new LambdaUpdateWrapper<Schedule>().set(Schedule::getDeleted, 1)...)`

改为：

- `scheduleMapper.delete(new LambdaQueryWrapper<Schedule>().eq(Schedule::getSemesterId, semesterId).eq(Schedule::getPlanId, oldPlan.getId()))`

这样由 `Schedule.deleted` 上的 `@TableLogic` 处理逻辑删除语义，与项目中其他批量删除路径保持一致。

## 影响范围

- 只影响“应用新方案时，将旧 APPLIED 方案对应正式课表退役”的路径。
- 旧方案状态仍改回 `DRAFT`。
- 新方案正式课表插入逻辑不变。
- 不改变数据库结构。

## 本轮验证

- 新增 `M39ManualDeletedUpdateInvestigationTest`，锁定：
  - 主代码不再出现 `.setDeleted(1)`。
  - 主代码不再出现 `set(Schedule::getDeleted, 1)`。
  - `SchedulePlanService` 使用 `scheduleMapper.delete(new LambdaQueryWrapper<Schedule>()...)` 退役旧正式课表。

